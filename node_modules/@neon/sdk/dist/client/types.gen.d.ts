//#region src/client/types.gen.d.ts
type ClientOptions = {
  baseUrl: 'https://console.neon.tech/api/v2' | (string & {});
};
type Features = {
  [key: string]: boolean;
};
type FeatureFlags = {
  [key: string]: boolean | string;
};
type TelemetryConnection = {
  /**
   * Required. Communication protocol used to send telemetry data. Options: grpc, http.
   * For gRPC, the endpoint must include an explicit port.
   *
   */
  protocol: 'grpc' | 'http';
  /**
   * Required. URI of the OpenTelemetry Collector (e.g., https://collector.customer.com:4317).
   *
   */
  endpoint: string;
  authentication: TelemetryAuthentication;
};
type TelemetryAuthentication = {
  bearer_token?: {
    token: string;
  };
  basic?: {
    username: string;
    password: string;
  };
  api_key?: {
    key: string;
    /**
     * Optional. Custom header name for the API key. If not specified, defaults to "X-API-Key".
     * This allows using custom header names like "Authorization", "X-Custom-Key", etc.
     *
     */
    header_name?: string;
  };
};
type TelemetryConfig = {
  /**
   * Required. The telemetry data types to enable. One or both of: metrics, logs.
   *
   */
  types: Array<'metrics' | 'logs'>;
  /**
   * Optional. Overrides the default endpoint for metrics (e.g., https://metrics.customer.com:4317).
   *
   */
  metrics_endpoint_override?: string;
  /**
   * Optional. Overrides the default endpoint for logs (e.g., https://logs.customer.com:4318).
   *
   */
  logs_endpoint_override?: string;
};
type TelemetryResource = {
  /**
   * Optional. Key-value attributes that describe the source of telemetry (e.g., service.name: neon-test).
   * See: @https://opentelemetry.io/docs/specs/semconv/resource/#services
   *
   */
  attributes?: {
    [key: string]: string;
  };
};
type ComputeUnit = number;
/**
 * The Neon compute provisioner.
 * Specify the `k8s-neonvm` provisioner to create a compute endpoint that supports Autoscaling.
 *
 * Provisioner can be one of the following values:
 * * k8s-pod
 * * k8s-neonvm
 * * serverless-platform
 *
 * Clients must expect, that any string value that is not documented in the description above should be treated as a error. UNKNOWN value if safe to treat as an error too.
 *
 */
type Provisioner = string;
type PaginationResponse = {
  pagination?: Pagination;
};
/**
 * Cursor based pagination is used. The user must pass the cursor as is to the backend.
 * For more information about cursor based pagination, see
 * https://learn.microsoft.com/en-us/ef/core/querying/pagination#keyset-pagination
 *
 */
type Pagination = {
  cursor: string;
};
/**
 * Empty response.
 */
type EmptyResponse = {
  [key: string]: unknown;
};
type PlanDetails = {
  name: string;
  version?: PlanVersion;
};
type PlanVersion = {
  major: number;
  minor: number;
};
/**
 * Add a new JWKS to a specific endpoint of a project
 */
type AddProjectJwksRequest = {
  /**
   * The URL that lists the JWKS
   */
  jwks_url: string;
  /**
   * The name of the authentication provider (e.g., Clerk, Stytch, Auth0)
   */
  provider_name: string;
  /**
   * Branch ID
   */
  branch_id?: string;
  /**
   * The name of the required JWT Audience to be used
   */
  jwt_audience?: string;
  /**
   * DEPRECATED. This field should only be used when using Neon RLS. The roles the JWKS should be mapped to. By default, the JWKS is mapped to the `authenticator`, `authenticated` and `anonymous` roles.
   *
   * @deprecated
   */
  role_names?: Array<string>;
  /**
   * DEPRECATED. This field should only be used when using Neon RLS. If true, the role creation will be skipped.
   */
  skip_role_creation?: boolean;
};
type Jwks = {
  /**
   * JWKS ID
   */
  id: string;
  /**
   * Project ID
   */
  project_id: string;
  /**
   * Branch ID
   */
  branch_id?: string;
  /**
   * The URL that lists the JWKS
   */
  jwks_url: string;
  /**
   * The name of the authentication provider (e.g., Clerk, Stytch, Auth0)
   */
  provider_name: string;
  /**
   * The date and time when the JWKS was created
   */
  created_at: string;
  /**
   * The date and time when the JWKS was last modified
   */
  updated_at: string;
  /**
   * The name of the required JWT Audience to be used
   */
  jwt_audience?: string;
  role_names?: Array<string>;
};
/**
 * The list of configured JWKS definitions for a project
 */
type ProjectJwksResponse = {
  jwks: Array<Jwks>;
};
/**
 * Category of an advisor issue
 */
type AdvisorCategory = 'SECURITY' | 'PERFORMANCE';
type AdvisorIssue = {
  /**
   * Unique identifier for the issue type
   */
  name: string;
  /**
   * Human-readable title
   */
  title: string;
  /**
   * Severity level of the issue
   */
  level: string;
  /**
   * Whether this issue affects external API exposure or is internal only
   */
  facing: 'EXTERNAL' | 'INTERNAL';
  /**
   * Categories this issue belongs to
   */
  categories: Array<AdvisorCategory>;
  /**
   * Detailed description of what this check detects
   */
  description: string;
  /**
   * Specific details about this instance of the issue
   */
  detail: string;
  /**
   * URL to documentation on how to fix this issue
   */
  remediation: string;
  /**
   * Additional context-specific metadata
   */
  metadata: {
    [key: string]: unknown;
  };
  /**
   * Unique key for caching this issue result
   */
  cache_key: string;
};
type ApiKeyCreateRequest = {
  /**
   * A user-specified API key name. This value is required when creating an API key.
   */
  key_name: string;
};
type OrgApiKeyCreateRequest = ApiKeyCreateRequest & {
  /**
   * If set, the API key can access only this project
   */
  project_id?: string;
};
type ApiKeyCreateResponse = {
  /**
   * The API key ID
   */
  id: number;
  /**
   * The generated 64-bit token required to access the Neon API
   */
  key: string;
  /**
   * The user-specified API key name
   */
  name: string;
  /**
   * A timestamp indicating when the API key was created
   */
  created_at: string;
  /**
   * ID of the user who created this API key
   */
  created_by: string;
};
type OrgApiKeyCreateResponse = ApiKeyCreateResponse & {
  /**
   * If set, the API key can access only this project
   */
  project_id?: string;
};
type ApiKeyRevokeResponse = {
  /**
   * The API key ID
   */
  id: number;
  /**
   * The user-specified API key name
   */
  name: string;
  /**
   * A timestamp indicating when the API key was created
   */
  created_at: string;
  /**
   * ID of the user who created this API key
   */
  created_by: string;
  /**
   * A timestamp indicating when the API was last used
   */
  last_used_at?: string | null;
  /**
   * The IP address from which the API key was last used
   */
  last_used_from_addr: string;
  /**
   * A `true` or `false` value indicating whether the API key is revoked
   */
  revoked: boolean;
};
type OrgApiKeyRevokeResponse = ApiKeyRevokeResponse & {
  /**
   * If set, the API key can access only this project
   */
  project_id?: string;
};
type ApiKeysListResponseItem = {
  /**
   * The API key ID
   */
  id: number;
  /**
   * The user-specified API key name
   */
  name: string;
  /**
   * A timestamp indicating when the API key was created
   */
  created_at: string;
  created_by: ApiKeyCreatorData;
  /**
   * A timestamp indicating when the API was last used
   */
  last_used_at?: string | null;
  /**
   * The IP address from which the API key was last used
   */
  last_used_from_addr: string;
};
type OrgApiKeysListResponseItem = ApiKeysListResponseItem & {
  /**
   * If set, the API key can access only this project
   */
  project_id?: string;
};
/**
 * The user data of the user that created this API key.
 */
type ApiKeyCreatorData = {
  /**
   * ID of the user who created this API key
   */
  id: string;
  /**
   * The name of the user.
   */
  name: string;
  /**
   * The URL to the user's avatar image.
   */
  image: string;
};
type Operation = {
  /**
   * The operation ID
   */
  id: string;
  /**
   * The Neon project ID
   */
  project_id: string;
  /**
   * The branch ID
   */
  branch_id?: string;
  /**
   * The endpoint ID
   */
  endpoint_id?: string;
  action: OperationAction;
  status: OperationStatus;
  /**
   * The error that occurred
   */
  error?: string;
  /**
   * The number of times the operation failed
   */
  failures_count: number;
  /**
   * A timestamp indicating when the operation was last retried
   */
  retry_at?: string;
  /**
   * A timestamp indicating when the operation was created
   */
  created_at: string;
  /**
   * A timestamp indicating when the operation status was last updated
   */
  updated_at: string;
  /**
   * The total duration of the operation in milliseconds
   */
  total_duration_ms: number;
};
type OperationResponse = {
  operation: Operation;
};
type OperationsResponse = {
  operations: Array<Operation>;
};
/**
 * The action performed by the operation
 */
type OperationAction = 'create_compute' | 'create_timeline' | 'start_compute' | 'suspend_compute' | 'apply_config' | 'check_availability' | 'delete_timeline' | 'create_branch' | 'import_data' | 'tenant_ignore' | 'tenant_attach' | 'tenant_detach' | 'tenant_reattach' | 'replace_safekeeper' | 'disable_maintenance' | 'apply_storage_config' | 'prepare_secondary_pageserver' | 'switch_pageserver' | 'detach_parent_branch' | 'timeline_archive' | 'timeline_unarchive' | 'start_reserved_compute' | 'sync_dbs_and_roles_from_compute' | 'apply_schema_from_branch' | 'timeline_mark_invisible' | 'timeline_update_protected_config' | 'prewarm_replica' | 'promote_replica' | 'set_storage_non_dirty' | 'swap_binding_id' | 'finalize_migration' | 'mark_migration_prepared';
/**
 * The status of the operation
 */
type OperationStatus = 'scheduling' | 'running' | 'finished' | 'failed' | 'error' | 'cancelling' | 'cancelled' | 'skipped';
/**
 * Essential data about the project. Full data is available at the getProject endpoint.
 *
 */
type ProjectListItem = {
  /**
   * The project ID
   */
  id: string;
  /**
   * The cloud platform identifier. Currently, only AWS is supported, for which the identifier is `aws`.
   *
   */
  platform_id: string;
  /**
   * The region identifier
   *
   */
  region_id: string;
  /**
   * The project name
   *
   */
  name: string;
  provisioner: Provisioner;
  default_endpoint_settings?: DefaultEndpointSettings;
  settings?: ProjectSettingsData;
  pg_version: PgVersion;
  /**
   * The proxy host for the project. This value combines the `region_id`, the `platform_id`, and the Neon domain (`neon.tech`).
   *
   */
  proxy_host: string;
  /**
   * The logical size limit for a branch. The value is in MiB.
   *
   */
  branch_logical_size_limit: number;
  /**
   * The logical size limit for a branch. The value is in B.
   *
   */
  branch_logical_size_limit_bytes: number;
  /**
   * Whether or not passwords are stored for roles in the Neon project. Storing passwords facilitates access to Neon features that require authorization.
   *
   */
  store_passwords: boolean;
  /**
   * Control plane observed endpoints of this project being active this amount of wall-clock time.
   *
   */
  active_time: number;
  /**
   * DEPRECATED. Use data from the getProject endpoint instead.
   *
   *
   * @deprecated
   */
  cpu_used_sec: number;
  /**
   * A timestamp indicating when project maintenance begins. If set, the project is placed into maintenance mode at this time.
   *
   */
  maintenance_starts_at?: string;
  /**
   * The project creation source
   *
   */
  creation_source: string;
  /**
   * A timestamp indicating when the project was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the project was last updated
   *
   */
  updated_at: string;
  /**
   * The current space occupied by the project in storage, in bytes. Synthetic storage size combines the logical data size and Write-Ahead Log (WAL) size for all branches in a project.
   *
   */
  synthetic_storage_size?: number;
  /**
   * DEPRECATED. Use `consumption_period_end` from the getProject endpoint instead.
   * A timestamp indicating when the project quota resets
   *
   *
   * @deprecated
   */
  quota_reset_at?: string;
  owner_id: string;
  /**
   * The most recent time when any endpoint of this project was active.
   *
   * Omitted when observed no activity for endpoints of this project.
   *
   */
  compute_last_active_at?: string;
  /**
   * Organization id if the project belongs to an organization.
   * Permissions for the project will be given to organization members as defined by the organization admins.
   * The permissions of the project do not depend on the user that created the project if a project belongs to an organization.
   *
   */
  org_id?: string;
  /**
   * Organization name if the project belongs to an organization.
   *
   */
  org_name?: string;
  /**
   * The number of seconds to retain the shared history for all branches in this project.
   *
   */
  history_retention_seconds?: number;
  /**
   * A timestamp indicating when HIPAA was enabled for this project
   */
  hipaa_enabled_at?: string;
  /**
   * A timestamp indicating when the project was deleted
   */
  deleted_at?: string;
  /**
   * A timestamp indicating the project will be recoverable until this date and time.
   */
  recoverable_until?: string;
  effective_project_permission?: ProjectPermissionLevel | null;
};
type Project = {
  /**
   * Bytes-Hour. Project consumed that much storage hourly during the billing period. The value has some lag.
   * The value is reset at the beginning of each billing period.
   *
   */
  data_storage_bytes_hour: number;
  /**
   * Bytes. Egress traffic from the Neon cloud to the client for given project over the billing period.
   * Includes deleted endpoints. The value has some lag. The value is reset at the beginning of each billing period.
   *
   */
  data_transfer_bytes: number;
  /**
   * Bytes. Amount of WAL that travelled through storage for given project across all branches.
   * The value has some lag. The value is reset at the beginning of each billing period.
   *
   */
  written_data_bytes: number;
  /**
   * Seconds. The number of CPU seconds used by the project's compute endpoints, including compute endpoints that have been deleted.
   * The value has some lag. The value is reset at the beginning of each billing period.
   * Examples:
   * 1. An endpoint that uses 1 CPU for 1 second is equal to `compute_time=1`.
   * 2. An endpoint that uses 2 CPUs simultaneously for 1 second is equal to `compute_time=2`.
   *
   */
  compute_time_seconds: number;
  /**
   * Seconds. Control plane observed endpoints of this project being active this amount of wall-clock time.
   * The value has some lag.
   * The value is reset at the beginning of each billing period.
   *
   */
  active_time_seconds: number;
  /**
   * DEPRECATED, use compute_time instead.
   *
   *
   * @deprecated
   */
  cpu_used_sec: number;
  /**
   * The project ID
   */
  id: string;
  /**
   * The cloud platform identifier. Currently, only AWS is supported, for which the identifier is `aws`.
   *
   */
  platform_id: string;
  /**
   * The region identifier
   *
   */
  region_id: string;
  /**
   * The project name
   *
   */
  name: string;
  provisioner: Provisioner;
  default_endpoint_settings?: DefaultEndpointSettings;
  settings?: ProjectSettingsData;
  pg_version: PgVersion;
  /**
   * The proxy host for the project. This value combines the `region_id`, the `platform_id`, and the Neon domain (`neon.tech`).
   *
   */
  proxy_host: string;
  /**
   * The logical size limit for a branch. The value is in MiB.
   *
   */
  branch_logical_size_limit: number;
  /**
   * The logical size limit for a branch. The value is in B.
   *
   */
  branch_logical_size_limit_bytes: number;
  /**
   * Whether or not passwords are stored for roles in the Neon project. Storing passwords facilitates access to Neon features that require authorization.
   *
   */
  store_passwords: boolean;
  /**
   * A timestamp indicating when project maintenance begins. If set, the project is placed into maintenance mode at this time.
   *
   */
  maintenance_starts_at?: string;
  /**
   * The project creation source
   *
   */
  creation_source: string;
  /**
   * The number of seconds to retain the shared history for all branches in this project.
   *
   */
  history_retention_seconds: number;
  /**
   * A timestamp indicating when the project was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the project was last updated
   *
   */
  updated_at: string;
  /**
   * The current space occupied by the project in storage, in bytes. Synthetic storage size combines the logical data size and Write-Ahead Log (WAL) size for all branches in a project.
   *
   */
  synthetic_storage_size?: number;
  /**
   * A date-time indicating when Neon Cloud started measuring consumption for current consumption period.
   *
   */
  consumption_period_start: string;
  /**
   * A date-time indicating when Neon Cloud plans to stop measuring consumption for current consumption period.
   *
   */
  consumption_period_end: string;
  /**
   * DEPRECATED. Use `consumption_period_end` from the getProject endpoint instead.
   * A timestamp indicating when the project quota resets.
   *
   *
   * @deprecated
   */
  quota_reset_at?: string;
  owner_id: string;
  owner?: ProjectOwnerData;
  /**
   * The most recent time when any endpoint of this project was active.
   *
   * Omitted when observed no activity for endpoints of this project.
   *
   */
  compute_last_active_at?: string;
  org_id?: string;
  /**
   * A timestamp indicating when project update begins. If set, computes might experience a brief restart around this time.
   *
   */
  maintenance_scheduled_for?: string;
  /**
   * A timestamp indicating when HIPAA was enabled for this project
   */
  hipaa_enabled_at?: string;
  effective_project_permission?: ProjectPermissionLevel | null;
};
type ProjectCreateRequest = {
  project: {
    settings?: ProjectSettingsData;
    /**
     * The project name. If not specified, the name will be identical to the generated project ID
     */
    name?: string;
    branch?: {
      /**
       * The default branch name. If not specified, the default branch name, `main`, will be used.
       *
       */
      name?: string;
      /**
       * The role name. If not specified, the default role name, `{database_name}_owner`, will be used.
       *
       */
      role_name?: string;
      /**
       * The database name. If not specified, the default database name, `neondb`, will be used.
       *
       */
      database_name?: string;
      annotations?: AnnotationValueData;
    };
    autoscaling_limit_min_cu?: ComputeUnit;
    autoscaling_limit_max_cu?: ComputeUnit;
    provisioner?: Provisioner;
    /**
     * The region identifier. Refer to our [Regions](https://neon.com/docs/introduction/regions) documentation for supported regions. Values are specified in this format: `aws-us-east-1`
     *
     */
    region_id?: string;
    default_endpoint_settings?: DefaultEndpointSettings;
    pg_version?: PgVersion;
    /**
     * Whether or not passwords are stored for roles in the Neon project. Storing passwords facilitates access to Neon features that require authorization.
     *
     */
    store_passwords?: boolean;
    /**
     * The number of seconds to retain the shared history for all branches in this project.
     * The default is 1 day (86400 seconds).
     *
     */
    history_retention_seconds?: number;
    /**
     * Organization id in case the project created belongs to an organization.
     * If not present, project is owned by a user and not by org.
     *
     */
    org_id?: string;
  };
};
type ProjectUpdateRequest = {
  project: {
    settings?: ProjectSettingsData;
    /**
     * The project name
     */
    name?: string;
    default_endpoint_settings?: DefaultEndpointSettings;
    /**
     * The number of seconds to retain the shared history for all branches in this project.
     * The default is 1 day (604800 seconds).
     *
     */
    history_retention_seconds?: number;
  };
};
type ProjectTransferRequestResponse = {
  /**
   * The unique identifier for the transfer request
   */
  id: string;
  /**
   * The ID of the project that is being transferred
   */
  project_id: string;
  /**
   * The timestamp when the transfer request was created
   */
  created_at: string;
  /**
   * The timestamp when the transfer request will expire
   */
  expires_at: string;
};
type AcceptProjectTransferRequestSatisfiesPlanError = {
  reasons: Array<{
    /**
     * Description of why the plan is not satisfied
     */
    message: string;
    /**
     * A short code identifying the reason
     */
    code: string;
  }>;
};
type ProjectSettingsData = {
  quota?: ProjectQuota;
  allowed_ips?: AllowedIps;
  /**
   * Sets wal_level=logical for all compute endpoints in this project.
   * All active endpoints will be suspended.
   * Once enabled, logical replication cannot be disabled.
   *
   */
  enable_logical_replication?: boolean;
  maintenance_window?: MaintenanceWindow;
  /**
   * When set, connections from the public internet
   * are disallowed. This supersedes the AllowedIPs list.
   * This parameter is under active development and its semantics may change in the future.
   *
   */
  block_public_connections?: boolean;
  /**
   * When set, connections using VPC endpoints are disallowed.
   * This parameter is under active development and its semantics may change in the future.
   *
   */
  block_vpc_connections?: boolean;
  audit_log_level?: ProjectAuditLogLevel;
  hipaa?: boolean;
  preload_libraries?: PreloadLibraries;
};
type ProjectResponse = {
  project: Project;
};
type ProjectRecoverResponse = ProjectResponse & BranchesResponse;
type ProjectsResponse = {
  projects: Array<ProjectListItem>;
  /**
   * A list of project IDs indicating which projects are known to exist, but whose details could not
   * be fetched within the requested (or implicit) time limit
   *
   */
  unavailable_project_ids?: Array<string>;
};
type ProjectsCountResponse = {
  /**
   * The total number of projects matching the query criteria.
   */
  count: number;
};
type ProjectPermission = {
  id: string;
  granted_to_email: string;
  granted_at: string;
  revoked_at?: string;
};
type ProjectPermissions = {
  project_permissions: Array<ProjectPermission>;
};
type GrantPermissionToProjectRequest = {
  email: string;
};
/**
 * The caller's effective permission for a project when
 * per-project permissions are enabled. Values correspond to viewer,
 * editor, and admin/manage project access levels. Omitted for personal
 * projects, flag-off organizations, and non-user subjects.
 *
 */
type ProjectPermissionLevel = 'CAN_VIEW' | 'CAN_EDIT' | 'CAN_MANAGE';
type ConsumptionHistoryPerProjectResponse = {
  projects: Array<ConsumptionHistoryPerProject>;
};
type ConsumptionHistoryPerProjectV2Response = {
  projects: Array<ConsumptionHistoryPerProjectV2>;
};
type ConsumptionHistoryPerProject = {
  /**
   * The project ID
   */
  project_id: string;
  periods: Array<ConsumptionHistoryPerPeriod>;
};
type ConsumptionHistoryPerProjectV2 = {
  /**
   * The project ID
   */
  project_id: string;
  periods: Array<ConsumptionHistoryPerPeriodV2>;
};
type ConsumptionHistoryPerBranchV2Response = {
  branches: Array<ConsumptionHistoryPerBranchV2>;
};
type ConsumptionHistoryPerBranchV2 = {
  /**
   * The project that owns the branch
   */
  project_id: string;
  /**
   * The branch ID
   */
  branch_id: string;
  periods: Array<ConsumptionHistoryPerPeriodV2>;
};
type ConsumptionHistoryPerPeriod = {
  /**
   * The ID assigned to the specified billing period.
   */
  period_id: string;
  /**
   * The billing plan applicable during the billing period.
   */
  period_plan: string;
  /**
   * The start date-time of the billing period.
   *
   */
  period_start: string;
  /**
   * The end date-time of the billing period, available for the past periods only.
   *
   */
  period_end?: string;
  consumption: Array<ConsumptionHistoryPerTimeframe>;
};
type ConsumptionHistoryPerPeriodV2 = {
  /**
   * The ID assigned to the specified billing period.
   */
  period_id: string;
  /**
   * The billing plan applicable during the billing period.
   */
  period_plan: string;
  /**
   * The start date-time of the billing period.
   *
   */
  period_start: string;
  /**
   * The end date-time of the billing period, available for the past periods only.
   *
   */
  period_end?: string;
  consumption: Array<ConsumptionHistoryPerTimeframeV2>;
};
type ConsumptionHistoryPerTimeframe = {
  /**
   * The specified start date-time for the reported consumption.
   *
   */
  timeframe_start: string;
  /**
   * The specified end date-time for the reported consumption.
   *
   */
  timeframe_end: string;
  /**
   * Seconds. The amount of time the compute endpoints have been active.
   *
   */
  active_time_seconds: number;
  /**
   * Seconds. The number of CPU seconds used by compute endpoints, including compute endpoints that have been deleted.
   *
   */
  compute_time_seconds: number;
  /**
   * Bytes. The amount of written data for all branches.
   *
   */
  written_data_bytes: number;
  /**
   * Bytes. The space occupied in storage. Synthetic storage size combines the logical data size and Write-Ahead Log (WAL) size for all branches.
   *
   */
  synthetic_storage_size_bytes: number;
  /**
   * Bytes-Hour. The amount of storage consumed hourly.
   *
   */
  data_storage_bytes_hour?: number;
  /**
   * Bytes. The amount of logical size consumed.
   *
   */
  logical_size_bytes?: number;
  /**
   * Bytes-Hour. The amount of logical size consumed hourly.
   *
   */
  logical_size_bytes_hour?: number;
};
type ConsumptionHistoryPerTimeframeV2 = {
  /**
   * The specified start date-time for the reported consumption.
   *
   */
  timeframe_start?: string;
  /**
   * The specified end date-time for the reported consumption.
   *
   */
  timeframe_end?: string;
  metrics?: Array<ConsumptionMetricValue>;
};
type ConsumptionMetricValue = {
  metric_name: string;
  value: number;
};
type ConsumptionHistoryGranularity = 'hourly' | 'daily' | 'monthly';
type ConsumptionHistoryQueryMetrics = Array<string>;
type ProjectLimits = {
  limits: Limits;
  features: Features;
};
type Limits = {
  active_time: number;
  max_projects: number;
  max_branches: number;
  max_snapshots: number;
  max_protected_branches: number;
  max_autoscaling_cu: number;
  max_fixed_size_cu: number;
  cpu_seconds: number;
  max_compute_time_non_primary: number;
  max_active_endpoints: number;
  max_read_only_endpoints: number;
  max_read_computes_per_branch?: number;
  max_allowed_ips: number;
  max_vpc_endpoints_per_region: number;
  max_monitoring_retention_hours: number;
  max_history_retention_seconds: number;
  min_autosuspend_seconds: number;
  max_data_transfer: number;
  min_idle_seconds_to_autoarchive: number;
  min_age_seconds_to_autoarchive: number;
  max_branch_roles: number;
  max_branch_databases: number;
  max_concurrent_scheduled_operation_chains_per_project: number;
  max_concurrent_executing_operation_chains_per_project: number;
  max_root_branches: number;
  max_import_size: number;
  max_organization_members: number;
  schema_only_branches_size_limit: number;
  per_project: {
    compute_time_seconds: number;
    written_data_bytes: number;
    data_transfer_bytes: number;
    /**
     * Soft limit on PITR history size per project, in bytes. Display-only nudge —
     * not enforced server-side. Value <= 0 means unlimited.
     *
     */
    history_size_bytes: number;
    suspend_default_branch: boolean;
  };
};
type ProjectAuditLogLevel = 'base' | 'extended' | 'full';
type AvailablePreloadLibrary = {
  library_name: string;
  description: string;
  is_default: boolean;
  is_experimental: boolean;
  version: string;
};
type AvailablePreloadLibraries = {
  libraries?: Array<AvailablePreloadLibrary>;
};
type Branch = {
  /**
   * The branch ID. This value is generated when a branch is created. A `branch_id` value has a `br` prefix. For example: `br-small-term-683261`.
   *
   */
  id: string;
  /**
   * The ID of the project to which the branch belongs
   *
   */
  project_id: string;
  /**
   * The `branch_id` of the parent branch
   *
   */
  parent_id?: string;
  /**
   * The Log Sequence Number (LSN) on the parent branch from which this branch was created.
   * When restoring a branch using the `POST /projects/{project_id}/branches/{branch_id}/restore` endpoint,
   * this value isn’t finalized until all operations related to the restore have completed successfully.
   *
   */
  parent_lsn?: string;
  /**
   * The point in time on the parent branch from which this branch was created.
   * When restoring a branch using the `POST /projects/{project_id}/branches/{branch_id}/restore` endpoint,
   * this value isn’t finalized until all operations related to the restore have completed successfully.
   * After all the operations completed, this value might stay empty.
   *
   */
  parent_timestamp?: string;
  /**
   * The branch name
   *
   */
  name: string;
  current_state: BranchState;
  pending_state?: BranchState;
  /**
   * A UTC timestamp indicating when the `current_state` began
   *
   */
  state_changed_at: string;
  /**
   * The logical size of the branch, in bytes
   *
   */
  logical_size?: number;
  /**
   * The branch creation source
   *
   */
  creation_source: string;
  /**
   * DEPRECATED. Use `default` field.
   * Whether the branch is the project's primary branch
   *
   *
   * @deprecated
   */
  primary?: boolean;
  /**
   * Whether the branch is the project's default branch
   *
   */
  default: boolean;
  /**
   * Whether the branch is protected
   *
   */
  protected: boolean;
  /**
   * CPU seconds used by all of the branch's compute endpoints, including deleted ones.
   * This value is reset at the beginning of each billing period.
   * Examples:
   * 1. A branch that uses 1 CPU for 1 second is equal to `cpu_used_sec=1`.
   * 2. A branch that uses 2 CPUs simultaneously for 1 second is equal to `cpu_used_sec=2`.
   *
   *
   * @deprecated
   */
  cpu_used_sec: number;
  compute_time_seconds: number;
  active_time_seconds: number;
  written_data_bytes: number;
  data_transfer_bytes: number;
  /**
   * A timestamp indicating when the branch was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the branch was last updated
   *
   */
  updated_at: string;
  /**
   * The time-to-live (TTL) duration originally configured for the branch, in seconds. This read-only value represents the interval between the time `expires_at` was set and the expiration timestamp itself. It is preserved to ensure the same TTL duration is reapplied when resetting the branch from its parent, and only updates when a new `expires_at` value is set.
   *
   * Access to this feature is currently limited to participants in the Early Access Program.
   *
   */
  ttl_interval_seconds?: number;
  /**
   * The timestamp when the branch is scheduled to expire and be automatically deleted. Must be set by the client following the [RFC 3339, section 5.6](https://tools.ietf.org/html/rfc3339#section-5.6) format with precision up to seconds (such as 2025-06-09T18:02:16Z). Deletion is performed by a background job and may not occur exactly at the specified time.
   *
   * Access to this feature is currently limited to participants in the Early Access Program.
   *
   */
  expires_at?: string;
  /**
   * A timestamp indicating when the branch was last reset
   *
   */
  last_reset_at?: string;
  /**
   * The resolved user model that contains details of the user/org/integration/api_key used for branch creation. This field is filled only in listing/get/create/get/update/delete methods, if it is empty when calling other handlers, it does not mean that it is empty in the system.
   *
   */
  created_by?: {
    /**
     * The name of the user.
     */
    name?: string;
    /**
     * The URL to the user's avatar image.
     */
    image?: string;
  };
  /**
   * The source of initialization for the branch. Valid values are `schema-only` and `parent-data` (default).
   * * `schema-only` - creates a new root branch containing only the schema. Use `parent_id` to specify the source branch. Optionally, you can provide `parent_lsn` or `parent_timestamp` to branch from a specific point in time or LSN. These fields define which branch to copy the schema from and at what point—they do not establish a parent-child relationship between the `parent_id` branch and the new schema-only branch.
   * * `parent-data` - creates the branch with both schema and data from the parent.
   *
   */
  init_source?: string;
  restore_status?: BranchRestoreStatus;
  /**
   * ID of the snapshot that was the restore source for this branch
   *
   */
  restored_from?: string;
  /**
   * ID of the target branch which was replaced when this branch was restored
   *
   */
  restored_as?: string;
  /**
   * A list of actions that are currently restricted for this branch and the reason why.
   *
   */
  restricted_actions?: Array<BranchRestrictedAction>;
  recovery?: BranchRecoveryInfo;
};
/**
 * The branch’s state, indicating if it is initializing, ready for use, or archived.
 * * 'init' - the branch is being created but is not available for querying.
 * * 'resetting' - the branch is being reset to a specific point in time or LSN and is not yet available for querying.
 * * 'ready' - the branch is fully operational and ready for querying. Expect normal query response times.
 * * 'archived' - the branch is stored in cost-effective archival storage. Expect slow query response times.
 *
 */
type BranchState = string;
/**
 * Could be `restored`, `finalized` or `detaching`.
 * A `restored` branch becomes permanently `finalized` when you call `finalizeRestoreBranch`
 * A `restored` or `finalized` branch may begin `detaching` as a one-time performance optimisation, after which it will continue in its original state
 *
 */
type BranchRestoreStatus = string;
/**
 * An action that is currently restricted for the branch and the reason why.
 *
 */
type BranchRestrictedAction = {
  /**
   * The name of a restricted action. Possible values include `restore`, `delete-rw-endpoint`.
   *
   */
  name: string;
  /**
   * A human-readable explanation of why the action is restricted.
   *
   */
  reason: string;
};
/**
 * Recovery information for a deleted branch. Only present when listing deleted branches
 * with `include_deleted=true`.
 *
 * This is part of the Branch Recovery feature, which is in preview and not available to all users.
 *
 */
type BranchRecoveryInfo = {
  /**
   * Timestamp when the branch was deleted
   *
   */
  deleted_at: string;
  /**
   * Timestamp when the recovery window expires and the branch will be permanently deleted
   *
   */
  recoverable_until: string;
  /**
   * How the branch was deleted: 'user' for manual deletion, 'ttl' for TTL expiration
   *
   */
  deletion_method: 'user' | 'ttl';
};
type BranchConsumption = {
  /**
   * Seconds. The number of CPU seconds used by the branch's compute endpoints, including compute endpoints that have been deleted.
   *
   */
  compute_time_seconds: number;
  /**
   * Bytes. The number of bytes transferred to and from the branch.
   *
   */
  data_transfer_bytes: number;
  /**
   * Bytes. The logical size of the branch.
   *
   */
  logical_size_bytes: number;
  /**
   * Bytes. The number of bytes written to the branch since the parent branch.
   *
   */
  written_size_since_parent_bytes: number;
  /**
   * Bytes. The number of bytes in the PITR history of the branch since the parent branch.
   *
   */
  pitr_history_size_since_parent_bytes: number;
};
type BranchCreateRequestEndpointOptions = {
  type: EndpointType;
  settings?: EndpointSettingsData;
  autoscaling_limit_min_cu?: ComputeUnit;
  autoscaling_limit_max_cu?: ComputeUnit;
  provisioner?: Provisioner;
  suspend_timeout_seconds?: SuspendTimeoutSeconds;
};
type BranchAnonymizedCreateRequest = AnnotationCreateValueRequest & {
  branch_create?: BranchCreateRequest;
  /**
   * List of masking rules to apply to the branch.
   *
   */
  masking_rules?: Array<MaskingRule>;
  /**
   * If true, automatically start anonymization after the branch is created.
   * Defaults to false.
   *
   */
  start_anonymization?: boolean;
};
type BranchCreateRequest = {
  endpoints?: Array<BranchCreateRequestEndpointOptions>;
  branch?: {
    /**
     * The `branch_id` of the parent branch. If omitted or empty, the branch will be created from the project's default branch.
     *
     */
    parent_id?: string;
    /**
     * The branch name
     *
     */
    name?: string;
    /**
     * A Log Sequence Number (LSN) on the parent branch. The branch will be created with data from this LSN.
     *
     */
    parent_lsn?: string;
    /**
     * A timestamp identifying a point in time on the parent branch. The branch will be created with data starting from this point in time.
     * The timestamp must be provided in ISO 8601 format; for example: `2024-02-26T12:00:00Z`.
     *
     */
    parent_timestamp?: string;
    /**
     * Whether the branch is protected
     *
     */
    protected?: boolean;
    /**
     * Whether to create the branch as archived
     *
     */
    archived?: boolean;
    /**
     * The source of initialization for the branch. Valid values are `schema-only` and `parent-data` (default).
     * * `schema-only` - creates a new root branch containing only the schema. Use `parent_id` to specify the source branch. Optionally, you can provide `parent_lsn` or `parent_timestamp` to branch from a specific point in time or LSN. These fields define which branch to copy the schema from and at what point—they do not establish a parent-child relationship between the `parent_id` branch and the new schema-only branch.
     * * `parent-data` - creates the branch with both schema and data from the parent.
     *
     */
    init_source?: string;
    /**
     * The timestamp when the branch is scheduled to expire and be automatically deleted. Must be set by the client following the [RFC 3339, section 5.6](https://tools.ietf.org/html/rfc3339#section-5.6) format with precision up to seconds (such as 2025-06-09T18:02:16Z). Deletion is performed by a background job and may not occur exactly at the specified time.
     *
     * Access to this feature is currently limited to participants in the Early Access Program.
     *
     */
    expires_at?: string;
  };
};
type BranchUpdateRequest = {
  branch: {
    name?: string;
    protected?: boolean;
    /**
     * The timestamp when the branch is scheduled to expire and be automatically deleted. Must be set by the client following the [RFC 3339, section 5.6](https://tools.ietf.org/html/rfc3339#section-5.6) format with precision up to seconds (such as 2025-06-09T18:02:16Z). Deletion is performed by a background job and may not occur exactly at the specified time. If this field is set to null, the expiration timestamp is removed.
     *
     * Access to this feature is currently limited to participants in the Early Access Program.
     *
     */
    expires_at?: string | null;
  };
};
type BranchRestoreRequest = {
  /**
   * The `branch_id` of the restore source branch.
   * If `source_timestamp` and `source_lsn` are omitted, the branch will be restored to head.
   * If `source_branch_id` is equal to the branch's id, `source_timestamp` or `source_lsn` is required.
   *
   */
  source_branch_id: string;
  /**
   * A Log Sequence Number (LSN) on the source branch. The branch will be restored with data from this LSN.
   *
   */
  source_lsn?: string;
  /**
   * A timestamp identifying a point in time on the source branch. The branch will be restored with data starting from this point in time.
   * The timestamp must be provided in ISO 8601 format; for example: `2024-02-26T12:00:00Z`.
   *
   */
  source_timestamp?: string;
  /**
   * If not empty, the previous state of the branch will be saved to a branch with this name.
   * If the branch has children or the `source_branch_id` is equal to the branch id, this field is required. All existing child branches will be moved to the newly created branch under the name `preserve_under_name`.
   *
   */
  preserve_under_name?: string;
};
type BranchResponse = {
  branch: Branch;
};
type BranchConsumptionResponse = {
  consumption: BranchConsumption;
};
type BranchSchemaResponse = {
  sql?: string;
  json?: BranchSchemaJson;
};
type BranchSchemaCompareResponse = {
  diff?: string;
};
type BranchesResponse = {
  branches: Array<Branch>;
};
type BranchesCountResponse = {
  count: number;
};
type ProjectsBranchesCountResponse = {
  projects: {
    [key: string]: {
      count: number;
    };
  };
};
type MaskingRule = {
  /**
   * The name of the database containing the table to be masked
   *
   */
  database_name: string;
  /**
   * The name of the schema containing the table to be masked
   *
   */
  schema_name: string;
  /**
   * The name of the table containing the column to be masked
   *
   */
  table_name: string;
  /**
   * The name of the column to be masked
   *
   */
  column_name: string;
  /**
   * The PostgreSQL Anonymizer masking function to apply.
   * Can be a predefined function (e.g., 'anon.random_string(10)', 'anon.fake_email()')
   * or a custom function definition (e.g., 'anon.hash(column_name)')
   *
   */
  masking_function?: string;
  /**
   * A literal value to set on the column when masking.
   *
   */
  masking_value?: string;
};
type MaskingRulesResponse = {
  /**
   * List of masking rules for the branch
   *
   */
  masking_rules: Array<MaskingRule>;
};
type MaskingRulesUpdateRequest = {
  /**
   * List of masking rules to apply to the branch.
   * This will replace all existing masking rules for the branch.
   *
   */
  masking_rules: Array<MaskingRule>;
};
type AnonymizedBranchStatusResponse = {
  /**
   * The ID of the project
   *
   */
  project_id: string;
  /**
   * The ID of the anonymized branch
   *
   */
  branch_id: string;
  /**
   * The current state of the anonymized branch. Possible values: created, initialized, initialization_error, anonymizing, anonymized, error
   *
   */
  state: string;
  /**
   * A descriptive message about the current status or any errors
   *
   */
  status_message?: string;
  /**
   * A timestamp indicating when the anonymized branch was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the anonymized branch status was last updated
   *
   */
  updated_at: string;
  /**
   * A timestamp indicating when the anonymized branch operation failed (if applicable)
   *
   */
  failed_at?: string;
  last_run?: AnonymizationRunMetadata;
};
/**
 * Metadata about the most recent anonymization attempt for the branch.
 */
type AnonymizationRunMetadata = {
  /**
   * Timestamp indicating when the latest anonymization attempt started.
   *
   */
  started_at?: string;
  /**
   * Timestamp indicating when the latest anonymization attempt completed.
   * Populated even if the attempt failed.
   *
   */
  completed_at?: string;
  /**
   * UUID of the user who triggered the latest anonymization attempt.
   *
   */
  triggered_by?: string;
  /**
   * Username of the user who triggered the latest anonymization attempt.
   *
   */
  triggered_by_username?: string;
  /**
   * Number of columns that had masking rules applied during the attempt.
   *
   */
  masked_columns?: number;
};
type ConnectionParameters = {
  /**
   * Database name
   *
   */
  database: string;
  /**
   * Password for the role
   *
   */
  password: string;
  /**
   * Role name
   *
   */
  role: string;
  /**
   * Hostname
   *
   */
  host: string;
  /**
   * Pooler hostname
   *
   */
  pooler_host: string;
};
type ConnectionDetails = {
  /**
   * The connection URI is defined as specified here: [Connection URIs](https://www.postgresql.org/docs/current/libpq-connect.html#LIBPQ-CONNSTRING-URIS)
   * The connection URI can be used to connect to a Postgres database with psql or defined in a DATABASE_URL environment variable.
   * When creating a branch from a parent with more than one role or database, the response body does not include a connection URI.
   *
   */
  connection_uri: string;
  connection_parameters: ConnectionParameters;
};
type ConnectionUriResponse = {
  /**
   * The connection URI.
   *
   */
  uri: string;
};
type Endpoint = {
  /**
   * The hostname of the compute endpoint. This is the hostname specified when connecting to a Neon database.
   *
   */
  host: string;
  /**
   * The compute endpoint ID. Compute endpoint IDs have an `ep-` prefix. For example: `ep-little-smoke-851426`
   *
   */
  id: string;
  /**
   * Optional name of the compute endpoint
   *
   */
  name?: string;
  /**
   * The ID of the project to which the compute endpoint belongs
   *
   */
  project_id: string;
  /**
   * The ID of the branch that the compute endpoint is associated with
   *
   */
  branch_id: string;
  autoscaling_limit_min_cu: ComputeUnit;
  autoscaling_limit_max_cu: ComputeUnit;
  /**
   * The region identifier
   *
   */
  region_id: string;
  type: EndpointType;
  current_state: EndpointState;
  pending_state?: EndpointState;
  settings: EndpointSettingsData;
  /**
   * DEPRECATED. Whether to enable connection pooling for the compute endpoint.
   * The recommended way to enable connection pooling is to append `-pooler` to the endpoint ID in the connection string.
   * See [How to use connection pooling](https://neon.com/docs/connect/connection-pooling#how-to-use-connection-pooling)
   *
   *
   * @deprecated
   */
  pooler_enabled: boolean;
  pooler_mode: EndpointPoolerMode;
  /**
   * Whether to restrict connections to the compute endpoint.
   * Enabling this option schedules a suspend compute operation.
   * A disabled compute endpoint cannot be enabled by a connection or
   * console action.
   *
   */
  disabled: boolean;
  /**
   * Whether to permit passwordless access to the compute endpoint
   *
   */
  passwordless_access: boolean;
  /**
   * A timestamp indicating when the compute endpoint was last active
   *
   */
  last_active?: string;
  /**
   * The compute endpoint creation source
   *
   */
  creation_source: string;
  /**
   * A timestamp indicating when the compute endpoint was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the compute endpoint was last updated
   *
   */
  updated_at: string;
  /**
   * A timestamp indicating when the compute endpoint was last started
   *
   */
  started_at?: string;
  /**
   * A timestamp indicating when the compute endpoint was last suspended
   *
   */
  suspended_at?: string;
  /**
   * DEPRECATED. Use the "host" property instead.
   *
   */
  proxy_host: string;
  suspend_timeout_seconds: SuspendTimeoutSeconds;
  provisioner: Provisioner;
  /**
   * Attached compute's release version number.
   *
   */
  compute_release_version?: string;
};
/**
 * The state of the compute endpoint
 *
 */
type EndpointState = 'init' | 'active' | 'idle';
/**
 * The compute endpoint type. Either `read_write` or `read_only`.
 *
 */
type EndpointType = 'read_only' | 'read_write';
/**
 * DEPRECATED. The connection pooler mode. Neon supports PgBouncer in `transaction` mode only. This schema is deprecated and will be removed after 2026-06-20.
 *
 *
 * @deprecated
 */
type EndpointPoolerMode = 'transaction';
/**
 * Duration of inactivity in seconds after which the compute endpoint is
 * automatically suspended. The value `0` means use the default value.
 * The value `-1` means never suspend. The default value is `300` seconds (5 minutes).
 * The minimum value is `60` seconds (1 minute).
 * The maximum value is `604800` seconds (1 week). For more information, see
 * [Scale to zero configuration](https://neon.com/docs/manage/endpoints#scale-to-zero-configuration).
 *
 */
type SuspendTimeoutSeconds = number;
/**
 * A list of IP addresses that are allowed to connect to the compute endpoint.
 * If the list is empty or not set, all IP addresses are allowed.
 * If protected_branches_only is true, the list will be applied only to protected branches.
 *
 */
type AllowedIps = {
  /**
   * A list of IP addresses that are allowed to connect to the endpoint.
   */
  ips?: Array<string>;
  /**
   * If true, the list will be applied only to protected branches.
   */
  protected_branches_only?: boolean;
};
/**
 * A maintenance window is a time period during which Neon may perform maintenance on the project's infrastructure.
 * During this time, the project's compute endpoints may be unavailable and existing connections can be
 * interrupted.
 *
 */
type MaintenanceWindow = {
  /**
   * A list of weekdays when the maintenance window is active.
   * Encoded as ints, where 1 - Monday, and 7 - Sunday.
   *
   */
  weekdays: Array<number>;
  /**
   * Start time of the maintenance window, in the format of "HH:MM". Uses UTC.
   *
   */
  start_time: string;
  /**
   * End time of the maintenance window, in the format of "HH:MM". Uses UTC.
   *
   */
  end_time: string;
};
/**
 * The shared libraries to preload into the project's compute instances.
 *
 */
type PreloadLibraries = {
  use_defaults?: boolean;
  enabled_libraries?: Array<string>;
};
type EndpointCreateRequest = {
  endpoint: {
    /**
     * The ID of the branch the compute endpoint will be associated with
     *
     */
    branch_id: string;
    /**
     * The region where the compute endpoint will be created. Only the project's `region_id` is permitted.
     *
     */
    region_id?: string;
    type: EndpointType;
    settings?: EndpointSettingsData;
    autoscaling_limit_min_cu?: ComputeUnit;
    autoscaling_limit_max_cu?: ComputeUnit;
    provisioner?: Provisioner;
    /**
     * DEPRECATED. Whether to enable connection pooling for the compute endpoint.
     * The recommended way to enable connection pooling is to append `-pooler` to the endpoint ID in the connection string.
     * See [How to use connection pooling](https://neon.com/docs/connect/connection-pooling#how-to-use-connection-pooling)
     *
     *
     * @deprecated
     */
    pooler_enabled?: boolean;
    pooler_mode?: EndpointPoolerMode;
    /**
     * Whether to restrict connections to the compute endpoint.
     * Enabling this option schedules a suspend compute operation.
     * A disabled compute endpoint cannot be enabled by a connection or
     * console action. However, the compute endpoint is periodically
     * enabled by check_availability operations.
     *
     */
    disabled?: boolean;
    /**
     * NOT YET IMPLEMENTED. Whether to permit passwordless access to the compute endpoint.
     *
     */
    passwordless_access?: boolean;
    suspend_timeout_seconds?: SuspendTimeoutSeconds;
    /**
     * Optional name of the compute endpoint
     *
     */
    name?: string;
  };
};
type EndpointUpdateRequest = {
  endpoint: {
    /**
     * DEPRECATED: This field will be removed in a future release.
     * The destination branch ID. The destination branch must not have an existing read-write endpoint.
     *
     *
     * @deprecated
     */
    branch_id?: string;
    autoscaling_limit_min_cu?: ComputeUnit;
    autoscaling_limit_max_cu?: ComputeUnit;
    provisioner?: Provisioner;
    settings?: EndpointSettingsData;
    /**
     * DEPRECATED. Whether to enable connection pooling for the compute endpoint.
     * The recommended way to enable connection pooling is to append `-pooler` to the endpoint ID in the connection string.
     * See [How to use connection pooling](https://neon.com/docs/connect/connection-pooling#how-to-use-connection-pooling)
     *
     *
     * @deprecated
     */
    pooler_enabled?: boolean;
    pooler_mode?: EndpointPoolerMode;
    /**
     * Whether to restrict connections to the compute endpoint.
     * Enabling this option schedules a suspend compute operation.
     * A disabled compute endpoint cannot be enabled by a connection or
     * console action. However, the compute endpoint is periodically
     * enabled by check_availability operations.
     *
     */
    disabled?: boolean;
    /**
     * NOT YET IMPLEMENTED. Whether to permit passwordless access to the compute endpoint.
     *
     */
    passwordless_access?: boolean;
    suspend_timeout_seconds?: SuspendTimeoutSeconds;
    /**
     * Optional name of the compute endpoint
     *
     */
    name?: string;
  };
};
type EndpointResponse = {
  endpoint: Endpoint;
};
type ConnectionUrisResponse = {
  connection_uris: Array<ConnectionDetails>;
};
type ConnectionUrisOptionalResponse = {
  connection_uris?: Array<ConnectionDetails>;
};
type VpcEndpointsResponse = {
  endpoints: Array<VpcEndpoint>;
};
type VpcEndpointsWithRegionResponse = {
  endpoints: Array<VpcEndpointWithRegion>;
};
type VpcEndpoint = {
  /**
   * The VPC endpoint ID
   */
  vpc_endpoint_id: string;
  /**
   * A descriptive label for the VPC endpoint
   */
  label: string;
};
type VpcEndpointWithRegion = VpcEndpoint & {
  /**
   * The region where the VPC endpoint is located
   */
  region_id: string;
};
type VpcEndpointDetails = {
  /**
   * The VPC endpoint ID
   */
  vpc_endpoint_id: string;
  /**
   * A descriptive label for the VPC endpoint
   */
  label: string;
  /**
   * The current state of the VPC endpoint. Possible values are
   * `new` (just configured, pending acceptance) or `accepted`
   * (VPC connection was accepted by Neon).
   *
   */
  state: string;
  /**
   * The number of projects that are restricted to use this VPC endpoint.
   *
   */
  num_restricted_projects: number;
  /**
   * A list of example projects that are restricted to use this VPC endpoint.
   * There are at most 3 projects in the list, even if more projects are restricted.
   *
   */
  example_restricted_projects: Array<string>;
};
type VpcEndpointAssignment = {
  label: string;
};
type EndpointsResponse = {
  endpoints: Array<Endpoint>;
};
type EndpointsOptionalResponse = {
  endpoints?: Array<Endpoint>;
};
type EndpointPasswordlessSessionAuthRequest = {
  session_id: string;
};
/**
 * A Duration represents the elapsed time between two instants
 * as an int64 nanosecond count. The representation limits the
 * largest representable duration to approximately 290 years.
 */
type Duration = number;
type StatementResult = {
  data?: StatementData;
  error?: string;
  explain_data?: Array<ExplainData>;
  query: string;
};
type StatementData = {
  fields?: Array<string>;
  rows?: Array<Array<string>>;
  truncated: boolean;
};
type ExplainData = {
  'QUERY PLAN': string;
};
type Role = {
  /**
   * The ID of the branch to which the role belongs
   *
   */
  branch_id: string;
  /**
   * The role name
   *
   */
  name: string;
  /**
   * The role password
   *
   */
  password?: string;
  /**
   * Whether or not the role is system-protected
   *
   */
  protected?: boolean;
  /**
   * Authentication method configured for this role. Valid options: `password`, `oauth`, `no_login`
   *
   */
  authentication_method?: string;
  /**
   * A timestamp indicating when the role was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the role was last updated
   *
   */
  updated_at: string;
};
type RoleCreateRequest = {
  role: {
    /**
     * The role name. Cannot exceed 63 bytes in length.
     *
     */
    name: string;
    /**
     * Whether to create a role that cannot login.
     *
     */
    no_login?: boolean;
  };
};
type RoleResponse = {
  role: Role;
};
type JwksResponse = {
  jwks: Jwks;
};
type RolesResponse = {
  roles: Array<Role>;
};
type RolePasswordResponse = {
  /**
   * The role password
   *
   */
  password: string;
};
type PaymentSourceBankCard = {
  /**
   * Last 4 digits of the card.
   *
   */
  last4: string;
  /**
   * Brand of credit card.
   *
   */
  brand?: 'amex' | 'diners' | 'discover' | 'jcb' | 'mastercard' | 'unionpay' | 'unknown' | 'visa';
  /**
   * Credit card expiration month
   *
   */
  exp_month?: number;
  /**
   * Credit card expiration year
   *
   */
  exp_year?: number;
};
type PaymentSource = {
  /**
   * Type of payment source. E.g. "card".
   *
   */
  type: string;
  card?: PaymentSourceBankCard;
};
type BillingAccount = {
  state: BillingAccountState;
  payment_source: PaymentSource;
  subscription_type: BillingSubscriptionType;
  payment_method: BillingPaymentMethod;
  /**
   * The last time the quota was reset. Defaults to the date-time the account is created.
   *
   */
  quota_reset_at_last: string;
  /**
   * The full name of the individual or entity that owns the billing account. This name appears on invoices.
   */
  name: string;
  /**
   * Billing email, to receive emails related to invoices and subscriptions.
   *
   */
  email: string;
  /**
   * Billing address city.
   *
   */
  address_city: string;
  /**
   * Billing address country code defined by ISO 3166-1 alpha-2.
   *
   */
  address_country: string;
  /**
   * Billing address country name.
   *
   */
  address_country_name?: string;
  /**
   * Billing address line 1.
   *
   */
  address_line1: string;
  /**
   * Billing address line 2.
   *
   */
  address_line2: string;
  /**
   * Billing address postal code.
   *
   */
  address_postal_code: string;
  /**
   * Billing address state or region.
   *
   */
  address_state: string;
  /**
   * Orb user portal url
   *
   */
  orb_portal_url?: string;
  /**
   * The tax identification number for the billing account, displayed on invoices.
   *
   */
  tax_id?: string;
  /**
   * The type of the tax identification number based on the country.
   *
   */
  tax_id_type?: string;
  plan_details?: PlanDetails;
  /**
   * Monthly spending cap in cents for V3 paid plans. When set,
   * notifications are sent at 80% and 100% of this limit. `null`
   * means no limit is configured.
   *
   */
  spending_limit_cents?: number | null;
};
type SpendingLimitUpdateRequest = {
  /**
   * Monthly spending cap in cents. Must be positive. To remove a
   * previously configured limit, send a DELETE request to the
   * spending_limit endpoint — `0` and `null` are rejected here.
   * The cap is alert-only: notifications fire at 80% and 100%, but
   * computes are not suspended. Setting a cap below the period's
   * already-accrued spend is permitted and will trigger the
   * over-limit notification on the next worker run.
   *
   */
  spending_limit_cents: number;
};
type SpendingLimitResponse = {
  /**
   * Monthly spending cap in cents. `null` indicates that no limit
   * is currently configured.
   *
   */
  spending_limit_cents: number | null;
};
/**
 * State of the billing account.
 *
 */
type BillingAccountState = 'UNKNOWN' | 'active' | 'suspended' | 'deactivated' | 'deleted';
/**
 * Type of subscription to Neon Cloud.
 * Notice that for users without billing account this will be "UNKNOWN"
 *
 */
type BillingSubscriptionType = 'UNKNOWN' | 'direct_sales' | 'direct_sales_v3' | 'aws_marketplace' | 'free_v2' | 'free_v3' | 'launch' | 'launch_v3' | 'scale' | 'scale_v3' | 'business' | 'vercel_pg_legacy';
/**
 * Indicates whether and how an account makes payments.
 *
 */
type BillingPaymentMethod = 'UNKNOWN' | 'none' | 'stripe' | 'direct_payment' | 'aws_mp' | 'azure_mp' | 'vercel_mp' | 'staff' | 'trial' | 'sponsorship' | 'shared_payment_token';
type Database = {
  /**
   * The database ID
   *
   */
  id: number;
  /**
   * The ID of the branch to which the database belongs
   *
   */
  branch_id: string;
  /**
   * The database name
   *
   */
  name: string;
  /**
   * The name of role that owns the database
   *
   */
  owner_name: string;
  /**
   * A timestamp indicating when the database was created
   *
   */
  created_at: string;
  /**
   * A timestamp indicating when the database was last updated
   *
   */
  updated_at: string;
};
type DatabaseCreateRequest = {
  database: {
    /**
     * The name of the database
     *
     */
    name: string;
    /**
     * The name of the role that owns the database
     *
     */
    owner_name: string;
  };
};
type DatabaseUpdateRequest = {
  database: {
    /**
     * The name of the database
     *
     */
    name?: string;
    /**
     * The name of the role that owns the database
     *
     */
    owner_name?: string;
  };
};
type DatabaseResponse = {
  database: Database;
};
type DatabasesResponse = {
  databases: Array<Database>;
};
type Invitation = {
  id: string;
  /**
   * Email of the invited user
   */
  email: string;
  /**
   * Organization id as it is stored in Neon
   */
  org_id: string;
  /**
   * UUID for the user_id who extended the invitation
   */
  invited_by: string;
  /**
   * Timestamp when the invitation was created
   */
  invited_at: string;
  role: MemberRole;
};
/**
 * The role of the organization member. Some role values may not be
 * available for all organizations.
 *
 */
type MemberRole = 'admin' | 'member' | 'editor' | 'viewer' | 'collaborator';
type Member = {
  id: string;
  user_id: string;
  org_id: string;
  role: MemberRole;
  joined_at?: string;
};
type MemberUserInfo = {
  email: string;
  /**
   * Whether the member has MFA (TOTP) enabled
   */
  has_mfa?: boolean;
  /**
   * Timestamp of when the user account was deactivated.
   * Absent for active users. When present, the UI should render a
   * "Deactivated" badge inline next to the member.
   *
   */
  deactivated_at?: string;
};
type MemberWithUser = {
  member: Member;
  user: MemberUserInfo;
};
type Organization = {
  id: string;
  name: string;
  handle: string;
  plan: string;
  /**
   * A timestamp indicting when the organization was created
   *
   */
  created_at: string;
  /**
   * Organizations created via the Console or the API are managed by `console`.
   * Organizations created by other methods can't be deleted via the Console or the API.
   *
   */
  managed_by: string;
  /**
   * A timestamp indicating when the organization was updated
   *
   */
  updated_at: string;
  /**
   * If true, allow account to mark projects as HIPAA
   */
  allow_hipaa_projects?: boolean;
  /**
   * If true, all members must have MFA enabled to access this organization
   */
  require_mfa?: boolean;
};
type EnableOrganizationFeatureRequest = {
  /**
   * The feature to enable for the organization
   */
  feature: 'allow_hipaa_projects';
};
type OrganizationsResponse = {
  organizations: Array<Organization>;
};
type OrganizationMembership = {
  organization: Organization;
  role: MemberRole;
};
type OrganizationMembershipsResponse = {
  memberships: Array<OrganizationMembership>;
};
type OrganizationsUpdateRequest = {
  name?: string;
  /**
   * If true, all members must have MFA enabled to access this organization
   */
  require_mfa?: boolean;
};
type OrganizationInvitationsResponse = {
  invitations: Array<Invitation>;
};
type OrganizationInviteCreateRequest = {
  email: string;
  role: MemberRole;
};
type OrganizationInvitesCreateRequest = {
  invitations: Array<OrganizationInviteCreateRequest>;
};
type OrganizationInviteUpdateRequest = {
  email?: string;
  role?: MemberRole;
  resend?: boolean;
};
/**
 * A list of details for guests of an organization
 *
 */
type OrganizationGuestsResponse = Array<OrganizationGuest>;
/**
 * Details of an organization guest, who is not directly a member of
 * an organization but has been shared one of the projects it owns
 *
 */
type OrganizationGuest = {
  permission_id: string;
  user_email: string;
  project_id: string;
  project_name: string;
};
type OrganizationMemberUpdateRequest = {
  role: MemberRole;
};
type OrganizationMembersResponse = {
  members: Array<MemberWithUser>;
};
type InvitationCreateRequest = {
  /**
   * Email to invite
   */
  email: string;
  role: MemberRole;
};
type OrganizationCreateRequest = {
  organization: {
    /**
     * The organization name
     */
    name?: string;
    /**
     * Emails with roles to invite to the organization
     */
    invitations?: Array<InvitationCreateRequest>;
  };
  subscription_type: BillingSubscriptionType;
  /**
   * Whether to transfer credits from the user account to the newly created organization account.
   *
   */
  transfer_credits?: boolean;
};
type ActiveRegionsResponse = {
  /**
   * The list of active regions
   */
  regions: Array<RegionResponse>;
};
type RegionResponse = {
  /**
   * The region ID as used in other API endpoints
   */
  region_id: string;
  /**
   * A short description of the region.
   */
  name: string;
  /**
   * Whether this region is used by default in new projects.
   */
  default: boolean;
  /**
   * The geographical latitude (approximate) for the region. Empty if unknown.
   */
  geo_lat: string;
  /**
   * The geographical longitude (approximate) for the region. Empty if unknown.
   */
  geo_long: string;
};
type CurrentUserAuthAccount = {
  email: string;
  image: string;
  /**
   * DEPRECATED. Use `email` field.
   *
   *
   * @deprecated
   */
  login: string;
  name: string;
  provider: IdentityProviderId;
};
type LinkedAuthAccount = {
  provider: IdentityProviderId;
  provider_display_name: string;
  username: string;
};
type UpdateUserInfoRequest = {
  email?: string;
  id: string;
  /**
   * DEPRECATED. This field is ignored.
   *
   *
   * @deprecated
   */
  image?: string;
  first_name?: string;
  last_name?: string;
  password?: string;
  new_password?: string;
};
type CurrentUserInfoResponse = {
  /**
   * Control plane observes active endpoints of a user this amount of wall-clock time.
   *
   */
  active_seconds_limit: number;
  billing_account?: BillingAccount;
  auth_accounts: Array<CurrentUserAuthAccount>;
  email: string;
  id: string;
  image: string;
  /**
   * DEPRECATED. Use `email` field.
   *
   *
   * @deprecated
   */
  login: string;
  name: string;
  last_name: string;
  projects_limit: number;
  branches_limit: number;
  /**
   * The maximum autoscaling limit in Compute Units.
   * A value of 0 indicates no limit is configured.
   *
   */
  max_autoscaling_limit: number;
  compute_seconds_limit?: number;
  plan: string;
};
type ConvertUserToOrgRequest = {
  name: string;
};
type CurrentUserInfoAuthResponse = {
  password_stored: boolean;
  auth_accounts: Array<CurrentUserAuthAccount>;
  linked_accounts: Array<LinkedAuthAccount>;
  provider: string;
};
type AuthDetailsResponse = {
  account_id: string;
  auth_method: 'keycloak' | 'session_cookie' | 'api_key_user' | 'api_key_org' | 'oauth';
  auth_data?: string;
};
type TransferProjectsToOrganizationRequest = {
  /**
   * The destination organization identifier
   */
  destination_org_id: string;
  /**
   * The list of projects ids to transfer. Maximum of 400 project ids
   */
  project_ids: Array<string>;
};
type VerifyUserPasswordRequest = {
  password: string;
};
/**
 * Identity provider id from keycloak
 */
type IdentityProviderId = 'github' | 'google' | 'hasura' | 'microsoft' | 'microsoftv2' | 'vercelmp' | 'keycloak';
/**
 * A collection of settings for a compute endpoint
 */
type EndpointSettingsData = {
  pg_settings?: PgSettingsData;
  pgbouncer_settings?: PgbouncerSettingsData;
  preload_libraries?: PreloadLibraries;
};
/**
 * Per-project consumption quotas. If a quota is exceeded, all active computes
 * are automatically suspended and cannot be started via API calls or incoming connections.
 *
 * The exception is `logical_size_bytes`, which is enforced per branch.
 * If a branch exceeds its `logical_size_bytes` quota, computes can still be started,
 * but write operations will fail—allowing data to be deleted to free up space.
 * Computes on other branches are not affected.
 *
 * Setting `logical_size_bytes` overrides any lower value set by the `neon.max_cluster_size` Postgres setting.
 *
 * Quotas are enforced using per-project consumption metrics with the same names.
 * These metrics reset at the start of each billing period. `logical_size_bytes`
 * is also an exception—it reflects the total data stored in a branch and does not reset.
 *
 * A zero or empty quota value means “unlimited.”
 *
 */
type ProjectQuota = {
  /**
   * The total amount of wall-clock time allowed to be spent by the project's compute endpoints.
   *
   */
  active_time_seconds?: number;
  /**
   * The total amount of CPU seconds allowed to be spent by the project's compute endpoints.
   *
   */
  compute_time_seconds?: number;
  /**
   * Total amount of data written to all of a project's branches.
   *
   */
  written_data_bytes?: number;
  /**
   * Total amount of data transferred from all of a project's branches using the proxy.
   *
   */
  data_transfer_bytes?: number;
  /**
   * Limit on the logical size of every project's branch.
   *
   * If a branch exceeds its `logical_size_bytes` quota, computes can still be started,
   * but write operations will fail—allowing data to be deleted to free up space.
   * Computes on other branches are not affected.
   *
   * Setting `logical_size_bytes` overrides any lower value set by the `neon.max_cluster_size` Postgres setting.
   *
   */
  logical_size_bytes?: number;
};
/**
 * A collection of settings for a Neon endpoint
 */
type DefaultEndpointSettings = {
  pg_settings?: PgSettingsData;
  pgbouncer_settings?: PgbouncerSettingsData;
  autoscaling_limit_min_cu?: ComputeUnit;
  autoscaling_limit_max_cu?: ComputeUnit;
  suspend_timeout_seconds?: SuspendTimeoutSeconds;
};
/**
 * A raw representation of Postgres settings
 */
type PgSettingsData = {
  [key: string]: string;
};
/**
 * DEPRECATED. A raw representation of PgBouncer settings. This schema is deprecated and will be removed after 2026-06-20.
 *
 *
 * @deprecated
 */
type PgbouncerSettingsData = {
  [key: string]: string;
};
/**
 * The major Postgres version number. Currently supported versions are `14`, `15`, `16`, `17`, and `18`.
 */
type PgVersion = number;
type HealthCheck = {
  /**
   * Service status
   */
  status: string;
};
type ProjectOwnerData = {
  email: string;
  name: string;
  branches_limit: number;
  subscription_type: BillingSubscriptionType;
};
type LimitsUnsatisfiedResponse = {
  limits: Array<{
    /**
     * Identifier of the unsatisfied limit. Possible values are:
     * - subscription_type
     * - projects_count
     * - project_region
     *
     */
    name: string;
    expected: string;
    actual: string;
  }>;
};
type ProjectsWithIntegrationResponse = {
  projects: Array<{
    id: string;
    integration: string;
  }>;
};
type UserDeletionConditionName = 'project_count' | 'org_admin_membership_count' | 'subscription_type';
type OrgDeletionConditionName = 'project_count';
/**
 * Configuration settings for the Neon Data API
 */
type DataApiSettings = {
  /**
   * Enable aggregates feature
   */
  db_aggregates_enabled?: boolean;
  /**
   * Database role to use for anonymous requests
   */
  db_anon_role?: string;
  /**
   * Extra schemas to add to the search path
   */
  db_extra_search_path?: string;
  /**
   * Maximum number of rows that can be returned in a single request
   */
  db_max_rows?: number;
  /**
   * List of schemas to expose via the API. Default: ["public"]
   */
  db_schemas?: Array<string>;
  /**
   * JWT claim key to use for role extraction
   */
  jwt_role_claim_key?: string;
  /**
   * Maximum lifetime for JWT cache in seconds
   */
  jwt_cache_max_lifetime?: number;
  /**
   * OpenAPI specification mode (ignore-privileges, disabled)
   */
  openapi_mode?: string;
  /**
   * CORS allowed origins
   */
  server_cors_allowed_origins?: string;
  /**
   * Enable server timing headers
   */
  server_timing_enabled?: boolean;
};
/**
 * Create Neon Data API
 */
type DataApiCreateRequest = {
  /**
   * The authentication provider to use for the Neon Data API
   */
  auth_provider?: 'neon_auth' | 'external';
  /**
   * The URL that lists the JWKS
   */
  jwks_url?: string;
  /**
   * The name of the authentication provider (e.g., Clerk, Stytch, Auth0)
   */
  provider_name?: string;
  /**
   * WARNING - using this setting will only reject tokens with a
   * different audience claim. Tokens without audience claim will still
   * be accepted.
   *
   */
  jwt_audience?: string;
  /**
   * Grant all permissions to the tables in the public schema to authenticated users
   */
  add_default_grants?: boolean;
  /**
   * Skip creating the auth schema and RLS functions
   */
  skip_auth_schema?: boolean;
  settings?: DataApiSettings;
};
/**
 * Neon Data API created successfully
 */
type DataApiCreateResponse = {
  url: string;
};
/**
 * Neon Data API response
 */
type DataApiReponse = {
  /**
   * The URL of the Neon Data API
   */
  url: string;
  /**
   * The status of the Neon Data API deployment
   */
  status: string;
  /**
   * Configuration settings for the Data API (SubZero only)
   */
  settings?: DataApiSettings | null;
  /**
   * List of available database schemas (SubZero only)
   */
  available_schemas?: Array<string> | null;
};
/**
 * Update Neon Data API
 */
type DataApiUpdateRequest = {
  settings?: DataApiSettings;
};
type NeonAuthSupportedAuthProvider = 'mock' | 'stack' | 'better_auth';
type NeonAuthProviderProjectOwnedBy = 'user' | 'neon';
type NeonAuthProviderProjectTransferStatus = 'initiated' | 'finished';
type NeonAuthRedirectUriWhitelistDomain = {
  domain: string;
  auth_provider: NeonAuthSupportedAuthProvider;
};
type NeonAuthRedirectUriWhitelistResponse = {
  domains: Array<NeonAuthRedirectUriWhitelistDomain>;
};
type NeonAuthAddDomainToRedirectUriWhitelistRequest = {
  domain: string;
  auth_provider: NeonAuthSupportedAuthProvider;
};
type NeonAuthDeleteDomainFromRedirectUriWhitelistRequest = {
  auth_provider: NeonAuthSupportedAuthProvider;
  domains: Array<NeonAuthDeleteDomainFromRedirectUriWhitelistItem>;
};
type NeonAuthDeleteDomainFromRedirectUriWhitelistItem = {
  domain: string;
};
type NeonAuthCreateIntegrationRequest = {
  auth_provider: NeonAuthSupportedAuthProvider;
  project_id: string;
  branch_id: string;
  database_name?: string;
  /**
   * @deprecated
   */
  role_name?: string;
};
type EnableNeonAuthIntegrationRequest = {
  auth_provider: NeonAuthSupportedAuthProvider;
  database_name?: string;
};
type NeonAuthCreateIntegrationResponse = {
  auth_provider: NeonAuthSupportedAuthProvider;
  auth_provider_project_id: string;
  pub_client_key: string;
  secret_server_key: string;
  jwks_url: string;
  schema_name: string;
  table_name: string;
  base_url?: string;
};
type NeonAuthCreateAuthProviderSdkKeysRequest = {
  project_id: string;
  auth_provider: NeonAuthSupportedAuthProvider;
};
type NeonAuthCreateNewUserRequest = {
  project_id: string;
  auth_provider: NeonAuthSupportedAuthProvider;
  email: string;
  name?: string;
};
type CreateBranchNeonAuthNewUserRequest = {
  email: string;
  name?: string;
};
type NeonAuthCreateNewUserResponse = {
  /**
   * ID of newly created user
   */
  id: string;
};
type UpdateNeonAuthUserRoleRequest = {
  /**
   * Array of roles to assign to the user
   */
  roles: Array<string>;
};
type UpdateNeonAuthUserRoleResponse = {
  /**
   * ID of the updated user
   */
  id: string;
};
type NeonAuthAllowLocalhostResponse = {
  /**
   * Whether to allow localhost connections
   */
  allow_localhost: boolean;
};
type UpdateNeonAuthAllowLocalhostRequest = {
  /**
   * Whether to allow localhost connections
   */
  allow_localhost: boolean;
};
type NeonAuthOrganizationConfig = {
  /**
   * Whether the organization plugin is enabled
   */
  enabled: boolean;
  /**
   * Maximum number of organizations a user can create
   */
  organization_limit: number;
  /**
   * Maximum number of members per organization
   */
  membership_limit: number;
  /**
   * The role assigned to the user who creates an organization
   */
  creator_role: 'admin' | 'owner';
  /**
   * Whether to send invitation emails when inviting members to an organization
   */
  send_invitation_email: boolean;
};
type NeonAuthOrganizationConfigUpdate = {
  /**
   * Whether the organization plugin is enabled
   */
  enabled?: boolean;
  /**
   * Maximum number of organizations a user can create
   */
  organization_limit?: number;
  /**
   * Maximum number of members per organization
   */
  membership_limit?: number;
  /**
   * The role assigned to the user who creates an organization
   */
  creator_role?: 'admin' | 'owner';
  /**
   * Whether to send invitation emails when inviting members to an organization
   */
  send_invitation_email?: boolean;
};
type NeonAuthMagicLinkConfig = {
  /**
   * Whether the magic link plugin is enabled
   */
  enabled: boolean;
  /**
   * Time in minutes before the magic link expires
   */
  expires_in: number;
  /**
   * Whether to disable sign-up via magic link
   */
  disable_sign_up: boolean;
};
type NeonAuthMagicLinkConfigUpdate = {
  /**
   * Whether the magic link plugin is enabled
   */
  enabled?: boolean;
  /**
   * Time in minutes before the magic link expires
   */
  expires_in?: number;
  /**
   * Whether to disable sign-up via magic link
   */
  disable_sign_up?: boolean;
};
type NeonAuthPhoneNumberConfig = {
  /**
   * Whether the phone number plugin is enabled
   */
  enabled: boolean;
  /**
   * Time in seconds before the OTP expires
   */
  otp_expires_in?: number;
};
type NeonAuthPhoneNumberConfigUpdate = {
  /**
   * Whether the phone number plugin is enabled
   */
  enabled?: boolean;
  /**
   * Time in seconds before the OTP expires
   */
  otp_expires_in?: number;
};
type NeonAuthTransferAuthProviderProjectRequest = {
  project_id: string;
  auth_provider: NeonAuthSupportedAuthProvider;
};
type NeonAuthTransferAuthProviderProjectResponse = {
  /**
   * URL for completing the process of ownership transfer
   */
  url: string;
};
type ListNeonAuthIntegrationsResponse = {
  data: Array<NeonAuthIntegration>;
};
type ListNeonAuthOauthProvidersResponse = {
  providers: Array<NeonAuthOauthProvider>;
};
/**
 * Aggregated plugin configurations for Neon Auth
 */
type NeonAuthPluginConfigs = {
  organization?: NeonAuthOrganizationConfig;
  magic_link?: NeonAuthMagicLinkConfig;
  phone_number?: NeonAuthPhoneNumberConfig;
  email_provider?: NeonAuthEmailServerConfig;
  email_and_password?: NeonAuthEmailAndPasswordConfig;
  oauth_providers?: Array<NeonAuthOauthProvider>;
  allow_localhost?: boolean;
};
type NeonAuthWebhookConfig = {
  enabled: boolean;
  webhook_url?: string;
  enabled_events?: Array<'user.before_create' | 'user.created' | 'send.otp' | 'send.magic_link' | 'organization.invitation.created' | 'organization.invitation.accepted' | 'phone_number.verified'>;
  timeout_seconds?: number;
};
type NeonAuthOauthProvider = {
  id: NeonAuthOauthProviderId;
  type: NeonAuthOauthProviderType;
  client_id?: string;
  client_secret?: string;
};
type NeonAuthOauthProviderId = 'google' | 'github' | 'microsoft' | 'vercel';
type NeonAuthOauthProviderType = 'standard' | 'shared';
type NeonAuthAddOAuthProviderRequest = {
  id: NeonAuthOauthProviderId;
  client_id?: string;
  client_secret?: string;
  microsoft_tenant_id?: string;
};
type NeonAuthUpdateOAuthProviderRequest = {
  client_id?: string;
  client_secret?: string;
  microsoft_tenant_id?: string;
};
type SharedEmailServer = {
  sender_email?: string;
  sender_name?: string;
};
type StandardEmailServer = {
  host: string;
  port: number;
  username: string;
  password: string;
  sender_email: string;
  sender_name: string;
};
type NeonAuthEmailServerConfig = ({
  type: 'standard';
} & StandardEmailServer) | ({
  type: 'shared';
} & SharedEmailServer);
type SendNeonAuthTestEmailRequest = StandardEmailServer & {
  /**
   * The email address to send the test email to.
   */
  recipient_email: string;
};
type SendNeonAuthTestEmailResponse = {
  /**
   * Whether the test email was sent successfully.
   */
  success: boolean;
  /**
   * The error message from the email server.
   */
  error_message?: string;
};
/**
 * The email verification method to use.
 * - `link`: Sends a verification link via email
 * - `otp`: Sends a one-time password (OTP) via email
 *
 */
type NeonAuthEmailVerificationMethod = 'link' | 'otp';
type NeonAuthEmailAndPasswordConfig = {
  /**
   * Whether email and password authentication is enabled
   */
  enabled: boolean;
  email_verification_method: NeonAuthEmailVerificationMethod;
  /**
   * Whether email verification is required before users can sign in
   */
  require_email_verification: boolean;
  /**
   * Whether users are automatically signed in after verifying their email
   */
  auto_sign_in_after_verification: boolean;
  /**
   * Whether to send a verification email when users sign up
   */
  send_verification_email_on_sign_up: boolean;
  /**
   * Whether to send a verification email when users sign in
   */
  send_verification_email_on_sign_in: boolean;
  /**
   * Whether to disable new user sign ups
   */
  disable_sign_up: boolean;
};
type NeonAuthEmailAndPasswordConfigUpdate = {
  /**
   * Whether email and password authentication is enabled
   */
  enabled?: boolean;
  email_verification_method?: NeonAuthEmailVerificationMethod;
  /**
   * Whether email verification is required before users can sign in
   */
  require_email_verification?: boolean;
  /**
   * Whether users are automatically signed in after verifying their email
   */
  auto_sign_in_after_verification?: boolean;
  /**
   * Whether to send a verification email when users sign up
   */
  send_verification_email_on_sign_up?: boolean;
  /**
   * Whether to send a verification email when users sign in
   */
  send_verification_email_on_sign_in?: boolean;
  /**
   * Whether to disable new user sign ups
   */
  disable_sign_up?: boolean;
};
type NeonAuthIntegration = {
  auth_provider: NeonAuthSupportedAuthProvider;
  auth_provider_project_id: string;
  branch_id: string;
  db_name: string;
  created_at: string;
  owned_by: NeonAuthProviderProjectOwnedBy;
  transfer_status?: NeonAuthProviderProjectTransferStatus;
  jwks_url: string;
  base_url?: string;
  /**
   * The application name used in auth emails and communications. Defaults to the Neon project name.
   */
  name?: string;
};
type NeonAuthConfigUpdate = {
  /**
   * The application name used in auth emails and communications.
   */
  name: string;
};
type NeonAuthConfigResponse = {
  /**
   * The application name used in auth emails and communications.
   */
  name: string;
};
type GeneralError = {
  /**
   * Unique identifier for the request, useful for debugging.
   * You can set this value manually by including an `X-Request-ID` header in the request. If not provided, the value will be generated automatically.
   *
   */
  request_id?: string;
  code: ErrorCode;
  /**
   * Error message
   */
  message: string;
};
type ErrorCode = string;
type BranchOperations = BranchResponse & OperationsResponse;
type BranchRecoverResponse = BranchResponse & EndpointsOptionalResponse;
type EndpointOperations = EndpointResponse & OperationsResponse;
type DatabaseOperations = DatabaseResponse & OperationsResponse;
type RoleOperations = RoleResponse & OperationsResponse;
type JwksCreationOperation = JwksResponse & OperationsResponse;
type SupportTicketSeverity = 'low' | 'normal' | 'high' | 'critical' | 'urgent';
type AnnotationData = {
  object: AnnotationObjectData;
  value: AnnotationValueData;
  created_at?: string;
  updated_at?: string;
};
/**
 * Annotation properties.
 */
type AnnotationValueData = {
  [key: string]: string;
};
type AnnotationObjectData = {
  type: string;
  id: string;
};
type AnnotationCreateValueRequest = {
  annotation_value?: AnnotationValueData;
};
type AnnotationResponse = {
  annotation: AnnotationData;
};
type AnnotationsMapResponse = {
  annotations: {
    [key: string]: AnnotationData;
  };
};
/**
 * Type of application integration
 */
type ApplicationType = 'vercel' | 'github' | 'datadog' | 'opentelemetry';
/**
 * A map where key is a project ID and a value is a list of installed applications.
 *
 */
type ProjectsApplicationsMapResponse = {
  applications: {
    [key: string]: Array<ApplicationType>;
  };
};
/**
 * A map where key is a project ID and a value is a list of installed integrations.
 *
 */
type ProjectsIntegrationsMapResponse = {
  integrations: {
    [key: string]: Array<ApplicationType>;
  };
};
type CursorPaginationResponse = {
  pagination?: CursorPagination;
};
/**
 * To paginate the response, issue an initial request with `limit` value. Then, add the value returned in the response `.pagination.next` attribute into the request under the `cursor` query parameter to the subsequent request to retrieve next page in pagination. The contents on cursor `next` are opaque, clients are not expected to make any assumptions on the format of the data inside the cursor.
 */
type CursorPagination = {
  next?: string;
  sort_by?: string;
  sort_order?: string;
};
type Snapshot = {
  id: string;
  name: string;
  lsn?: string;
  timestamp?: string;
  source_branch_id?: string;
  created_at: string;
  expires_at?: string;
  manual?: boolean;
  /**
   * Full logical size of the snapshot in bytes at the time it was taken.
   *
   * When absent, the logical size has not been calculated yet and the snapshot is not being charged.
   *
   * When present, a value of 0 means the snapshot is not being charged.
   *
   */
  full_size?: number;
  /**
   * Incremental storage size in bytes since the previous scheduled snapshot, when the snapshot is billed on incremental (diff) usage.
   *
   * When absent, either the incremental size has not been calculated yet and the snapshot is not being charged, or the snapshot is charged at full logical size (in that case `full_size` is set).
   *
   */
  diff_size?: number;
};
type SnapshotUpdateRequest = {
  snapshot: {
    name?: string;
  };
};
type BackupScheduleItem = {
  /**
   * How often to take snapshots. Must be one of the following values:
   * - `hourly`
   * - `daily`
   * - `weekly`
   * - `monthly`
   * - `yearly`
   *
   */
  frequency: string;
  /**
   * The hour of the day to take the snapshot (if applicable).
   *
   */
  hour?: number;
  /**
   * The day of the week or month to take the snapshot (if applicable).
   *
   */
  day?: number;
  /**
   * The month of the year to take the snapshot (if applicable).
   *
   */
  month?: number;
  /**
   * How long to keep a snapshot (in seconds) before it's automatically deleted.
   * If not set, the snapshot is kept indefinitely.
   *
   */
  retention_seconds?: number;
};
type BackupSchedule = {
  schedule: Array<BackupScheduleItem>;
};
type SupportTicket = {
  salesforce_id?: string;
  failed_uploads?: Array<string>;
};
type BranchSchemaJson = {
  tables: Array<{
    schema: string;
    name: string;
    columns: Array<{
      name: string;
      type: string;
      /**
       * Whether the column allows NULL values
       */
      nullable?: boolean;
      /**
       * Whether the column is a generated (computed) column
       */
      generated?: boolean;
    }>;
    constraints?: Array<{
      /**
       * Type of constraint. Possible values: `primary_key`, `unique`, `foreign_key`
       *
       */
      type: string;
      /**
       * Column names participating in this constraint
       */
      columns: Array<string>;
      /**
       * Optional name of the constraint
       */
      name?: string;
      /**
       * For foreign keys - the referenced table information
       */
      referenced_table?: {
        /**
         * Schema of the referenced table
         */
        schema: string;
        /**
         * Name of the referenced table
         */
        table: string;
        /**
         * Names of the referenced columns
         */
        columns: Array<string>;
      };
    }>;
  }>;
};
type SystemStatusSummaryResponse = {
  ongoing_incidents: number;
  in_progress_maintenances: number;
  scheduled_maintenances: number;
};
/**
 * A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list.
 */
type CursorParam = string;
/**
 * The maximum number of records to be returned in the response
 */
type LimitParam = number;
/**
 * Defines the sorting order of entities.
 */
type SortOrderParam = 'asc' | 'desc';
/**
 * Specify an explicit timeout in milliseconds to limit response delay.
 * After timing out, the incomplete list of project data fetched so far will be returned.
 * Projects still being fetched when the timeout occurred are listed in the "unavailable" attribute of the response.
 * If not specified, an implicit implementation defined timeout is chosen with the same behaviour as above
 *
 */
type TimeoutParam = number;
type GetProjectAdvisorSecurityIssuesData = {
  body?: never;
  path: {
    /**
     * Neon project ID
     */
    project_id: string;
  };
  query?: {
    /**
     * Branch ID to analyze. If not specified, the project's default branch is used.
     */
    branch_id?: string;
    /**
     * Database name to analyze. Required if branch has multiple databases.
     */
    database_name?: string;
    /**
     * Filter issues by category
     */
    category?: AdvisorCategory;
    /**
     * Minimum severity level to include. For example, WARN returns WARN and ERROR issues, excluding INFO.
     */
    min_severity?: 'INFO' | 'WARN' | 'ERROR';
  };
  url: '/projects/{project_id}/advisors';
};
type GetProjectAdvisorSecurityIssuesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectAdvisorSecurityIssuesError = GetProjectAdvisorSecurityIssuesErrors[keyof GetProjectAdvisorSecurityIssuesErrors];
type GetProjectAdvisorSecurityIssuesResponses = {
  /**
   * Successfully retrieved security advisor issues
   */
  200: {
    issues: Array<AdvisorIssue>;
  };
};
type GetProjectAdvisorSecurityIssuesResponse = GetProjectAdvisorSecurityIssuesResponses[keyof GetProjectAdvisorSecurityIssuesResponses];
type ListApiKeysData = {
  body?: never;
  path?: never;
  query?: never;
  url: '/api_keys';
};
type ListApiKeysErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListApiKeysError = ListApiKeysErrors[keyof ListApiKeysErrors];
type ListApiKeysResponses = {
  /**
   * Returned the API keys for the Neon account
   */
  200: Array<ApiKeysListResponseItem>;
};
type ListApiKeysResponse = ListApiKeysResponses[keyof ListApiKeysResponses];
type CreateApiKeyData = {
  body: ApiKeyCreateRequest;
  path?: never;
  query?: never;
  url: '/api_keys';
};
type CreateApiKeyErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateApiKeyError = CreateApiKeyErrors[keyof CreateApiKeyErrors];
type CreateApiKeyResponses = {
  /**
   * Created an API key
   */
  200: ApiKeyCreateResponse;
};
type CreateApiKeyResponse = CreateApiKeyResponses[keyof CreateApiKeyResponses];
type RevokeApiKeyData = {
  body?: never;
  path: {
    /**
     * The API key ID
     */
    key_id: number;
  };
  query?: never;
  url: '/api_keys/{key_id}';
};
type RevokeApiKeyErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RevokeApiKeyError = RevokeApiKeyErrors[keyof RevokeApiKeyErrors];
type RevokeApiKeyResponses = {
  /**
   * Revoked the specified API key
   */
  200: ApiKeyRevokeResponse;
};
type RevokeApiKeyResponse = RevokeApiKeyResponses[keyof RevokeApiKeyResponses];
type GetProjectOperationData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The operation ID
     */
    operation_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/operations/{operation_id}';
};
type GetProjectOperationErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectOperationError = GetProjectOperationErrors[keyof GetProjectOperationErrors];
type GetProjectOperationResponses = {
  /**
   * Returned details for the specified operation
   */
  200: OperationResponse;
};
type GetProjectOperationResponse = GetProjectOperationResponses[keyof GetProjectOperationResponses];
type ListProjectsData = {
  body?: never;
  path?: never;
  query?: {
    /**
     * Specify the cursor value from the previous response to retrieve the next batch of projects.
     */
    cursor?: string;
    /**
     * Specify a value from 1 to 400 to limit number of projects in the response.
     */
    limit?: number;
    /**
     * Search by project `name` or `id`. You can specify partial `name` or `id` values to filter results.
     */
    search?: string;
    /**
     * Search for projects by `org_id`.
     */
    org_id?: string;
    /**
     * Specify an explicit timeout in milliseconds to limit response delay.
     * After timing out, the incomplete list of project data fetched so far will be returned.
     * Projects still being fetched when the timeout occurred are listed in the "unavailable" attribute of the response.
     * If not specified, an implicit implementation defined timeout is chosen with the same behaviour as above
     *
     */
    timeout?: number;
    /**
     * Show only deleted projects within the recovery window.
     *
     */
    recoverable?: boolean;
  };
  url: '/projects';
};
type ListProjectsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectsError = ListProjectsErrors[keyof ListProjectsErrors];
type ListProjectsResponses = {
  /**
   * Returned a list of projects for the Neon account
   */
  200: ProjectsResponse & PaginationResponse & ProjectsApplicationsMapResponse & ProjectsIntegrationsMapResponse;
};
type ListProjectsResponse = ListProjectsResponses[keyof ListProjectsResponses];
type CreateProjectData = {
  body: ProjectCreateRequest;
  path?: never;
  query?: never;
  url: '/projects';
};
type CreateProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectError = CreateProjectErrors[keyof CreateProjectErrors];
type CreateProjectResponses = {
  /**
   * Created a project.
   * The project includes a connection URI with a database, password, and role.
   * At least one non-protected role is created with a password.
   * Wait until the operations are finished before attempting to connect to a project database.
   *
   */
  201: ProjectResponse & ConnectionUrisResponse & RolesResponse & DatabasesResponse & OperationsResponse & BranchResponse & EndpointsResponse;
};
type CreateProjectResponse = CreateProjectResponses[keyof CreateProjectResponses];
type ListSharedProjectsData = {
  body?: never;
  path?: never;
  query?: {
    /**
     * Specify the cursor value from the previous response to get the next batch of projects.
     */
    cursor?: string;
    /**
     * Specify a value from 1 to 400 to limit number of projects in the response.
     */
    limit?: number;
    /**
     * Search query by name or id.
     */
    search?: string;
    /**
     * Specify an explicit timeout in milliseconds to limit response delay.
     * After timing out, the incomplete list of project data fetched so far will be returned.
     * Projects still being fetched when the timeout occurred are listed in the "unavailable" attribute of the response.
     * If not specified, an implicit implementation defined timeout is chosen with the same behaviour as above
     *
     */
    timeout?: number;
  };
  url: '/projects/shared';
};
type ListSharedProjectsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListSharedProjectsError = ListSharedProjectsErrors[keyof ListSharedProjectsErrors];
type ListSharedProjectsResponses = {
  /**
   * Returned a list of shared projects for the Neon account
   */
  200: ProjectsResponse & PaginationResponse;
};
type ListSharedProjectsResponse = ListSharedProjectsResponses[keyof ListSharedProjectsResponses];
type DeleteProjectData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}';
};
type DeleteProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectError = DeleteProjectErrors[keyof DeleteProjectErrors];
type DeleteProjectResponses = {
  /**
   * Deleted the specified project
   */
  200: ProjectResponse;
};
type DeleteProjectResponse = DeleteProjectResponses[keyof DeleteProjectResponses];
type GetProjectData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}';
};
type GetProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectError = GetProjectErrors[keyof GetProjectErrors];
type GetProjectResponses = {
  /**
   * Returned information about the specified project
   */
  200: ProjectResponse;
};
type GetProjectResponse = GetProjectResponses[keyof GetProjectResponses];
type UpdateProjectData = {
  body: ProjectUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}';
};
type UpdateProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateProjectError = UpdateProjectErrors[keyof UpdateProjectErrors];
type UpdateProjectResponses = {
  /**
   * Updated the specified project
   */
  200: ProjectResponse & OperationsResponse;
};
type UpdateProjectResponse = UpdateProjectResponses[keyof UpdateProjectResponses];
type RecoverProjectData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/recover';
};
type RecoverProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RecoverProjectError = RecoverProjectErrors[keyof RecoverProjectErrors];
type RecoverProjectResponses = {
  /**
   * Returned the recovered project
   */
  200: ProjectRecoverResponse;
};
type RecoverProjectResponse = RecoverProjectResponses[keyof RecoverProjectResponses];
type ListProjectOperationsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: {
    /**
     * Specify the cursor value from the previous response to get the next batch of operations
     */
    cursor?: string;
    /**
     * Specify a value from 1 to 1000 to limit number of operations in the response
     */
    limit?: number;
  };
  url: '/projects/{project_id}/operations';
};
type ListProjectOperationsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectOperationsError = ListProjectOperationsErrors[keyof ListProjectOperationsErrors];
type ListProjectOperationsResponses = {
  /**
   * Returned a list of operations
   *
   */
  200: OperationsResponse & PaginationResponse;
};
type ListProjectOperationsResponse = ListProjectOperationsResponses[keyof ListProjectOperationsResponses];
type ListProjectPermissionsData = {
  body?: never;
  path: {
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/permissions';
};
type ListProjectPermissionsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectPermissionsError = ListProjectPermissionsErrors[keyof ListProjectPermissionsErrors];
type ListProjectPermissionsResponses = {
  /**
   * Returned project access details
   */
  200: ProjectPermissions;
};
type ListProjectPermissionsResponse = ListProjectPermissionsResponses[keyof ListProjectPermissionsResponses];
type GrantPermissionToProjectData = {
  body: GrantPermissionToProjectRequest;
  path: {
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/permissions';
};
type GrantPermissionToProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GrantPermissionToProjectError = GrantPermissionToProjectErrors[keyof GrantPermissionToProjectErrors];
type GrantPermissionToProjectResponses = {
  /**
   * Granted project access
   */
  200: ProjectPermission;
};
type GrantPermissionToProjectResponse = GrantPermissionToProjectResponses[keyof GrantPermissionToProjectResponses];
type RevokePermissionFromProjectData = {
  body?: never;
  path: {
    project_id: string;
    permission_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/permissions/{permission_id}';
};
type RevokePermissionFromProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RevokePermissionFromProjectError = RevokePermissionFromProjectErrors[keyof RevokePermissionFromProjectErrors];
type RevokePermissionFromProjectResponses = {
  /**
   * Revoked project access
   */
  200: ProjectPermission;
};
type RevokePermissionFromProjectResponse = RevokePermissionFromProjectResponses[keyof RevokePermissionFromProjectResponses];
type GetAvailablePreloadLibrariesData = {
  body?: never;
  path: {
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/available_preload_libraries';
};
type GetAvailablePreloadLibrariesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetAvailablePreloadLibrariesError = GetAvailablePreloadLibrariesErrors[keyof GetAvailablePreloadLibrariesErrors];
type GetAvailablePreloadLibrariesResponses = {
  /**
   * Successfully returned available shared preload libraries
   */
  200: AvailablePreloadLibraries;
};
type GetAvailablePreloadLibrariesResponse = GetAvailablePreloadLibrariesResponses[keyof GetAvailablePreloadLibrariesResponses];
type CreateProjectTransferRequestData = {
  body?: {
    /**
     * Specifies the validity duration of the transfer request in seconds. If not provided,
     * the request will expire after 24 hours (86,400 seconds).
     *
     */
    ttl_seconds?: number;
  };
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/transfer_requests';
};
type CreateProjectTransferRequestErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectTransferRequestError = CreateProjectTransferRequestErrors[keyof CreateProjectTransferRequestErrors];
type CreateProjectTransferRequestResponses = {
  /**
   * Project transfer request created successfully
   */
  201: ProjectTransferRequestResponse;
};
type CreateProjectTransferRequestResponse = CreateProjectTransferRequestResponses[keyof CreateProjectTransferRequestResponses];
type AcceptProjectTransferRequestData = {
  body?: {
    /**
     * The Neon organization ID to transfer the project to. If not provided, the project will be
     * transferred to the current user or organization account.
     *
     */
    org_id?: string;
  };
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon project transfer request ID
     */
    request_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/transfer_requests/{request_id}';
};
type AcceptProjectTransferRequestErrors = {
  /**
   * Account doesn't satisfy the plan requirements to own the project
   */
  406: AcceptProjectTransferRequestSatisfiesPlanError;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AcceptProjectTransferRequestError = AcceptProjectTransferRequestErrors[keyof AcceptProjectTransferRequestErrors];
type AcceptProjectTransferRequestResponses = {
  /**
   * Project transfer request accepted successfully
   */
  204: void;
};
type AcceptProjectTransferRequestResponse = AcceptProjectTransferRequestResponses[keyof AcceptProjectTransferRequestResponses];
type GetProjectJwksData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/jwks';
};
type GetProjectJwksErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectJwksError = GetProjectJwksErrors[keyof GetProjectJwksErrors];
type GetProjectJwksResponses = {
  /**
   * The JWKS URLs available for the project
   */
  200: ProjectJwksResponse;
};
type GetProjectJwksResponse = GetProjectJwksResponses[keyof GetProjectJwksResponses];
type AddProjectJwksData = {
  body: AddProjectJwksRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/jwks';
};
type AddProjectJwksErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AddProjectJwksError = AddProjectJwksErrors[keyof AddProjectJwksErrors];
type AddProjectJwksResponses = {
  /**
   * The JWKS URL was added to the project's authentication connections
   */
  201: JwksCreationOperation;
};
type AddProjectJwksResponse = AddProjectJwksResponses[keyof AddProjectJwksResponses];
type DeleteProjectJwksData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The JWKS ID
     */
    jwks_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/jwks/{jwks_id}';
};
type DeleteProjectJwksErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectJwksError = DeleteProjectJwksErrors[keyof DeleteProjectJwksErrors];
type DeleteProjectJwksResponses = {
  /**
   * Deleted a JWKS URL from the project
   */
  200: Jwks;
};
type DeleteProjectJwksResponse = DeleteProjectJwksResponses[keyof DeleteProjectJwksResponses];
type DeleteProjectBranchDataApiData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/data-api/{database_name}';
};
type DeleteProjectBranchDataApiErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectBranchDataApiError = DeleteProjectBranchDataApiErrors[keyof DeleteProjectBranchDataApiErrors];
type DeleteProjectBranchDataApiResponses = {
  /**
   * Deleted the Neon Data API for the specified branch
   */
  200: EmptyResponse;
};
type DeleteProjectBranchDataApiResponse = DeleteProjectBranchDataApiResponses[keyof DeleteProjectBranchDataApiResponses];
type GetProjectBranchDataApiData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/data-api/{database_name}';
};
type GetProjectBranchDataApiErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchDataApiError = GetProjectBranchDataApiErrors[keyof GetProjectBranchDataApiErrors];
type GetProjectBranchDataApiResponses = {
  /**
   * Returns the Neon Data API for the specified branch
   */
  200: DataApiReponse;
};
type GetProjectBranchDataApiResponse = GetProjectBranchDataApiResponses[keyof GetProjectBranchDataApiResponses];
type UpdateProjectBranchDataApiData = {
  body?: DataApiUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/data-api/{database_name}';
};
type UpdateProjectBranchDataApiErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateProjectBranchDataApiError = UpdateProjectBranchDataApiErrors[keyof UpdateProjectBranchDataApiErrors];
type UpdateProjectBranchDataApiResponses = {
  /**
   * Updated the Neon Data API configuration and refreshed the schema cache
   */
  201: EmptyResponse;
};
type UpdateProjectBranchDataApiResponse = UpdateProjectBranchDataApiResponses[keyof UpdateProjectBranchDataApiResponses];
type CreateProjectBranchDataApiData = {
  body?: DataApiCreateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/data-api/{database_name}';
};
type CreateProjectBranchDataApiErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectBranchDataApiError = CreateProjectBranchDataApiErrors[keyof CreateProjectBranchDataApiErrors];
type CreateProjectBranchDataApiResponses = {
  /**
   * Creates a new app
   */
  201: DataApiCreateResponse;
};
type CreateProjectBranchDataApiResponse = CreateProjectBranchDataApiResponses[keyof CreateProjectBranchDataApiResponses];
type CreateNeonAuthIntegrationData = {
  body: NeonAuthCreateIntegrationRequest;
  path?: never;
  query?: never;
  url: '/projects/auth/create';
};
type CreateNeonAuthIntegrationErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateNeonAuthIntegrationError = CreateNeonAuthIntegrationErrors[keyof CreateNeonAuthIntegrationErrors];
type CreateNeonAuthIntegrationResponses = {
  /**
   * Creates Neon Auth integration
   */
  201: NeonAuthCreateIntegrationResponse;
};
type CreateNeonAuthIntegrationResponse = CreateNeonAuthIntegrationResponses[keyof CreateNeonAuthIntegrationResponses];
type DisableNeonAuthData = {
  body?: {
    /**
     * If true, deletes the `neon_auth` schema from the database
     */
    delete_data?: boolean;
  };
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth';
};
type DisableNeonAuthErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DisableNeonAuthError = DisableNeonAuthErrors[keyof DisableNeonAuthErrors];
type DisableNeonAuthResponses = {
  /**
   * Delete the integration with the authentication provider
   */
  200: unknown;
};
type GetNeonAuthData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth';
};
type GetNeonAuthErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthError = GetNeonAuthErrors[keyof GetNeonAuthErrors];
type GetNeonAuthResponses = {
  /**
   * Fetched the details of the Neon Auth integration for the specified branch
   */
  200: NeonAuthIntegration;
};
type GetNeonAuthResponse = GetNeonAuthResponses[keyof GetNeonAuthResponses];
type CreateNeonAuthData = {
  body: EnableNeonAuthIntegrationRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth';
};
type CreateNeonAuthErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateNeonAuthError = CreateNeonAuthErrors[keyof CreateNeonAuthErrors];
type CreateNeonAuthResponses = {
  /**
   * Enables Neon Auth integration for the branch
   */
  201: NeonAuthCreateIntegrationResponse;
};
type CreateNeonAuthResponse = CreateNeonAuthResponses[keyof CreateNeonAuthResponses];
type DeleteNeonAuthDomainFromRedirectUriWhitelistData = {
  body: NeonAuthDeleteDomainFromRedirectUriWhitelistRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/domains';
};
type DeleteNeonAuthDomainFromRedirectUriWhitelistErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteNeonAuthDomainFromRedirectUriWhitelistError = DeleteNeonAuthDomainFromRedirectUriWhitelistErrors[keyof DeleteNeonAuthDomainFromRedirectUriWhitelistErrors];
type DeleteNeonAuthDomainFromRedirectUriWhitelistResponses = {
  /**
   * Deleted the domain from the redirect_uri whitelist
   */
  200: unknown;
};
type ListNeonAuthRedirectUriWhitelistDomainsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/domains';
};
type ListNeonAuthRedirectUriWhitelistDomainsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListNeonAuthRedirectUriWhitelistDomainsError = ListNeonAuthRedirectUriWhitelistDomainsErrors[keyof ListNeonAuthRedirectUriWhitelistDomainsErrors];
type ListNeonAuthRedirectUriWhitelistDomainsResponses = {
  /**
   * Returned the domains in the redirect_uri whitelist
   */
  200: NeonAuthRedirectUriWhitelistResponse;
};
type ListNeonAuthRedirectUriWhitelistDomainsResponse = ListNeonAuthRedirectUriWhitelistDomainsResponses[keyof ListNeonAuthRedirectUriWhitelistDomainsResponses];
type AddNeonAuthDomainToRedirectUriWhitelistData = {
  body: NeonAuthAddDomainToRedirectUriWhitelistRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/domains';
};
type AddNeonAuthDomainToRedirectUriWhitelistErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AddNeonAuthDomainToRedirectUriWhitelistError = AddNeonAuthDomainToRedirectUriWhitelistErrors[keyof AddNeonAuthDomainToRedirectUriWhitelistErrors];
type AddNeonAuthDomainToRedirectUriWhitelistResponses = {
  /**
   * Added the domain to the redirect_uri whitelist
   */
  201: unknown;
};
type DeleteBranchNeonAuthTrustedDomainData = {
  body: NeonAuthDeleteDomainFromRedirectUriWhitelistRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/domains';
};
type DeleteBranchNeonAuthTrustedDomainErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteBranchNeonAuthTrustedDomainError = DeleteBranchNeonAuthTrustedDomainErrors[keyof DeleteBranchNeonAuthTrustedDomainErrors];
type DeleteBranchNeonAuthTrustedDomainResponses = {
  /**
   * Deleted the domain from the redirect_uri whitelist
   */
  200: unknown;
};
type ListBranchNeonAuthTrustedDomainsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/domains';
};
type ListBranchNeonAuthTrustedDomainsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListBranchNeonAuthTrustedDomainsError = ListBranchNeonAuthTrustedDomainsErrors[keyof ListBranchNeonAuthTrustedDomainsErrors];
type ListBranchNeonAuthTrustedDomainsResponses = {
  /**
   * Returned the domains in the redirect_uri whitelist
   */
  200: NeonAuthRedirectUriWhitelistResponse;
};
type ListBranchNeonAuthTrustedDomainsResponse = ListBranchNeonAuthTrustedDomainsResponses[keyof ListBranchNeonAuthTrustedDomainsResponses];
type AddBranchNeonAuthTrustedDomainData = {
  body: NeonAuthAddDomainToRedirectUriWhitelistRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/domains';
};
type AddBranchNeonAuthTrustedDomainErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AddBranchNeonAuthTrustedDomainError = AddBranchNeonAuthTrustedDomainErrors[keyof AddBranchNeonAuthTrustedDomainErrors];
type AddBranchNeonAuthTrustedDomainResponses = {
  /**
   * Added the domain to the redirect_uri whitelist
   */
  201: unknown;
};
type CreateNeonAuthProviderSdkKeysData = {
  body: NeonAuthCreateAuthProviderSdkKeysRequest;
  path?: never;
  query?: never;
  url: '/projects/auth/keys';
};
type CreateNeonAuthProviderSdkKeysErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateNeonAuthProviderSdkKeysError = CreateNeonAuthProviderSdkKeysErrors[keyof CreateNeonAuthProviderSdkKeysErrors];
type CreateNeonAuthProviderSdkKeysResponses = {
  /**
   * Creates Auth Provider SDK keys
   */
  201: NeonAuthCreateIntegrationResponse;
};
type CreateNeonAuthProviderSdkKeysResponse = CreateNeonAuthProviderSdkKeysResponses[keyof CreateNeonAuthProviderSdkKeysResponses];
type CreateNeonAuthNewUserData = {
  body: NeonAuthCreateNewUserRequest;
  path?: never;
  query?: never;
  url: '/projects/auth/user';
};
type CreateNeonAuthNewUserErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateNeonAuthNewUserError = CreateNeonAuthNewUserErrors[keyof CreateNeonAuthNewUserErrors];
type CreateNeonAuthNewUserResponses = {
  /**
   * Creates new user
   */
  201: NeonAuthCreateNewUserResponse;
};
type CreateNeonAuthNewUserResponse = CreateNeonAuthNewUserResponses[keyof CreateNeonAuthNewUserResponses];
type CreateBranchNeonAuthNewUserData = {
  body: CreateBranchNeonAuthNewUserRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/users';
};
type CreateBranchNeonAuthNewUserErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateBranchNeonAuthNewUserError = CreateBranchNeonAuthNewUserErrors[keyof CreateBranchNeonAuthNewUserErrors];
type CreateBranchNeonAuthNewUserResponses = {
  /**
   * Creates new user
   */
  201: NeonAuthCreateNewUserResponse;
};
type CreateBranchNeonAuthNewUserResponse = CreateBranchNeonAuthNewUserResponses[keyof CreateBranchNeonAuthNewUserResponses];
type DeleteBranchNeonAuthUserData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The Neon user ID
     */
    auth_user_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/users/{auth_user_id}';
};
type DeleteBranchNeonAuthUserErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteBranchNeonAuthUserError = DeleteBranchNeonAuthUserErrors[keyof DeleteBranchNeonAuthUserErrors];
type DeleteBranchNeonAuthUserResponses = {
  /**
   * Deleted the auth user
   */
  204: void;
};
type DeleteBranchNeonAuthUserResponse = DeleteBranchNeonAuthUserResponses[keyof DeleteBranchNeonAuthUserResponses];
type UpdateNeonAuthUserRoleData = {
  body: UpdateNeonAuthUserRoleRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The Neon user ID
     */
    auth_user_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/users/{auth_user_id}/role';
};
type UpdateNeonAuthUserRoleErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthUserRoleError = UpdateNeonAuthUserRoleErrors[keyof UpdateNeonAuthUserRoleErrors];
type UpdateNeonAuthUserRoleResponses = {
  /**
   * Updated the auth user role
   */
  200: UpdateNeonAuthUserRoleResponse;
};
type UpdateNeonAuthUserRoleResponse2 = UpdateNeonAuthUserRoleResponses[keyof UpdateNeonAuthUserRoleResponses];
type DeleteNeonAuthUserData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon user ID
     */
    auth_user_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/users/{auth_user_id}';
};
type DeleteNeonAuthUserErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteNeonAuthUserError = DeleteNeonAuthUserErrors[keyof DeleteNeonAuthUserErrors];
type DeleteNeonAuthUserResponses = {
  /**
   * Deleted the auth user
   */
  204: void;
};
type DeleteNeonAuthUserResponse = DeleteNeonAuthUserResponses[keyof DeleteNeonAuthUserResponses];
type TransferNeonAuthProviderProjectData = {
  body: NeonAuthTransferAuthProviderProjectRequest;
  path?: never;
  query?: never;
  url: '/projects/auth/transfer_ownership';
};
type TransferNeonAuthProviderProjectErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type TransferNeonAuthProviderProjectError = TransferNeonAuthProviderProjectErrors[keyof TransferNeonAuthProviderProjectErrors];
type TransferNeonAuthProviderProjectResponses = {
  /**
   * Transfer initiated. Follow the URL to complete the process in your auth provider's UI.
   */
  200: NeonAuthTransferAuthProviderProjectResponse;
};
type TransferNeonAuthProviderProjectResponse = TransferNeonAuthProviderProjectResponses[keyof TransferNeonAuthProviderProjectResponses];
type ListNeonAuthIntegrationsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/integrations';
};
type ListNeonAuthIntegrationsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListNeonAuthIntegrationsError = ListNeonAuthIntegrationsErrors[keyof ListNeonAuthIntegrationsErrors];
type ListNeonAuthIntegrationsResponses = {
  /**
   * Return management API keys metadata
   */
  200: ListNeonAuthIntegrationsResponse;
};
type ListNeonAuthIntegrationsResponse2 = ListNeonAuthIntegrationsResponses[keyof ListNeonAuthIntegrationsResponses];
type ListNeonAuthOauthProvidersData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/oauth_providers';
};
type ListNeonAuthOauthProvidersErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListNeonAuthOauthProvidersError = ListNeonAuthOauthProvidersErrors[keyof ListNeonAuthOauthProvidersErrors];
type ListNeonAuthOauthProvidersResponses = {
  /**
   * Returns the OAuth providers for the Neon Auth
   */
  200: ListNeonAuthOauthProvidersResponse;
};
type ListNeonAuthOauthProvidersResponse2 = ListNeonAuthOauthProvidersResponses[keyof ListNeonAuthOauthProvidersResponses];
type AddNeonAuthOauthProviderData = {
  body: NeonAuthAddOAuthProviderRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/oauth_providers';
};
type AddNeonAuthOauthProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AddNeonAuthOauthProviderError = AddNeonAuthOauthProviderErrors[keyof AddNeonAuthOauthProviderErrors];
type AddNeonAuthOauthProviderResponses = {
  /**
   * The OAuth provider has been added to the project
   */
  200: NeonAuthOauthProvider;
};
type AddNeonAuthOauthProviderResponse = AddNeonAuthOauthProviderResponses[keyof AddNeonAuthOauthProviderResponses];
type ListBranchNeonAuthOauthProvidersData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/oauth_providers';
};
type ListBranchNeonAuthOauthProvidersErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListBranchNeonAuthOauthProvidersError = ListBranchNeonAuthOauthProvidersErrors[keyof ListBranchNeonAuthOauthProvidersErrors];
type ListBranchNeonAuthOauthProvidersResponses = {
  /**
   * Returns the OAuth providers for the Neon Auth
   */
  200: ListNeonAuthOauthProvidersResponse;
};
type ListBranchNeonAuthOauthProvidersResponse = ListBranchNeonAuthOauthProvidersResponses[keyof ListBranchNeonAuthOauthProvidersResponses];
type AddBranchNeonAuthOauthProviderData = {
  body: NeonAuthAddOAuthProviderRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/oauth_providers';
};
type AddBranchNeonAuthOauthProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AddBranchNeonAuthOauthProviderError = AddBranchNeonAuthOauthProviderErrors[keyof AddBranchNeonAuthOauthProviderErrors];
type AddBranchNeonAuthOauthProviderResponses = {
  /**
   * The OAuth provider has been added to the project
   */
  200: NeonAuthOauthProvider;
};
type AddBranchNeonAuthOauthProviderResponse = AddBranchNeonAuthOauthProviderResponses[keyof AddBranchNeonAuthOauthProviderResponses];
type DeleteNeonAuthOauthProviderData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The OAuth provider ID
     */
    oauth_provider_id: NeonAuthOauthProviderId;
  };
  query?: never;
  url: '/projects/{project_id}/auth/oauth_providers/{oauth_provider_id}';
};
type DeleteNeonAuthOauthProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteNeonAuthOauthProviderError = DeleteNeonAuthOauthProviderErrors[keyof DeleteNeonAuthOauthProviderErrors];
type DeleteNeonAuthOauthProviderResponses = {
  /**
   * Deleted the OAuth provider from the project
   */
  200: unknown;
};
type UpdateNeonAuthOauthProviderData = {
  body: NeonAuthUpdateOAuthProviderRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The OAuth provider ID
     */
    oauth_provider_id: NeonAuthOauthProviderId;
  };
  query?: never;
  url: '/projects/{project_id}/auth/oauth_providers/{oauth_provider_id}';
};
type UpdateNeonAuthOauthProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthOauthProviderError = UpdateNeonAuthOauthProviderErrors[keyof UpdateNeonAuthOauthProviderErrors];
type UpdateNeonAuthOauthProviderResponses = {
  /**
   * The OAuth provider has been added to the project
   */
  200: NeonAuthOauthProvider;
};
type UpdateNeonAuthOauthProviderResponse = UpdateNeonAuthOauthProviderResponses[keyof UpdateNeonAuthOauthProviderResponses];
type DeleteBranchNeonAuthOauthProviderData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The OAuth provider ID
     */
    oauth_provider_id: NeonAuthOauthProviderId;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/oauth_providers/{oauth_provider_id}';
};
type DeleteBranchNeonAuthOauthProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteBranchNeonAuthOauthProviderError = DeleteBranchNeonAuthOauthProviderErrors[keyof DeleteBranchNeonAuthOauthProviderErrors];
type DeleteBranchNeonAuthOauthProviderResponses = {
  /**
   * Deleted the OAuth provider from the project
   */
  200: unknown;
};
type UpdateBranchNeonAuthOauthProviderData = {
  body: NeonAuthUpdateOAuthProviderRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
    /**
     * The OAuth provider ID
     */
    oauth_provider_id: NeonAuthOauthProviderId;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/oauth_providers/{oauth_provider_id}';
};
type UpdateBranchNeonAuthOauthProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateBranchNeonAuthOauthProviderError = UpdateBranchNeonAuthOauthProviderErrors[keyof UpdateBranchNeonAuthOauthProviderErrors];
type UpdateBranchNeonAuthOauthProviderResponses = {
  /**
   * The OAuth provider has been added to the project
   */
  200: NeonAuthOauthProvider;
};
type UpdateBranchNeonAuthOauthProviderResponse = UpdateBranchNeonAuthOauthProviderResponses[keyof UpdateBranchNeonAuthOauthProviderResponses];
type GetNeonAuthEmailServerData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/email_server';
};
type GetNeonAuthEmailServerErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthEmailServerError = GetNeonAuthEmailServerErrors[keyof GetNeonAuthEmailServerErrors];
type GetNeonAuthEmailServerResponses = {
  /**
   * Returns the email server configuration for the Neon Auth
   */
  200: NeonAuthEmailServerConfig;
};
type GetNeonAuthEmailServerResponse = GetNeonAuthEmailServerResponses[keyof GetNeonAuthEmailServerResponses];
type UpdateNeonAuthEmailServerData = {
  body: NeonAuthEmailServerConfig;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/auth/email_server';
};
type UpdateNeonAuthEmailServerErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthEmailServerError = UpdateNeonAuthEmailServerErrors[keyof UpdateNeonAuthEmailServerErrors];
type UpdateNeonAuthEmailServerResponses = {
  /**
   * The OAuth provider has been added to the project
   */
  200: NeonAuthEmailServerConfig;
};
type UpdateNeonAuthEmailServerResponse = UpdateNeonAuthEmailServerResponses[keyof UpdateNeonAuthEmailServerResponses];
type SendNeonAuthTestEmailData = {
  body: SendNeonAuthTestEmailRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/send_test_email';
};
type SendNeonAuthTestEmailErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type SendNeonAuthTestEmailError = SendNeonAuthTestEmailErrors[keyof SendNeonAuthTestEmailErrors];
type SendNeonAuthTestEmailResponses = {
  /**
   * Response with the result of the test email send
   */
  200: SendNeonAuthTestEmailResponse;
};
type SendNeonAuthTestEmailResponse2 = SendNeonAuthTestEmailResponses[keyof SendNeonAuthTestEmailResponses];
type GetNeonAuthEmailAndPasswordConfigData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/email_and_password';
};
type GetNeonAuthEmailAndPasswordConfigErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthEmailAndPasswordConfigError = GetNeonAuthEmailAndPasswordConfigErrors[keyof GetNeonAuthEmailAndPasswordConfigErrors];
type GetNeonAuthEmailAndPasswordConfigResponses = {
  /**
   * Returns the email and password configuration for Neon Auth
   */
  200: NeonAuthEmailAndPasswordConfig;
};
type GetNeonAuthEmailAndPasswordConfigResponse = GetNeonAuthEmailAndPasswordConfigResponses[keyof GetNeonAuthEmailAndPasswordConfigResponses];
type UpdateNeonAuthEmailAndPasswordConfigData = {
  body: NeonAuthEmailAndPasswordConfigUpdate;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/email_and_password';
};
type UpdateNeonAuthEmailAndPasswordConfigErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthEmailAndPasswordConfigError = UpdateNeonAuthEmailAndPasswordConfigErrors[keyof UpdateNeonAuthEmailAndPasswordConfigErrors];
type UpdateNeonAuthEmailAndPasswordConfigResponses = {
  /**
   * The email and password configuration has been updated
   */
  200: NeonAuthEmailAndPasswordConfig;
};
type UpdateNeonAuthEmailAndPasswordConfigResponse = UpdateNeonAuthEmailAndPasswordConfigResponses[keyof UpdateNeonAuthEmailAndPasswordConfigResponses];
type GetNeonAuthEmailProviderData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/email_provider';
};
type GetNeonAuthEmailProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthEmailProviderError = GetNeonAuthEmailProviderErrors[keyof GetNeonAuthEmailProviderErrors];
type GetNeonAuthEmailProviderResponses = {
  /**
   * Returns the email provider configuration for the Neon Auth
   */
  200: NeonAuthEmailServerConfig;
};
type GetNeonAuthEmailProviderResponse = GetNeonAuthEmailProviderResponses[keyof GetNeonAuthEmailProviderResponses];
type UpdateNeonAuthEmailProviderData = {
  body: NeonAuthEmailServerConfig;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/email_provider';
};
type UpdateNeonAuthEmailProviderErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthEmailProviderError = UpdateNeonAuthEmailProviderErrors[keyof UpdateNeonAuthEmailProviderErrors];
type UpdateNeonAuthEmailProviderResponses = {
  /**
   * The email provider configuration has been updated
   */
  200: NeonAuthEmailServerConfig;
};
type UpdateNeonAuthEmailProviderResponse = UpdateNeonAuthEmailProviderResponses[keyof UpdateNeonAuthEmailProviderResponses];
type DeleteNeonAuthIntegrationData = {
  body?: {
    /**
     * If true, deletes the `neon_auth` schema from the database
     */
    delete_data?: boolean;
  };
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The authentication provider name
     */
    auth_provider: NeonAuthSupportedAuthProvider;
  };
  query?: never;
  url: '/projects/{project_id}/auth/integration/{auth_provider}';
};
type DeleteNeonAuthIntegrationErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteNeonAuthIntegrationError = DeleteNeonAuthIntegrationErrors[keyof DeleteNeonAuthIntegrationErrors];
type DeleteNeonAuthIntegrationResponses = {
  /**
   * Delete the integration with the authentication provider
   */
  200: unknown;
};
type GetConnectionUriData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query: {
    /**
     * The branch ID. Defaults to your project's default `branch_id` if not specified.
     */
    branch_id?: string;
    /**
     * The endpoint ID. Defaults to the read-write `endpoint_id` associated with the `branch_id` if not specified.
     */
    endpoint_id?: string;
    /**
     * The database name
     */
    database_name: string;
    /**
     * The role name
     */
    role_name: string;
    /**
     * Adds the `-pooler` option to the connection URI when set to `true`, creating a pooled connection URI.
     */
    pooled?: boolean;
  };
  url: '/projects/{project_id}/connection_uri';
};
type GetConnectionUriErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetConnectionUriError = GetConnectionUriErrors[keyof GetConnectionUriErrors];
type GetConnectionUriResponses = {
  /**
   * Returned the connection URI
   */
  200: ConnectionUriResponse;
};
type GetConnectionUriResponse = GetConnectionUriResponses[keyof GetConnectionUriResponses];
type GetNeonAuthAllowLocalhostData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/allow_localhost';
};
type GetNeonAuthAllowLocalhostErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthAllowLocalhostError = GetNeonAuthAllowLocalhostErrors[keyof GetNeonAuthAllowLocalhostErrors];
type GetNeonAuthAllowLocalhostResponses = {
  /**
   * The allow localhost configuration
   */
  200: NeonAuthAllowLocalhostResponse;
};
type GetNeonAuthAllowLocalhostResponse = GetNeonAuthAllowLocalhostResponses[keyof GetNeonAuthAllowLocalhostResponses];
type UpdateNeonAuthAllowLocalhostData = {
  body: UpdateNeonAuthAllowLocalhostRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/allow_localhost';
};
type UpdateNeonAuthAllowLocalhostErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthAllowLocalhostError = UpdateNeonAuthAllowLocalhostErrors[keyof UpdateNeonAuthAllowLocalhostErrors];
type UpdateNeonAuthAllowLocalhostResponses = {
  /**
   * Updated the allow localhost configuration
   */
  200: NeonAuthAllowLocalhostResponse;
};
type UpdateNeonAuthAllowLocalhostResponse = UpdateNeonAuthAllowLocalhostResponses[keyof UpdateNeonAuthAllowLocalhostResponses];
type GetNeonAuthPluginConfigsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/plugins';
};
type GetNeonAuthPluginConfigsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthPluginConfigsError = GetNeonAuthPluginConfigsErrors[keyof GetNeonAuthPluginConfigsErrors];
type GetNeonAuthPluginConfigsResponses = {
  /**
   * Returns all plugin configurations
   */
  200: NeonAuthPluginConfigs;
};
type GetNeonAuthPluginConfigsResponse = GetNeonAuthPluginConfigsResponses[keyof GetNeonAuthPluginConfigsResponses];
type UpdateNeonAuthOrganizationPluginData = {
  body: NeonAuthOrganizationConfigUpdate;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/plugins/organization';
};
type UpdateNeonAuthOrganizationPluginErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthOrganizationPluginError = UpdateNeonAuthOrganizationPluginErrors[keyof UpdateNeonAuthOrganizationPluginErrors];
type UpdateNeonAuthOrganizationPluginResponses = {
  /**
   * The organization plugin configuration has been updated
   */
  200: NeonAuthOrganizationConfig;
};
type UpdateNeonAuthOrganizationPluginResponse = UpdateNeonAuthOrganizationPluginResponses[keyof UpdateNeonAuthOrganizationPluginResponses];
type UpdateNeonAuthConfigData = {
  body: NeonAuthConfigUpdate;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/config';
};
type UpdateNeonAuthConfigErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthConfigError = UpdateNeonAuthConfigErrors[keyof UpdateNeonAuthConfigErrors];
type UpdateNeonAuthConfigResponses = {
  /**
   * The auth configuration has been updated
   */
  200: NeonAuthConfigResponse;
};
type UpdateNeonAuthConfigResponse = UpdateNeonAuthConfigResponses[keyof UpdateNeonAuthConfigResponses];
type UpdateNeonAuthMagicLinkPluginData = {
  body: NeonAuthMagicLinkConfigUpdate;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/plugins/magic-link';
};
type UpdateNeonAuthMagicLinkPluginErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthMagicLinkPluginError = UpdateNeonAuthMagicLinkPluginErrors[keyof UpdateNeonAuthMagicLinkPluginErrors];
type UpdateNeonAuthMagicLinkPluginResponses = {
  /**
   * The magic link plugin configuration has been updated
   */
  200: NeonAuthMagicLinkConfig;
};
type UpdateNeonAuthMagicLinkPluginResponse = UpdateNeonAuthMagicLinkPluginResponses[keyof UpdateNeonAuthMagicLinkPluginResponses];
type GetNeonAuthPhoneNumberPluginData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/plugins/phone-number';
};
type GetNeonAuthPhoneNumberPluginErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthPhoneNumberPluginError = GetNeonAuthPhoneNumberPluginErrors[keyof GetNeonAuthPhoneNumberPluginErrors];
type GetNeonAuthPhoneNumberPluginResponses = {
  /**
   * Returns the phone number plugin configuration
   */
  200: NeonAuthPhoneNumberConfig;
};
type GetNeonAuthPhoneNumberPluginResponse = GetNeonAuthPhoneNumberPluginResponses[keyof GetNeonAuthPhoneNumberPluginResponses];
type UpdateNeonAuthPhoneNumberPluginData = {
  body: NeonAuthPhoneNumberConfigUpdate;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/plugins/phone-number';
};
type UpdateNeonAuthPhoneNumberPluginErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthPhoneNumberPluginError = UpdateNeonAuthPhoneNumberPluginErrors[keyof UpdateNeonAuthPhoneNumberPluginErrors];
type UpdateNeonAuthPhoneNumberPluginResponses = {
  /**
   * The phone number plugin configuration has been updated
   */
  200: NeonAuthPhoneNumberConfig;
};
type UpdateNeonAuthPhoneNumberPluginResponse = UpdateNeonAuthPhoneNumberPluginResponses[keyof UpdateNeonAuthPhoneNumberPluginResponses];
type GetNeonAuthWebhookConfigData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/webhooks';
};
type GetNeonAuthWebhookConfigErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetNeonAuthWebhookConfigError = GetNeonAuthWebhookConfigErrors[keyof GetNeonAuthWebhookConfigErrors];
type GetNeonAuthWebhookConfigResponses = {
  /**
   * Returns webhook configuration for Neon Auth
   */
  200: NeonAuthWebhookConfig;
};
type GetNeonAuthWebhookConfigResponse = GetNeonAuthWebhookConfigResponses[keyof GetNeonAuthWebhookConfigResponses];
type UpdateNeonAuthWebhookConfigData = {
  body: NeonAuthWebhookConfig;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The Neon branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/auth/webhooks';
};
type UpdateNeonAuthWebhookConfigErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateNeonAuthWebhookConfigError = UpdateNeonAuthWebhookConfigErrors[keyof UpdateNeonAuthWebhookConfigErrors];
type UpdateNeonAuthWebhookConfigResponses = {
  /**
   * Returns the updated webhook configuration
   */
  200: NeonAuthWebhookConfig;
};
type UpdateNeonAuthWebhookConfigResponse = UpdateNeonAuthWebhookConfigResponses[keyof UpdateNeonAuthWebhookConfigResponses];
type ListProjectBranchesData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: {
    /**
     * Search by branch `name` or `id`. You can specify partial `name` or `id` values to filter results.
     */
    search?: string;
    /**
     * Sort the branches by sort_field. If not provided, branches will be sorted by updated_at descending order
     */
    sort_by?: 'name' | 'created_at' | 'updated_at';
    /**
     * A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list.
     */
    cursor?: string;
    /**
     * Defines the sorting order of entities.
     */
    sort_order?: 'asc' | 'desc';
    /**
     * The maximum number of records to be returned in the response
     */
    limit?: number;
    /**
     * If true, return recoverable deleted branches too (soft-deleted within the recovery window).
     * If false or not provided, return only active (non-deleted) branches.
     *
     * This parameter is part of the Branch Recovery feature, which is in preview and not available to all users.
     *
     */
    include_deleted?: boolean;
  };
  url: '/projects/{project_id}/branches';
};
type ListProjectBranchesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectBranchesError = ListProjectBranchesErrors[keyof ListProjectBranchesErrors];
type ListProjectBranchesResponses = {
  /**
   * Returned a list of branches for the specified project
   */
  200: BranchesResponse & AnnotationsMapResponse & CursorPaginationResponse;
};
type ListProjectBranchesResponse = ListProjectBranchesResponses[keyof ListProjectBranchesResponses];
type CreateProjectBranchData = {
  body?: BranchCreateRequest & AnnotationCreateValueRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches';
};
type CreateProjectBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectBranchError = CreateProjectBranchErrors[keyof CreateProjectBranchErrors];
type CreateProjectBranchResponses = {
  /**
   * Created a branch. An endpoint is only created if it was specified in the request.
   */
  201: BranchResponse & EndpointsResponse & OperationsResponse & RolesResponse & DatabasesResponse & ConnectionUrisOptionalResponse;
};
type CreateProjectBranchResponse = CreateProjectBranchResponses[keyof CreateProjectBranchResponses];
type CreateProjectBranchAnonymizedData = {
  body: BranchAnonymizedCreateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branch_anonymized';
};
type CreateProjectBranchAnonymizedErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectBranchAnonymizedError = CreateProjectBranchAnonymizedErrors[keyof CreateProjectBranchAnonymizedErrors];
type CreateProjectBranchAnonymizedResponses = {
  /**
   * Created a branch. An endpoint is only created if it was specified in the request.
   */
  201: BranchResponse & EndpointsResponse & OperationsResponse & RolesResponse & DatabasesResponse & ConnectionUrisOptionalResponse;
};
type CreateProjectBranchAnonymizedResponse = CreateProjectBranchAnonymizedResponses[keyof CreateProjectBranchAnonymizedResponses];
type CountProjectBranchesData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: {
    /**
     * Count branches matching the `name` in search query
     */
    search?: string;
  };
  url: '/projects/{project_id}/branches/count';
};
type CountProjectBranchesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CountProjectBranchesError = CountProjectBranchesErrors[keyof CountProjectBranchesErrors];
type CountProjectBranchesResponses = {
  /**
   * Returned a count of branches for the specified project
   */
  200: BranchesCountResponse;
};
type CountProjectBranchesResponse = CountProjectBranchesResponses[keyof CountProjectBranchesResponses];
type DeleteProjectBranchData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: {
    /**
     * If true, the branch is permanently deleted immediately without a recovery window.
     * If false (default), the branch can be recovered within 7 days via the recover endpoint.
     *
     * This parameter is part of the Branch Recovery feature, which is in preview and not available to all users.
     *
     */
    hard_delete?: boolean;
  };
  url: '/projects/{project_id}/branches/{branch_id}';
};
type DeleteProjectBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectBranchError = DeleteProjectBranchErrors[keyof DeleteProjectBranchErrors];
type DeleteProjectBranchResponses = {
  /**
   * Deleted the specified branch
   */
  200: BranchOperations;
  /**
   * Returned if the branch doesn't exist or has already been deleted
   */
  204: void;
};
type DeleteProjectBranchResponse = DeleteProjectBranchResponses[keyof DeleteProjectBranchResponses];
type GetProjectBranchData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}';
};
type GetProjectBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchError = GetProjectBranchErrors[keyof GetProjectBranchErrors];
type GetProjectBranchResponses = {
  /**
   * Returned information about the specified branch
   */
  200: BranchResponse & AnnotationResponse;
};
type GetProjectBranchResponse = GetProjectBranchResponses[keyof GetProjectBranchResponses];
type UpdateProjectBranchData = {
  body: BranchUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}';
};
type UpdateProjectBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateProjectBranchError = UpdateProjectBranchErrors[keyof UpdateProjectBranchErrors];
type UpdateProjectBranchResponses = {
  /**
   * Updated the specified branch
   */
  200: BranchOperations;
};
type UpdateProjectBranchResponse = UpdateProjectBranchResponses[keyof UpdateProjectBranchResponses];
type RestoreProjectBranchData = {
  body: BranchRestoreRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/restore';
};
type RestoreProjectBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RestoreProjectBranchError = RestoreProjectBranchErrors[keyof RestoreProjectBranchErrors];
type RestoreProjectBranchResponses = {
  /**
   * Updated the specified branch
   */
  200: BranchOperations;
};
type RestoreProjectBranchResponse = RestoreProjectBranchResponses[keyof RestoreProjectBranchResponses];
type GetProjectBranchSchemaData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query: {
    /**
     * Name of the database for which the schema is retrieved
     */
    db_name: string;
    /**
     * The Log Sequence Number (LSN) for which the schema is retrieved
     *
     */
    lsn?: string;
    /**
     * The point in time for which the schema is retrieved
     *
     */
    timestamp?: string;
    /**
     * The format of the schema to retrieve. Possible values:
     * - `sql` (default)
     * - `json`
     *
     */
    format?: string;
  };
  url: '/projects/{project_id}/branches/{branch_id}/schema';
};
type GetProjectBranchSchemaErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchSchemaError = GetProjectBranchSchemaErrors[keyof GetProjectBranchSchemaErrors];
type GetProjectBranchSchemaResponses = {
  /**
   * Schema definition
   */
  200: BranchSchemaResponse;
};
type GetProjectBranchSchemaResponse = GetProjectBranchSchemaResponses[keyof GetProjectBranchSchemaResponses];
type GetProjectBranchSchemaComparisonData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query: {
    /**
     * The branch ID to compare the schema with
     */
    base_branch_id?: string;
    /**
     * Name of the database for which the schema is retrieved
     */
    db_name: string;
    /**
     * The Log Sequence Number (LSN) for which the schema is retrieved
     *
     */
    lsn?: string;
    /**
     * The point in time for which the schema is retrieved
     *
     */
    timestamp?: string;
    /**
     * The Log Sequence Number (LSN) for the base branch schema
     *
     */
    base_lsn?: string;
    /**
     * The point in time for the base branch schema
     *
     */
    base_timestamp?: string;
  };
  url: '/projects/{project_id}/branches/{branch_id}/compare_schema';
};
type GetProjectBranchSchemaComparisonErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchSchemaComparisonError = GetProjectBranchSchemaComparisonErrors[keyof GetProjectBranchSchemaComparisonErrors];
type GetProjectBranchSchemaComparisonResponses = {
  /**
   * Difference between the schemas
   */
  200: BranchSchemaCompareResponse;
};
type GetProjectBranchSchemaComparisonResponse = GetProjectBranchSchemaComparisonResponses[keyof GetProjectBranchSchemaComparisonResponses];
type GetMaskingRulesData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/masking_rules';
};
type GetMaskingRulesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetMaskingRulesError = GetMaskingRulesErrors[keyof GetMaskingRulesErrors];
type GetMaskingRulesResponses = {
  /**
   * Masking rules retrieved successfully
   */
  200: MaskingRulesResponse;
};
type GetMaskingRulesResponse = GetMaskingRulesResponses[keyof GetMaskingRulesResponses];
type UpdateMaskingRulesData = {
  body: MaskingRulesUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/masking_rules';
};
type UpdateMaskingRulesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateMaskingRulesError = UpdateMaskingRulesErrors[keyof UpdateMaskingRulesErrors];
type UpdateMaskingRulesResponses = {
  /**
   * Masking rules updated successfully
   */
  200: MaskingRulesResponse;
};
type UpdateMaskingRulesResponse = UpdateMaskingRulesResponses[keyof UpdateMaskingRulesResponses];
type GetAnonymizedBranchStatusData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/anonymized_status';
};
type GetAnonymizedBranchStatusErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetAnonymizedBranchStatusError = GetAnonymizedBranchStatusErrors[keyof GetAnonymizedBranchStatusErrors];
type GetAnonymizedBranchStatusResponses = {
  /**
   * Anonymized branch status retrieved successfully
   */
  200: AnonymizedBranchStatusResponse;
};
type GetAnonymizedBranchStatusResponse = GetAnonymizedBranchStatusResponses[keyof GetAnonymizedBranchStatusResponses];
type StartAnonymizationData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/anonymize';
};
type StartAnonymizationErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type StartAnonymizationError = StartAnonymizationErrors[keyof StartAnonymizationErrors];
type StartAnonymizationResponses = {
  /**
   * Anonymization started successfully
   */
  200: AnonymizedBranchStatusResponse;
};
type StartAnonymizationResponse = StartAnonymizationResponses[keyof StartAnonymizationResponses];
type SetDefaultProjectBranchData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/set_as_default';
};
type SetDefaultProjectBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type SetDefaultProjectBranchError = SetDefaultProjectBranchErrors[keyof SetDefaultProjectBranchErrors];
type SetDefaultProjectBranchResponses = {
  /**
   * Updated the specified branch
   */
  200: BranchOperations;
};
type SetDefaultProjectBranchResponse = SetDefaultProjectBranchResponses[keyof SetDefaultProjectBranchResponses];
type FinalizeRestoreBranchData = {
  body?: {
    /**
     * used to rename the existing branch when it is replaced. if omitted, a default name is generated and used
     */
    name?: string;
  };
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/finalize_restore';
};
type FinalizeRestoreBranchErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type FinalizeRestoreBranchError = FinalizeRestoreBranchErrors[keyof FinalizeRestoreBranchErrors];
type FinalizeRestoreBranchResponses = {
  /**
   * OK
   */
  200: OperationsResponse;
};
type FinalizeRestoreBranchResponse = FinalizeRestoreBranchResponses[keyof FinalizeRestoreBranchResponses];
type ListProjectBranchEndpointsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/endpoints';
};
type ListProjectBranchEndpointsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectBranchEndpointsError = ListProjectBranchEndpointsErrors[keyof ListProjectBranchEndpointsErrors];
type ListProjectBranchEndpointsResponses = {
  /**
   * Returned a list of endpoints for the specified branch
   */
  200: EndpointsResponse;
};
type ListProjectBranchEndpointsResponse = ListProjectBranchEndpointsResponses[keyof ListProjectBranchEndpointsResponses];
type ListProjectBranchDatabasesData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/databases';
};
type ListProjectBranchDatabasesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectBranchDatabasesError = ListProjectBranchDatabasesErrors[keyof ListProjectBranchDatabasesErrors];
type ListProjectBranchDatabasesResponses = {
  /**
   * Returned a list of databases of the specified branch
   */
  200: DatabasesResponse;
};
type ListProjectBranchDatabasesResponse = ListProjectBranchDatabasesResponses[keyof ListProjectBranchDatabasesResponses];
type CreateProjectBranchDatabaseData = {
  body: DatabaseCreateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/databases';
};
type CreateProjectBranchDatabaseErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectBranchDatabaseError = CreateProjectBranchDatabaseErrors[keyof CreateProjectBranchDatabaseErrors];
type CreateProjectBranchDatabaseResponses = {
  /**
   * Created a database in the specified branch
   */
  201: DatabaseOperations;
};
type CreateProjectBranchDatabaseResponse = CreateProjectBranchDatabaseResponses[keyof CreateProjectBranchDatabaseResponses];
type DeleteProjectBranchDatabaseData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/databases/{database_name}';
};
type DeleteProjectBranchDatabaseErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectBranchDatabaseError = DeleteProjectBranchDatabaseErrors[keyof DeleteProjectBranchDatabaseErrors];
type DeleteProjectBranchDatabaseResponses = {
  /**
   * Deleted the specified database
   */
  200: DatabaseOperations;
  /**
   * Returned if the database doesn't exist or has already been deleted
   */
  204: void;
};
type DeleteProjectBranchDatabaseResponse = DeleteProjectBranchDatabaseResponses[keyof DeleteProjectBranchDatabaseResponses];
type GetProjectBranchDatabaseData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/databases/{database_name}';
};
type GetProjectBranchDatabaseErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchDatabaseError = GetProjectBranchDatabaseErrors[keyof GetProjectBranchDatabaseErrors];
type GetProjectBranchDatabaseResponses = {
  /**
   * Returned the database details
   */
  200: DatabaseResponse;
};
type GetProjectBranchDatabaseResponse = GetProjectBranchDatabaseResponses[keyof GetProjectBranchDatabaseResponses];
type UpdateProjectBranchDatabaseData = {
  body: DatabaseUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The database name
     */
    database_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/databases/{database_name}';
};
type UpdateProjectBranchDatabaseErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateProjectBranchDatabaseError = UpdateProjectBranchDatabaseErrors[keyof UpdateProjectBranchDatabaseErrors];
type UpdateProjectBranchDatabaseResponses = {
  /**
   * Updated the database
   */
  200: DatabaseOperations;
};
type UpdateProjectBranchDatabaseResponse = UpdateProjectBranchDatabaseResponses[keyof UpdateProjectBranchDatabaseResponses];
type ListProjectBranchRolesData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/roles';
};
type ListProjectBranchRolesErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectBranchRolesError = ListProjectBranchRolesErrors[keyof ListProjectBranchRolesErrors];
type ListProjectBranchRolesResponses = {
  /**
   * Returned a list of roles from the specified branch.
   */
  200: RolesResponse;
};
type ListProjectBranchRolesResponse = ListProjectBranchRolesResponses[keyof ListProjectBranchRolesResponses];
type CreateProjectBranchRoleData = {
  body: RoleCreateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/roles';
};
type CreateProjectBranchRoleErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectBranchRoleError = CreateProjectBranchRoleErrors[keyof CreateProjectBranchRoleErrors];
type CreateProjectBranchRoleResponses = {
  /**
   * Created a role in the specified branch
   */
  201: RoleOperations;
};
type CreateProjectBranchRoleResponse = CreateProjectBranchRoleResponses[keyof CreateProjectBranchRoleResponses];
type DeleteProjectBranchRoleData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The role name
     */
    role_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/roles/{role_name}';
};
type DeleteProjectBranchRoleErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectBranchRoleError = DeleteProjectBranchRoleErrors[keyof DeleteProjectBranchRoleErrors];
type DeleteProjectBranchRoleResponses = {
  /**
   * Deleted the specified role from the branch
   */
  200: RoleOperations;
  /**
   * Returned if the role doesn't exist or has already been deleted
   */
  204: void;
};
type DeleteProjectBranchRoleResponse = DeleteProjectBranchRoleResponses[keyof DeleteProjectBranchRoleResponses];
type GetProjectBranchRoleData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The role name
     */
    role_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/roles/{role_name}';
};
type GetProjectBranchRoleErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchRoleError = GetProjectBranchRoleErrors[keyof GetProjectBranchRoleErrors];
type GetProjectBranchRoleResponses = {
  /**
   * Returned details for the specified role
   */
  200: RoleResponse;
};
type GetProjectBranchRoleResponse = GetProjectBranchRoleResponses[keyof GetProjectBranchRoleResponses];
type GetProjectBranchRolePasswordData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The role name
     */
    role_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/roles/{role_name}/reveal_password';
};
type GetProjectBranchRolePasswordErrors = {
  /**
   * Role not found
   */
  404: GeneralError;
  /**
   * Storing passwords is disabled
   */
  412: GeneralError;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectBranchRolePasswordError = GetProjectBranchRolePasswordErrors[keyof GetProjectBranchRolePasswordErrors];
type GetProjectBranchRolePasswordResponses = {
  /**
   * Returned password for the specified role
   */
  200: RolePasswordResponse;
};
type GetProjectBranchRolePasswordResponse = GetProjectBranchRolePasswordResponses[keyof GetProjectBranchRolePasswordResponses];
type ResetProjectBranchRolePasswordData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
    /**
     * The role name
     */
    role_name: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/roles/{role_name}/reset_password';
};
type ResetProjectBranchRolePasswordErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ResetProjectBranchRolePasswordError = ResetProjectBranchRolePasswordErrors[keyof ResetProjectBranchRolePasswordErrors];
type ResetProjectBranchRolePasswordResponses = {
  /**
   * Reset the password for the specified role
   */
  200: RoleOperations;
};
type ResetProjectBranchRolePasswordResponse = ResetProjectBranchRolePasswordResponses[keyof ResetProjectBranchRolePasswordResponses];
type ListProjectVpcEndpointsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/vpc_endpoints';
};
type ListProjectVpcEndpointsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectVpcEndpointsError = ListProjectVpcEndpointsErrors[keyof ListProjectVpcEndpointsErrors];
type ListProjectVpcEndpointsResponses = {
  /**
   * Returned VPC endpoint restrictions for the specified project
   */
  200: VpcEndpointsResponse;
};
type ListProjectVpcEndpointsResponse = ListProjectVpcEndpointsResponses[keyof ListProjectVpcEndpointsResponses];
type DeleteProjectVpcEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The VPC endpoint ID
     */
    vpc_endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/vpc_endpoints/{vpc_endpoint_id}';
};
type DeleteProjectVpcEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectVpcEndpointError = DeleteProjectVpcEndpointErrors[keyof DeleteProjectVpcEndpointErrors];
type DeleteProjectVpcEndpointResponses = {
  /**
   * Removed the VPC endpoint restriction from the specified Neon project
   */
  200: unknown;
};
type AssignProjectVpcEndpointData = {
  body: VpcEndpointAssignment;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The VPC endpoint ID
     */
    vpc_endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/vpc_endpoints/{vpc_endpoint_id}';
};
type AssignProjectVpcEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AssignProjectVpcEndpointError = AssignProjectVpcEndpointErrors[keyof AssignProjectVpcEndpointErrors];
type AssignProjectVpcEndpointResponses = {
  /**
   * Configured the specified VPC endpoint as a restriction for the specified project.
   */
  200: unknown;
};
type ListProjectEndpointsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints';
};
type ListProjectEndpointsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListProjectEndpointsError = ListProjectEndpointsErrors[keyof ListProjectEndpointsErrors];
type ListProjectEndpointsResponses = {
  /**
   * Returned a list of endpoints for the specified project
   */
  200: EndpointsResponse;
};
type ListProjectEndpointsResponse = ListProjectEndpointsResponses[keyof ListProjectEndpointsResponses];
type CreateProjectEndpointData = {
  body: EndpointCreateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints';
};
type CreateProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateProjectEndpointError = CreateProjectEndpointErrors[keyof CreateProjectEndpointErrors];
type CreateProjectEndpointResponses = {
  /**
   * Created a compute endpoint
   */
  201: EndpointOperations;
};
type CreateProjectEndpointResponse = CreateProjectEndpointResponses[keyof CreateProjectEndpointResponses];
type DeleteProjectEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The endpoint ID
     */
    endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints/{endpoint_id}';
};
type DeleteProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteProjectEndpointError = DeleteProjectEndpointErrors[keyof DeleteProjectEndpointErrors];
type DeleteProjectEndpointResponses = {
  /**
   * Deleted the specified compute endpoint
   */
  200: EndpointOperations;
  /**
   * Returned if the endpoint doesn't exist or has already been deleted
   */
  204: void;
};
type DeleteProjectEndpointResponse = DeleteProjectEndpointResponses[keyof DeleteProjectEndpointResponses];
type GetProjectEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The endpoint ID
     */
    endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints/{endpoint_id}';
};
type GetProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetProjectEndpointError = GetProjectEndpointErrors[keyof GetProjectEndpointErrors];
type GetProjectEndpointResponses = {
  /**
   * Returned information about the specified endpoint
   */
  200: EndpointResponse;
};
type GetProjectEndpointResponse = GetProjectEndpointResponses[keyof GetProjectEndpointResponses];
type UpdateProjectEndpointData = {
  body: EndpointUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The endpoint ID
     */
    endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints/{endpoint_id}';
};
type UpdateProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateProjectEndpointError = UpdateProjectEndpointErrors[keyof UpdateProjectEndpointErrors];
type UpdateProjectEndpointResponses = {
  /**
   * Updated the specified compute endpoint
   */
  200: EndpointOperations;
};
type UpdateProjectEndpointResponse = UpdateProjectEndpointResponses[keyof UpdateProjectEndpointResponses];
type StartProjectEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The endpoint ID
     */
    endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints/{endpoint_id}/start';
};
type StartProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type StartProjectEndpointError = StartProjectEndpointErrors[keyof StartProjectEndpointErrors];
type StartProjectEndpointResponses = {
  /**
   * Started the specified compute endpoint
   */
  200: EndpointOperations;
};
type StartProjectEndpointResponse = StartProjectEndpointResponses[keyof StartProjectEndpointResponses];
type SuspendProjectEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The endpoint ID
     */
    endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints/{endpoint_id}/suspend';
};
type SuspendProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type SuspendProjectEndpointError = SuspendProjectEndpointErrors[keyof SuspendProjectEndpointErrors];
type SuspendProjectEndpointResponses = {
  /**
   * Suspended the specified endpoint
   */
  200: EndpointOperations;
};
type SuspendProjectEndpointResponse = SuspendProjectEndpointResponses[keyof SuspendProjectEndpointResponses];
type RestartProjectEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The endpoint ID
     */
    endpoint_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/endpoints/{endpoint_id}/restart';
};
type RestartProjectEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RestartProjectEndpointError = RestartProjectEndpointErrors[keyof RestartProjectEndpointErrors];
type RestartProjectEndpointResponses = {
  /**
   * Restarted endpoint
   */
  200: EndpointOperations;
};
type RestartProjectEndpointResponse = RestartProjectEndpointResponses[keyof RestartProjectEndpointResponses];
type GetConsumptionHistoryPerProjectData = {
  body?: never;
  path?: never;
  query: {
    /**
     * Specify the cursor value from the previous response to get the next batch of projects.
     */
    cursor?: string;
    /**
     * Specify a value from 1 to 100 to limit number of projects in the response.
     */
    limit?: number;
    /**
     * Specify a list of project IDs to filter the response.
     * If omitted, the response will contain all projects.
     * A list of project IDs can be specified as an array of parameter values or as a comma-separated list in a single parameter value.
     * - As an array of parameter values: `project_ids=cold-poetry-09157238%20&project_ids=quiet-snow-71788278`
     * - As a comma-separated list in a single parameter value: `project_ids=cold-poetry-09157238,quiet-snow-71788278`
     *
     */
    project_ids?: Array<string>;
    /**
     * Specify the start `date-time` for the consumption period.
     * The `date-time` value is rounded according to the specified `granularity`.
     * For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`.
     * The specified `date-time` value must respect the specified `granularity`:
     * - For `hourly`, consumption metrics are limited to the last 168 hours.
     * - For `daily`, consumption metrics are limited to the last 60 days.
     * - For `monthly`, consumption metrics are limited to the last year.
     *
     * The consumption history is available starting from `March 1, 2024, at 00:00:00 UTC`.
     *
     */
    from: string;
    /**
     * Specify the end `date-time` for the consumption period.
     * The `date-time` value is rounded according to the specified granularity.
     * For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`.
     * The specified `date-time` value must respect the specified `granularity`:
     * - For `hourly`, consumption metrics are limited to the last 168 hours.
     * - For `daily`, consumption metrics are limited to the last 60 days.
     * - For `monthly`, consumption metrics are limited to the last year.
     *
     */
    to: string;
    /**
     * Specify the granularity of consumption metrics.
     * Hourly, daily, and monthly metrics are available for the last 168 hours, 60 days,
     * and 1 year, respectively.
     *
     */
    granularity: ConsumptionHistoryGranularity;
    /**
     * Specify the organization for which the project consumption metrics should be returned.
     * If this parameter is not provided, the endpoint will return the metrics for the
     * authenticated user's projects.
     *
     */
    org_id?: string;
    /**
     * The field is deprecated. Please use `metrics` instead.
     * If `metrics` is specified, this field is ignored.
     * Include metrics utilized in previous pricing models.
     * - **data_storage_bytes_hour**: The sum of the maximum observed storage values for each hour,
     * which never decreases.
     *
     *
     * @deprecated
     */
    include_v1_metrics?: boolean;
    /**
     * Specify a list of metrics to include in the response.
     * If omitted, active_time, compute_time, written_data, synthetic_storage_size are returned.
     * Possible values:
     * - `active_time_seconds`
     * - `compute_time_seconds`
     * - `written_data_bytes`
     * - `synthetic_storage_size_bytes`
     * - `data_storage_bytes_hour`
     * - `logical_size_bytes`
     * - `logical_size_bytes_hour`
     *
     * A list of metrics can be specified as an array of parameter values or as a comma-separated list in a single parameter value.
     * - As an array of parameter values: `metrics=cpu_seconds&metrics=ram_bytes`
     * - As a comma-separated list in a single parameter value: `metrics=cpu_seconds,ram_bytes`
     *
     */
    metrics?: ConsumptionHistoryQueryMetrics;
  };
  url: '/consumption_history/projects';
};
type GetConsumptionHistoryPerProjectErrors = {
  /**
   * This endpoint is not available. It is only supported with Scale, Business, and Enterprise plan accounts.
   */
  403: GeneralError;
  /**
   * Account is not a member of the organization specified by `org_id`.
   */
  404: GeneralError;
  /**
   * The specified `date-time` range is outside the boundaries of the specified `granularity`.
   * Adjust your `from` and `to` values or select a different `granularity`.
   *
   */
  406: GeneralError;
  /**
   * Too many requests
   */
  429: GeneralError;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetConsumptionHistoryPerProjectError = GetConsumptionHistoryPerProjectErrors[keyof GetConsumptionHistoryPerProjectErrors];
type GetConsumptionHistoryPerProjectResponses = {
  /**
   * Returned project consumption metrics for the Neon account
   */
  200: ConsumptionHistoryPerProjectResponse & PaginationResponse;
};
type GetConsumptionHistoryPerProjectResponse = GetConsumptionHistoryPerProjectResponses[keyof GetConsumptionHistoryPerProjectResponses];
type GetConsumptionHistoryPerProjectV2Data = {
  body?: never;
  path?: never;
  query: {
    /**
     * Cursor from the previous response (`pagination.cursor`). Pass it to fetch the next page
     * of projects. Pages are ordered by project creation order (newest first).
     *
     */
    cursor?: string;
    /**
     * Maximum number of projects per page. Allowed range: 1 to 100. Default: 10.
     *
     */
    limit?: number;
    /**
     * Optional project IDs to filter the response (up to 100). If omitted, projects in the
     * organization are included across pages (use `cursor` and `limit`).
     *
     * Pass multiple IDs as repeated query parameters or a comma-separated list:
     * - `project_ids=cold-poetry-09157238&project_ids=quiet-snow-71788278`
     * - `project_ids=cold-poetry-09157238,quiet-snow-71788278`
     *
     */
    project_ids?: Array<string>;
    /**
     * Specify the start `date-time` for the consumption period.
     * The `date-time` value is rounded according to the specified `granularity`.
     * For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`.
     * The specified `date-time` value must respect the specified `granularity`:
     * - For `hourly`, consumption metrics are limited to the last 168 hours.
     * - For `daily`, consumption metrics are limited to the last 60 days.
     * - For `monthly`, consumption metrics are limited to the last year.
     *
     * The earliest allowed `from` value is `March 1, 2024, at 00:00:00 UTC`.
     * Metrics are returned from when the account upgraded to an eligible plan, which may be
     * later than that date.
     *
     */
    from: string;
    /**
     * Specify the end `date-time` for the consumption period.
     * The `date-time` value is rounded according to the specified `granularity`.
     * For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`.
     * The specified `date-time` value must respect the specified `granularity`:
     * - For `hourly`, consumption metrics are limited to the last 168 hours.
     * - For `daily`, consumption metrics are limited to the last 60 days.
     * - For `monthly`, consumption metrics are limited to the last year.
     *
     */
    to: string;
    /**
     * Specify the granularity of consumption metrics.
     * Hourly, daily, and monthly metrics are available for the last 168 hours, 60 days,
     * and 1 year, respectively.
     *
     */
    granularity: ConsumptionHistoryGranularity;
    /**
     * Organization ID. Metrics are returned for projects in this organization.
     *
     */
    org_id: string;
    /**
     * Required. List the metrics to return. Supported values:
     * - `compute_unit_seconds`
     * - `root_branch_bytes_month`
     * - `child_branch_bytes_month`
     * - `instant_restore_bytes_month`
     * - `public_network_transfer_bytes`
     * - `private_network_transfer_bytes`
     * - `extra_branches_month`
     * - `snapshot_storage_bytes_month`
     *
     * Pass multiple values as repeated query parameters or a comma-separated list:
     * - `metrics=compute_unit_seconds&metrics=extra_branches_month`
     * - `metrics=compute_unit_seconds,extra_branches_month`
     *
     */
    metrics: ConsumptionHistoryQueryMetrics;
  };
  url: '/consumption_history/v2/projects';
};
type GetConsumptionHistoryPerProjectV2Errors = {
  /**
   * Not available for this account. Project consumption history requires a Launch, Scale,
   * Agent, Business, or Enterprise plan.
   *
   */
  403: GeneralError;
  /**
   * Account is not a member of the organization specified by `org_id`.
   */
  404: GeneralError;
  /**
   * The `from` and `to` range is not valid for the selected `granularity`. Adjust the range or
   * choose a different granularity.
   *
   */
  406: GeneralError;
  /**
   * Too many requests
   */
  429: GeneralError;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetConsumptionHistoryPerProjectV2Error = GetConsumptionHistoryPerProjectV2Errors[keyof GetConsumptionHistoryPerProjectV2Errors];
type GetConsumptionHistoryPerProjectV2Responses = {
  /**
   * Project consumption metrics for the Neon account.
   */
  200: ConsumptionHistoryPerProjectV2Response & PaginationResponse;
};
type GetConsumptionHistoryPerProjectV2Response = GetConsumptionHistoryPerProjectV2Responses[keyof GetConsumptionHistoryPerProjectV2Responses];
type GetConsumptionHistoryPerBranchV2Data = {
  body?: never;
  path?: never;
  query: {
    /**
     * Cursor from the previous response (`pagination.cursor`). Pass it to fetch the next page
     * of branches. Pages are ordered by project ID, then branch ID.
     *
     */
    cursor?: string;
    /**
     * Maximum number of branches per page. Allowed range: 1 to 1000. Default: 100.
     *
     */
    limit?: number;
    /**
     * Project IDs to include (required, 1 to 100). Returns metrics for branches in these projects.
     *
     * Pass multiple IDs as repeated query parameters or a comma-separated list:
     * - `project_ids=cold-poetry-09157238&project_ids=quiet-snow-71788278`
     * - `project_ids=cold-poetry-09157238,quiet-snow-71788278`
     *
     */
    project_ids: Array<string>;
    /**
     * Optional branch IDs to filter the response (up to 100). If omitted, all branches in the
     * listed projects are included.
     *
     * Pass multiple IDs as repeated query parameters or a comma-separated list:
     * - `branch_ids=br-aged-salad-637688&branch_ids=br-sweet-breeze-497520`
     * - `branch_ids=br-aged-salad-637688,br-sweet-breeze-497520`
     *
     */
    branch_ids?: Array<string>;
    /**
     * Specify the start `date-time` for the consumption period.
     * The `date-time` value is rounded according to the specified `granularity`.
     * For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`.
     * The specified `date-time` value must respect the specified `granularity`:
     * - For `hourly`, consumption metrics are limited to the last 168 hours.
     * - For `daily`, consumption metrics are limited to the last 60 days.
     * - For `monthly`, consumption metrics are limited to the last year.
     *
     * Branch-level metrics are returned from when the account first ingests branch-level
     * consumption data. Periods before that time contain no branch metrics.
     *
     */
    from: string;
    /**
     * Specify the end `date-time` for the consumption period.
     * The `date-time` value is rounded according to the specified `granularity`.
     * For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`.
     * The specified `date-time` value must respect the specified `granularity`:
     * - For `hourly`, consumption metrics are limited to the last 168 hours.
     * - For `daily`, consumption metrics are limited to the last 60 days.
     * - For `monthly`, consumption metrics are limited to the last year.
     *
     */
    to: string;
    /**
     * Specify the granularity of consumption metrics.
     * Hourly, daily, and monthly metrics are available for the last 168 hours, 60 days,
     * and 1 year, respectively.
     *
     */
    granularity: ConsumptionHistoryGranularity;
    /**
     * Organization ID. Metrics are returned for projects in this organization.
     *
     */
    org_id: string;
    /**
     * Required. List the metrics to return. Only these values are supported:
     * - `compute_unit_seconds`
     * - `root_branch_bytes_month`
     * - `child_branch_bytes_month`
     * - `instant_restore_bytes_month`
     * - `public_network_transfer_bytes`
     * - `private_network_transfer_bytes`
     *
     * Not supported on this endpoint: `extra_branches_month`, `snapshot_storage_bytes_month`.
     * Use `GET /consumption_history/v2/projects` for those.
     *
     * Pass multiple values as repeated query parameters or a comma-separated list:
     * - `metrics=compute_unit_seconds&metrics=public_network_transfer_bytes`
     * - `metrics=compute_unit_seconds,public_network_transfer_bytes`
     *
     */
    metrics: ConsumptionHistoryQueryMetrics;
  };
  url: '/consumption_history/v2/branches';
};
type GetConsumptionHistoryPerBranchV2Errors = {
  /**
   * Not available for this account. Branch consumption history requires a paid usage-based
   * Launch, Scale, Agent, or Enterprise plan.
   *
   */
  403: GeneralError;
  /**
   * Account is not a member of the organization specified by `org_id`.
   */
  404: GeneralError;
  /**
   * The `from` and `to` range is not valid for the selected `granularity`. Adjust the range or
   * choose a different granularity.
   *
   */
  406: GeneralError;
  /**
   * Too many requests
   */
  429: GeneralError;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetConsumptionHistoryPerBranchV2Error = GetConsumptionHistoryPerBranchV2Errors[keyof GetConsumptionHistoryPerBranchV2Errors];
type GetConsumptionHistoryPerBranchV2Responses = {
  /**
   * Branch consumption metrics for the Neon account.
   */
  200: ConsumptionHistoryPerBranchV2Response & PaginationResponse;
};
type GetConsumptionHistoryPerBranchV2Response = GetConsumptionHistoryPerBranchV2Responses[keyof GetConsumptionHistoryPerBranchV2Responses];
type GetOrganizationData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}';
};
type GetOrganizationErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetOrganizationError = GetOrganizationErrors[keyof GetOrganizationErrors];
type GetOrganizationResponses = {
  /**
   * Returned information about the organization
   */
  200: Organization;
};
type GetOrganizationResponse = GetOrganizationResponses[keyof GetOrganizationResponses];
type ListOrgApiKeysData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/api_keys';
};
type ListOrgApiKeysErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListOrgApiKeysError = ListOrgApiKeysErrors[keyof ListOrgApiKeysErrors];
type ListOrgApiKeysResponses = {
  /**
   * Returned the API keys for the specified organization
   */
  200: Array<OrgApiKeysListResponseItem>;
};
type ListOrgApiKeysResponse = ListOrgApiKeysResponses[keyof ListOrgApiKeysResponses];
type CreateOrgApiKeyData = {
  body: OrgApiKeyCreateRequest;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/api_keys';
};
type CreateOrgApiKeyErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateOrgApiKeyError = CreateOrgApiKeyErrors[keyof CreateOrgApiKeyErrors];
type CreateOrgApiKeyResponses = {
  /**
   * Created an organization API key
   */
  200: OrgApiKeyCreateResponse;
};
type CreateOrgApiKeyResponse = CreateOrgApiKeyResponses[keyof CreateOrgApiKeyResponses];
type RevokeOrgApiKeyData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The API key ID
     */
    key_id: number;
  };
  query?: never;
  url: '/organizations/{org_id}/api_keys/{key_id}';
};
type RevokeOrgApiKeyErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RevokeOrgApiKeyError = RevokeOrgApiKeyErrors[keyof RevokeOrgApiKeyErrors];
type RevokeOrgApiKeyResponses = {
  /**
   * Revoked the specified organization API key
   */
  200: OrgApiKeyRevokeResponse;
};
type RevokeOrgApiKeyResponse = RevokeOrgApiKeyResponses[keyof RevokeOrgApiKeyResponses];
type DeleteOrganizationSpendingLimitData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/billing/spending_limit';
};
type DeleteOrganizationSpendingLimitErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteOrganizationSpendingLimitError = DeleteOrganizationSpendingLimitErrors[keyof DeleteOrganizationSpendingLimitErrors];
type DeleteOrganizationSpendingLimitResponses = {
  /**
   * The spending limit has been cleared.
   */
  200: EmptyResponse;
};
type DeleteOrganizationSpendingLimitResponse = DeleteOrganizationSpendingLimitResponses[keyof DeleteOrganizationSpendingLimitResponses];
type GetOrganizationSpendingLimitData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/billing/spending_limit';
};
type GetOrganizationSpendingLimitErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetOrganizationSpendingLimitError = GetOrganizationSpendingLimitErrors[keyof GetOrganizationSpendingLimitErrors];
type GetOrganizationSpendingLimitResponses = {
  /**
   * The organization's current spending limit.
   */
  200: SpendingLimitResponse;
};
type GetOrganizationSpendingLimitResponse = GetOrganizationSpendingLimitResponses[keyof GetOrganizationSpendingLimitResponses];
type SetOrganizationSpendingLimitData = {
  body: SpendingLimitUpdateRequest;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/billing/spending_limit';
};
type SetOrganizationSpendingLimitErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type SetOrganizationSpendingLimitError = SetOrganizationSpendingLimitErrors[keyof SetOrganizationSpendingLimitErrors];
type SetOrganizationSpendingLimitResponses = {
  /**
   * The updated spending limit value.
   */
  200: SpendingLimitResponse;
};
type SetOrganizationSpendingLimitResponse = SetOrganizationSpendingLimitResponses[keyof SetOrganizationSpendingLimitResponses];
type GetOrganizationMembersData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: {
    /**
     * Sort the members by the specified field. Defaults to `joined_at`.
     */
    sort_by?: 'email' | 'role' | 'joined_at';
    /**
     * A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list.
     */
    cursor?: string;
    /**
     * Defines the sorting order of entities.
     */
    sort_order?: 'asc' | 'desc';
    /**
     * The maximum number of members to return in the response
     */
    limit?: number;
  };
  url: '/organizations/{org_id}/members';
};
type GetOrganizationMembersErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetOrganizationMembersError = GetOrganizationMembersErrors[keyof GetOrganizationMembersErrors];
type GetOrganizationMembersResponses = {
  /**
   * Returned information about organization members
   */
  200: OrganizationMembersResponse & CursorPaginationResponse;
};
type GetOrganizationMembersResponse = GetOrganizationMembersResponses[keyof GetOrganizationMembersResponses];
type RemoveOrganizationMemberData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon organization member ID
     */
    member_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/members/{member_id}';
};
type RemoveOrganizationMemberErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RemoveOrganizationMemberError = RemoveOrganizationMemberErrors[keyof RemoveOrganizationMemberErrors];
type RemoveOrganizationMemberResponses = {
  /**
   * Removed organization member
   */
  200: EmptyResponse;
};
type RemoveOrganizationMemberResponse = RemoveOrganizationMemberResponses[keyof RemoveOrganizationMemberResponses];
type GetOrganizationMemberData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon organization member ID
     */
    member_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/members/{member_id}';
};
type GetOrganizationMemberErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetOrganizationMemberError = GetOrganizationMemberErrors[keyof GetOrganizationMemberErrors];
type GetOrganizationMemberResponses = {
  /**
   * Returned information about the organization member
   */
  200: Member;
};
type GetOrganizationMemberResponse = GetOrganizationMemberResponses[keyof GetOrganizationMemberResponses];
type UpdateOrganizationMemberData = {
  body: OrganizationMemberUpdateRequest;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon organization member ID
     */
    member_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/members/{member_id}';
};
type UpdateOrganizationMemberErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateOrganizationMemberError = UpdateOrganizationMemberErrors[keyof UpdateOrganizationMemberErrors];
type UpdateOrganizationMemberResponses = {
  /**
   * The updated organization member
   */
  200: Member;
};
type UpdateOrganizationMemberResponse = UpdateOrganizationMemberResponses[keyof UpdateOrganizationMemberResponses];
type GetOrganizationInvitationsData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/invitations';
};
type GetOrganizationInvitationsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetOrganizationInvitationsError = GetOrganizationInvitationsErrors[keyof GetOrganizationInvitationsErrors];
type GetOrganizationInvitationsResponses = {
  /**
   * Returned information about the organization invitations
   */
  200: OrganizationInvitationsResponse;
};
type GetOrganizationInvitationsResponse = GetOrganizationInvitationsResponses[keyof GetOrganizationInvitationsResponses];
type CreateOrganizationInvitationsData = {
  body: OrganizationInvitesCreateRequest;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/invitations';
};
type CreateOrganizationInvitationsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateOrganizationInvitationsError = CreateOrganizationInvitationsErrors[keyof CreateOrganizationInvitationsErrors];
type CreateOrganizationInvitationsResponses = {
  /**
   * The created organization invitation
   */
  200: OrganizationInvitationsResponse;
};
type CreateOrganizationInvitationsResponse = CreateOrganizationInvitationsResponses[keyof CreateOrganizationInvitationsResponses];
type TransferProjectsFromOrgToOrgData = {
  body: TransferProjectsToOrganizationRequest;
  path: {
    /**
     * The Neon organization ID (source org, which currently owns the project)
     */
    source_org_id: string;
  };
  query?: never;
  url: '/organizations/{source_org_id}/projects/transfer';
};
type TransferProjectsFromOrgToOrgErrors = {
  /**
   * Transfer failed - the target organization has too many projects or its plan is incompatible with the source organization. Reduce projects or upgrade the organization.
   */
  406: LimitsUnsatisfiedResponse;
  /**
   * One or more of the provided project IDs have GitHub or Vercel integrations installed. Transferring integration projects is currently not supported
   */
  422: ProjectsWithIntegrationResponse;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type TransferProjectsFromOrgToOrgError = TransferProjectsFromOrgToOrgErrors[keyof TransferProjectsFromOrgToOrgErrors];
type TransferProjectsFromOrgToOrgResponses = {
  /**
   * Projects successfully transferred from organization to organization
   */
  200: EmptyResponse;
};
type TransferProjectsFromOrgToOrgResponse = TransferProjectsFromOrgToOrgResponses[keyof TransferProjectsFromOrgToOrgResponses];
type ListOrganizationVpcEndpointsAllRegionsData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/vpc/vpc_endpoints';
};
type ListOrganizationVpcEndpointsAllRegionsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListOrganizationVpcEndpointsAllRegionsError = ListOrganizationVpcEndpointsAllRegionsErrors[keyof ListOrganizationVpcEndpointsAllRegionsErrors];
type ListOrganizationVpcEndpointsAllRegionsResponses = {
  /**
   * The list of configured VPC endpoint IDs for the specified organization across all regions
   */
  200: VpcEndpointsWithRegionResponse;
};
type ListOrganizationVpcEndpointsAllRegionsResponse = ListOrganizationVpcEndpointsAllRegionsResponses[keyof ListOrganizationVpcEndpointsAllRegionsResponses];
type ListOrganizationVpcEndpointsData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon region ID
     */
    region_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints';
};
type ListOrganizationVpcEndpointsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListOrganizationVpcEndpointsError = ListOrganizationVpcEndpointsErrors[keyof ListOrganizationVpcEndpointsErrors];
type ListOrganizationVpcEndpointsResponses = {
  /**
   * The list of configured VPC endpoint IDs for the specified organization
   */
  200: VpcEndpointsResponse;
};
type ListOrganizationVpcEndpointsResponse = ListOrganizationVpcEndpointsResponses[keyof ListOrganizationVpcEndpointsResponses];
type DeleteOrganizationVpcEndpointData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon region ID.
     * Azure regions are currently not supported.
     *
     */
    region_id: string;
    /**
     * The VPC endpoint ID
     */
    vpc_endpoint_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints/{vpc_endpoint_id}';
};
type DeleteOrganizationVpcEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteOrganizationVpcEndpointError = DeleteOrganizationVpcEndpointErrors[keyof DeleteOrganizationVpcEndpointErrors];
type DeleteOrganizationVpcEndpointResponses = {
  /**
   * Deleted the VPC endpoint from the specified Neon organization
   */
  200: unknown;
};
type GetOrganizationVpcEndpointDetailsData = {
  body?: never;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon region ID.
     * Azure regions are currently not supported.
     *
     */
    region_id: string;
    /**
     * The VPC endpoint ID
     */
    vpc_endpoint_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints/{vpc_endpoint_id}';
};
type GetOrganizationVpcEndpointDetailsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetOrganizationVpcEndpointDetailsError = GetOrganizationVpcEndpointDetailsErrors[keyof GetOrganizationVpcEndpointDetailsErrors];
type GetOrganizationVpcEndpointDetailsResponses = {
  /**
   * Returned the current status and configuration details of the specified VPC endpoint.
   */
  200: VpcEndpointDetails;
};
type GetOrganizationVpcEndpointDetailsResponse = GetOrganizationVpcEndpointDetailsResponses[keyof GetOrganizationVpcEndpointDetailsResponses];
type AssignOrganizationVpcEndpointData = {
  body: VpcEndpointAssignment;
  path: {
    /**
     * The Neon organization ID
     */
    org_id: string;
    /**
     * The Neon region ID.
     * Azure regions are currently not supported.
     *
     */
    region_id: string;
    /**
     * The VPC endpoint ID
     */
    vpc_endpoint_id: string;
  };
  query?: never;
  url: '/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints/{vpc_endpoint_id}';
};
type AssignOrganizationVpcEndpointErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type AssignOrganizationVpcEndpointError = AssignOrganizationVpcEndpointErrors[keyof AssignOrganizationVpcEndpointErrors];
type AssignOrganizationVpcEndpointResponses = {
  /**
   * Assigned the VPC endpoint to the specified Neon organization
   */
  200: unknown;
};
type GetActiveRegionsData = {
  body?: never;
  path?: never;
  query?: {
    /**
     * Organization ID. When provided, returns only regions available to this organization.
     * Recommended for accurate region availability.
     *
     */
    org_id?: string;
  };
  url: '/regions';
};
type GetActiveRegionsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetActiveRegionsError = GetActiveRegionsErrors[keyof GetActiveRegionsErrors];
type GetActiveRegionsResponses = {
  /**
   * The list of active regions
   */
  200: ActiveRegionsResponse;
};
type GetActiveRegionsResponse = GetActiveRegionsResponses[keyof GetActiveRegionsResponses];
type GetCurrentUserInfoData = {
  body?: never;
  path?: never;
  query?: never;
  url: '/users/me';
};
type GetCurrentUserInfoErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetCurrentUserInfoError = GetCurrentUserInfoErrors[keyof GetCurrentUserInfoErrors];
type GetCurrentUserInfoResponses = {
  /**
   * Returned information about the current user
   *
   */
  200: CurrentUserInfoResponse;
};
type GetCurrentUserInfoResponse = GetCurrentUserInfoResponses[keyof GetCurrentUserInfoResponses];
type GetCurrentUserOrganizationsData = {
  body?: never;
  path?: never;
  query?: never;
  url: '/users/me/organizations';
};
type GetCurrentUserOrganizationsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetCurrentUserOrganizationsError = GetCurrentUserOrganizationsErrors[keyof GetCurrentUserOrganizationsErrors];
type GetCurrentUserOrganizationsResponses = {
  /**
   * Returned information about the current user organizations
   *
   */
  200: OrganizationsResponse;
};
type GetCurrentUserOrganizationsResponse = GetCurrentUserOrganizationsResponses[keyof GetCurrentUserOrganizationsResponses];
type TransferProjectsFromUserToOrgData = {
  body: TransferProjectsToOrganizationRequest;
  path?: never;
  query?: never;
  url: '/users/me/projects/transfer';
};
type TransferProjectsFromUserToOrgErrors = {
  /**
   * Transfer failed - the target organization has too many projects or its plan is incompatible with the source account. Reduce the number of projects or upgrade the target organization to increase its capacity.
   */
  406: LimitsUnsatisfiedResponse;
  /**
   * One or more of the provided project IDs have GitHub or Vercel integrations installed. Transferring integration projects is currently not supported
   */
  422: ProjectsWithIntegrationResponse;
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type TransferProjectsFromUserToOrgError = TransferProjectsFromUserToOrgErrors[keyof TransferProjectsFromUserToOrgErrors];
type TransferProjectsFromUserToOrgResponses = {
  /**
   * Projects successfully transferred from personal account to organization
   */
  200: EmptyResponse;
};
type TransferProjectsFromUserToOrgResponse = TransferProjectsFromUserToOrgResponses[keyof TransferProjectsFromUserToOrgResponses];
type GetAuthDetailsData = {
  body?: never;
  path?: never;
  query?: never;
  url: '/auth';
};
type GetAuthDetailsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetAuthDetailsError = GetAuthDetailsErrors[keyof GetAuthDetailsErrors];
type GetAuthDetailsResponses = {
  /**
   * Returned auth information about the current auth entity
   *
   */
  200: AuthDetailsResponse;
};
type GetAuthDetailsResponse = GetAuthDetailsResponses[keyof GetAuthDetailsResponses];
type CreateSnapshotData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: {
    /**
     * The target Log Sequence Number (LSN) to take the snapshot from.
     * Must fall within the restore window. Cannot be used with `timestamp`
     *
     */
    lsn?: string;
    /**
     * The target timestamp for the snapshot. Must fall within the restore window.
     * Use ISO 8601 format (e.g. 2025-08-05T22:00:00Z). Cannot be used with `lsn`.
     *
     */
    timestamp?: string;
    /**
     * A name for the snapshot.
     */
    name?: string;
    /**
     * The time at which the snapshot will be automatically deleted.
     * Use ISO 8601 format (e.g. 2025-08-05T22:00:00Z).
     *
     */
    expires_at?: string;
  };
  url: '/projects/{project_id}/branches/{branch_id}/snapshot';
};
type CreateSnapshotErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type CreateSnapshotError = CreateSnapshotErrors[keyof CreateSnapshotErrors];
type CreateSnapshotResponses = {
  /**
   * Successfully created the snapshot
   */
  200: {
    snapshot: Snapshot;
    operations: Array<Operation>;
  };
};
type CreateSnapshotResponse = CreateSnapshotResponses[keyof CreateSnapshotResponses];
type ListSnapshotsData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/snapshots';
};
type ListSnapshotsErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type ListSnapshotsError = ListSnapshotsErrors[keyof ListSnapshotsErrors];
type ListSnapshotsResponses = {
  /**
   * Projects snapshots
   */
  200: {
    snapshots: Array<Snapshot>;
  };
};
type ListSnapshotsResponse = ListSnapshotsResponses[keyof ListSnapshotsResponses];
type DeleteSnapshotData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The snapshot ID
     */
    snapshot_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/snapshots/{snapshot_id}';
};
type DeleteSnapshotErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type DeleteSnapshotError = DeleteSnapshotErrors[keyof DeleteSnapshotErrors];
type DeleteSnapshotResponses = {
  /**
   * OK
   */
  202: OperationsResponse;
};
type DeleteSnapshotResponse = DeleteSnapshotResponses[keyof DeleteSnapshotResponses];
type UpdateSnapshotData = {
  body: SnapshotUpdateRequest;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The snapshot ID
     */
    snapshot_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/snapshots/{snapshot_id}';
};
type UpdateSnapshotErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type UpdateSnapshotError = UpdateSnapshotErrors[keyof UpdateSnapshotErrors];
type UpdateSnapshotResponses = {
  /**
   * Successfully updated the snapshot
   */
  200: {
    snapshot: Snapshot;
  };
};
type UpdateSnapshotResponse = UpdateSnapshotResponses[keyof UpdateSnapshotResponses];
type RestoreSnapshotData = {
  body?: {
    /**
     * A name for the newly restored branch.
     * If omitted, a default name will be generated.
     *
     */
    name?: string;
    /**
     * The ID of the branch to restore the snapshot into.
     * If not specified, the branch from which the snapshot was originally
     * created (`snapshot.source_branch_id`) will be used.
     *
     */
    target_branch_id?: string;
    /**
     * Set to `true` to finalize the restore operation immediately.
     * This will complete the restore and move any associated computes to the new branch,
     * similar to the `finalizeRestoreBranch` operation.
     * Defaults to `false` to allow previewing the restored snapshot data first.
     *
     */
    finalize_restore?: boolean;
  };
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The snapshot ID
     */
    snapshot_id: string;
  };
  query?: {
    /**
     * DEPRECATED. Use the `name` field in the request body instead.
     * A name for the newly restored branch. If omitted, a default name will be generated.
     *
     *
     * @deprecated
     */
    name?: string;
  };
  url: '/projects/{project_id}/snapshots/{snapshot_id}/restore';
};
type RestoreSnapshotErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type RestoreSnapshotError = RestoreSnapshotErrors[keyof RestoreSnapshotErrors];
type RestoreSnapshotResponses = {
  /**
   * Branch restored from snapshot and its operations.
   */
  200: BranchResponse & EndpointsOptionalResponse & OperationsResponse;
};
type RestoreSnapshotResponse = RestoreSnapshotResponses[keyof RestoreSnapshotResponses];
type GetSnapshotScheduleData = {
  body?: never;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/backup_schedule';
};
type GetSnapshotScheduleErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type GetSnapshotScheduleError = GetSnapshotScheduleErrors[keyof GetSnapshotScheduleErrors];
type GetSnapshotScheduleResponses = {
  /**
   * Schedule of frequencies to create snapshots
   */
  200: BackupSchedule;
};
type GetSnapshotScheduleResponse = GetSnapshotScheduleResponses[keyof GetSnapshotScheduleResponses];
type SetSnapshotScheduleData = {
  body: BackupSchedule;
  path: {
    /**
     * The Neon project ID
     */
    project_id: string;
    /**
     * The branch ID
     */
    branch_id: string;
  };
  query?: never;
  url: '/projects/{project_id}/branches/{branch_id}/backup_schedule';
};
type SetSnapshotScheduleErrors = {
  /**
   * General Error.
   *
   * The request may or may not be safe to retry, depending on the HTTP method, response status code,
   * and whether a response was received.
   *
   * - If no response is returned from the API, a network error or timeout likely occurred.
   * - In some cases, the request may have reached the server and been successfully processed, but the response failed to reach the client. As a result, retrying non-idempotent requests can lead to unintended results.
   *
   * The following HTTP methods are considered non-idempotent: `POST`, `PATCH`, `DELETE`, and `PUT`. Retrying these methods is generally **not safe**.
   * The following methods are considered idempotent: `GET`, `HEAD`, and `OPTIONS`. Retrying these methods is **safe** in the event of a network error or timeout.
   *
   * Any request that returns a `503 Service Unavailable` response is always safe to retry.
   *
   * Any request that returns a `423 Locked` response is safe to retry. `423 Locked` indicates that the resource is temporarily locked, for example, due to another operation in progress.
   *
   */
  '4XX': GeneralError;
};
type SetSnapshotScheduleError = SetSnapshotScheduleErrors[keyof SetSnapshotScheduleErrors];
type SetSnapshotScheduleResponses = {
  /**
   * Empty response
   */
  200: EmptyResponse;
};
type SetSnapshotScheduleResponse = SetSnapshotScheduleResponses[keyof SetSnapshotScheduleResponses];
//#endregion
export { AcceptProjectTransferRequestData, AcceptProjectTransferRequestError, AcceptProjectTransferRequestErrors, AcceptProjectTransferRequestResponse, AcceptProjectTransferRequestResponses, AcceptProjectTransferRequestSatisfiesPlanError, ActiveRegionsResponse, AddBranchNeonAuthOauthProviderData, AddBranchNeonAuthOauthProviderError, AddBranchNeonAuthOauthProviderErrors, AddBranchNeonAuthOauthProviderResponse, AddBranchNeonAuthOauthProviderResponses, AddBranchNeonAuthTrustedDomainData, AddBranchNeonAuthTrustedDomainError, AddBranchNeonAuthTrustedDomainErrors, AddBranchNeonAuthTrustedDomainResponses, AddNeonAuthDomainToRedirectUriWhitelistData, AddNeonAuthDomainToRedirectUriWhitelistError, AddNeonAuthDomainToRedirectUriWhitelistErrors, AddNeonAuthDomainToRedirectUriWhitelistResponses, AddNeonAuthOauthProviderData, AddNeonAuthOauthProviderError, AddNeonAuthOauthProviderErrors, AddNeonAuthOauthProviderResponse, AddNeonAuthOauthProviderResponses, AddProjectJwksData, AddProjectJwksError, AddProjectJwksErrors, AddProjectJwksRequest, AddProjectJwksResponse, AddProjectJwksResponses, AdvisorCategory, AdvisorIssue, AllowedIps, AnnotationCreateValueRequest, AnnotationData, AnnotationObjectData, AnnotationResponse, AnnotationValueData, AnnotationsMapResponse, AnonymizationRunMetadata, AnonymizedBranchStatusResponse, ApiKeyCreateRequest, ApiKeyCreateResponse, ApiKeyCreatorData, ApiKeyRevokeResponse, ApiKeysListResponseItem, ApplicationType, AssignOrganizationVpcEndpointData, AssignOrganizationVpcEndpointError, AssignOrganizationVpcEndpointErrors, AssignOrganizationVpcEndpointResponses, AssignProjectVpcEndpointData, AssignProjectVpcEndpointError, AssignProjectVpcEndpointErrors, AssignProjectVpcEndpointResponses, AuthDetailsResponse, AvailablePreloadLibraries, AvailablePreloadLibrary, BackupSchedule, BackupScheduleItem, BillingAccount, BillingAccountState, BillingPaymentMethod, BillingSubscriptionType, Branch, BranchAnonymizedCreateRequest, BranchConsumption, BranchConsumptionResponse, BranchCreateRequest, BranchCreateRequestEndpointOptions, BranchOperations, BranchRecoverResponse, BranchRecoveryInfo, BranchResponse, BranchRestoreRequest, BranchRestoreStatus, BranchRestrictedAction, BranchSchemaCompareResponse, BranchSchemaJson, BranchSchemaResponse, BranchState, BranchUpdateRequest, BranchesCountResponse, BranchesResponse, ClientOptions, ComputeUnit, ConnectionDetails, ConnectionParameters, ConnectionUriResponse, ConnectionUrisOptionalResponse, ConnectionUrisResponse, ConsumptionHistoryGranularity, ConsumptionHistoryPerBranchV2, ConsumptionHistoryPerBranchV2Response, ConsumptionHistoryPerPeriod, ConsumptionHistoryPerPeriodV2, ConsumptionHistoryPerProject, ConsumptionHistoryPerProjectResponse, ConsumptionHistoryPerProjectV2, ConsumptionHistoryPerProjectV2Response, ConsumptionHistoryPerTimeframe, ConsumptionHistoryPerTimeframeV2, ConsumptionHistoryQueryMetrics, ConsumptionMetricValue, ConvertUserToOrgRequest, CountProjectBranchesData, CountProjectBranchesError, CountProjectBranchesErrors, CountProjectBranchesResponse, CountProjectBranchesResponses, CreateApiKeyData, CreateApiKeyError, CreateApiKeyErrors, CreateApiKeyResponse, CreateApiKeyResponses, CreateBranchNeonAuthNewUserData, CreateBranchNeonAuthNewUserError, CreateBranchNeonAuthNewUserErrors, CreateBranchNeonAuthNewUserRequest, CreateBranchNeonAuthNewUserResponse, CreateBranchNeonAuthNewUserResponses, CreateNeonAuthData, CreateNeonAuthError, CreateNeonAuthErrors, CreateNeonAuthIntegrationData, CreateNeonAuthIntegrationError, CreateNeonAuthIntegrationErrors, CreateNeonAuthIntegrationResponse, CreateNeonAuthIntegrationResponses, CreateNeonAuthNewUserData, CreateNeonAuthNewUserError, CreateNeonAuthNewUserErrors, CreateNeonAuthNewUserResponse, CreateNeonAuthNewUserResponses, CreateNeonAuthProviderSdkKeysData, CreateNeonAuthProviderSdkKeysError, CreateNeonAuthProviderSdkKeysErrors, CreateNeonAuthProviderSdkKeysResponse, CreateNeonAuthProviderSdkKeysResponses, CreateNeonAuthResponse, CreateNeonAuthResponses, CreateOrgApiKeyData, CreateOrgApiKeyError, CreateOrgApiKeyErrors, CreateOrgApiKeyResponse, CreateOrgApiKeyResponses, CreateOrganizationInvitationsData, CreateOrganizationInvitationsError, CreateOrganizationInvitationsErrors, CreateOrganizationInvitationsResponse, CreateOrganizationInvitationsResponses, CreateProjectBranchAnonymizedData, CreateProjectBranchAnonymizedError, CreateProjectBranchAnonymizedErrors, CreateProjectBranchAnonymizedResponse, CreateProjectBranchAnonymizedResponses, CreateProjectBranchData, CreateProjectBranchDataApiData, CreateProjectBranchDataApiError, CreateProjectBranchDataApiErrors, CreateProjectBranchDataApiResponse, CreateProjectBranchDataApiResponses, CreateProjectBranchDatabaseData, CreateProjectBranchDatabaseError, CreateProjectBranchDatabaseErrors, CreateProjectBranchDatabaseResponse, CreateProjectBranchDatabaseResponses, CreateProjectBranchError, CreateProjectBranchErrors, CreateProjectBranchResponse, CreateProjectBranchResponses, CreateProjectBranchRoleData, CreateProjectBranchRoleError, CreateProjectBranchRoleErrors, CreateProjectBranchRoleResponse, CreateProjectBranchRoleResponses, CreateProjectData, CreateProjectEndpointData, CreateProjectEndpointError, CreateProjectEndpointErrors, CreateProjectEndpointResponse, CreateProjectEndpointResponses, CreateProjectError, CreateProjectErrors, CreateProjectResponse, CreateProjectResponses, CreateProjectTransferRequestData, CreateProjectTransferRequestError, CreateProjectTransferRequestErrors, CreateProjectTransferRequestResponse, CreateProjectTransferRequestResponses, CreateSnapshotData, CreateSnapshotError, CreateSnapshotErrors, CreateSnapshotResponse, CreateSnapshotResponses, CurrentUserAuthAccount, CurrentUserInfoAuthResponse, CurrentUserInfoResponse, CursorPagination, CursorPaginationResponse, CursorParam, DataApiCreateRequest, DataApiCreateResponse, DataApiReponse, DataApiSettings, DataApiUpdateRequest, Database, DatabaseCreateRequest, DatabaseOperations, DatabaseResponse, DatabaseUpdateRequest, DatabasesResponse, DefaultEndpointSettings, DeleteBranchNeonAuthOauthProviderData, DeleteBranchNeonAuthOauthProviderError, DeleteBranchNeonAuthOauthProviderErrors, DeleteBranchNeonAuthOauthProviderResponses, DeleteBranchNeonAuthTrustedDomainData, DeleteBranchNeonAuthTrustedDomainError, DeleteBranchNeonAuthTrustedDomainErrors, DeleteBranchNeonAuthTrustedDomainResponses, DeleteBranchNeonAuthUserData, DeleteBranchNeonAuthUserError, DeleteBranchNeonAuthUserErrors, DeleteBranchNeonAuthUserResponse, DeleteBranchNeonAuthUserResponses, DeleteNeonAuthDomainFromRedirectUriWhitelistData, DeleteNeonAuthDomainFromRedirectUriWhitelistError, DeleteNeonAuthDomainFromRedirectUriWhitelistErrors, DeleteNeonAuthDomainFromRedirectUriWhitelistResponses, DeleteNeonAuthIntegrationData, DeleteNeonAuthIntegrationError, DeleteNeonAuthIntegrationErrors, DeleteNeonAuthIntegrationResponses, DeleteNeonAuthOauthProviderData, DeleteNeonAuthOauthProviderError, DeleteNeonAuthOauthProviderErrors, DeleteNeonAuthOauthProviderResponses, DeleteNeonAuthUserData, DeleteNeonAuthUserError, DeleteNeonAuthUserErrors, DeleteNeonAuthUserResponse, DeleteNeonAuthUserResponses, DeleteOrganizationSpendingLimitData, DeleteOrganizationSpendingLimitError, DeleteOrganizationSpendingLimitErrors, DeleteOrganizationSpendingLimitResponse, DeleteOrganizationSpendingLimitResponses, DeleteOrganizationVpcEndpointData, DeleteOrganizationVpcEndpointError, DeleteOrganizationVpcEndpointErrors, DeleteOrganizationVpcEndpointResponses, DeleteProjectBranchData, DeleteProjectBranchDataApiData, DeleteProjectBranchDataApiError, DeleteProjectBranchDataApiErrors, DeleteProjectBranchDataApiResponse, DeleteProjectBranchDataApiResponses, DeleteProjectBranchDatabaseData, DeleteProjectBranchDatabaseError, DeleteProjectBranchDatabaseErrors, DeleteProjectBranchDatabaseResponse, DeleteProjectBranchDatabaseResponses, DeleteProjectBranchError, DeleteProjectBranchErrors, DeleteProjectBranchResponse, DeleteProjectBranchResponses, DeleteProjectBranchRoleData, DeleteProjectBranchRoleError, DeleteProjectBranchRoleErrors, DeleteProjectBranchRoleResponse, DeleteProjectBranchRoleResponses, DeleteProjectData, DeleteProjectEndpointData, DeleteProjectEndpointError, DeleteProjectEndpointErrors, DeleteProjectEndpointResponse, DeleteProjectEndpointResponses, DeleteProjectError, DeleteProjectErrors, DeleteProjectJwksData, DeleteProjectJwksError, DeleteProjectJwksErrors, DeleteProjectJwksResponse, DeleteProjectJwksResponses, DeleteProjectResponse, DeleteProjectResponses, DeleteProjectVpcEndpointData, DeleteProjectVpcEndpointError, DeleteProjectVpcEndpointErrors, DeleteProjectVpcEndpointResponses, DeleteSnapshotData, DeleteSnapshotError, DeleteSnapshotErrors, DeleteSnapshotResponse, DeleteSnapshotResponses, DisableNeonAuthData, DisableNeonAuthError, DisableNeonAuthErrors, DisableNeonAuthResponses, Duration, EmptyResponse, EnableNeonAuthIntegrationRequest, EnableOrganizationFeatureRequest, Endpoint, EndpointCreateRequest, EndpointOperations, EndpointPasswordlessSessionAuthRequest, EndpointPoolerMode, EndpointResponse, EndpointSettingsData, EndpointState, EndpointType, EndpointUpdateRequest, EndpointsOptionalResponse, EndpointsResponse, ErrorCode, ExplainData, FeatureFlags, Features, FinalizeRestoreBranchData, FinalizeRestoreBranchError, FinalizeRestoreBranchErrors, FinalizeRestoreBranchResponse, FinalizeRestoreBranchResponses, GeneralError, GetActiveRegionsData, GetActiveRegionsError, GetActiveRegionsErrors, GetActiveRegionsResponse, GetActiveRegionsResponses, GetAnonymizedBranchStatusData, GetAnonymizedBranchStatusError, GetAnonymizedBranchStatusErrors, GetAnonymizedBranchStatusResponse, GetAnonymizedBranchStatusResponses, GetAuthDetailsData, GetAuthDetailsError, GetAuthDetailsErrors, GetAuthDetailsResponse, GetAuthDetailsResponses, GetAvailablePreloadLibrariesData, GetAvailablePreloadLibrariesError, GetAvailablePreloadLibrariesErrors, GetAvailablePreloadLibrariesResponse, GetAvailablePreloadLibrariesResponses, GetConnectionUriData, GetConnectionUriError, GetConnectionUriErrors, GetConnectionUriResponse, GetConnectionUriResponses, GetConsumptionHistoryPerBranchV2Data, GetConsumptionHistoryPerBranchV2Error, GetConsumptionHistoryPerBranchV2Errors, GetConsumptionHistoryPerBranchV2Response, GetConsumptionHistoryPerBranchV2Responses, GetConsumptionHistoryPerProjectData, GetConsumptionHistoryPerProjectError, GetConsumptionHistoryPerProjectErrors, GetConsumptionHistoryPerProjectResponse, GetConsumptionHistoryPerProjectResponses, GetConsumptionHistoryPerProjectV2Data, GetConsumptionHistoryPerProjectV2Error, GetConsumptionHistoryPerProjectV2Errors, GetConsumptionHistoryPerProjectV2Response, GetConsumptionHistoryPerProjectV2Responses, GetCurrentUserInfoData, GetCurrentUserInfoError, GetCurrentUserInfoErrors, GetCurrentUserInfoResponse, GetCurrentUserInfoResponses, GetCurrentUserOrganizationsData, GetCurrentUserOrganizationsError, GetCurrentUserOrganizationsErrors, GetCurrentUserOrganizationsResponse, GetCurrentUserOrganizationsResponses, GetMaskingRulesData, GetMaskingRulesError, GetMaskingRulesErrors, GetMaskingRulesResponse, GetMaskingRulesResponses, GetNeonAuthAllowLocalhostData, GetNeonAuthAllowLocalhostError, GetNeonAuthAllowLocalhostErrors, GetNeonAuthAllowLocalhostResponse, GetNeonAuthAllowLocalhostResponses, GetNeonAuthData, GetNeonAuthEmailAndPasswordConfigData, GetNeonAuthEmailAndPasswordConfigError, GetNeonAuthEmailAndPasswordConfigErrors, GetNeonAuthEmailAndPasswordConfigResponse, GetNeonAuthEmailAndPasswordConfigResponses, GetNeonAuthEmailProviderData, GetNeonAuthEmailProviderError, GetNeonAuthEmailProviderErrors, GetNeonAuthEmailProviderResponse, GetNeonAuthEmailProviderResponses, GetNeonAuthEmailServerData, GetNeonAuthEmailServerError, GetNeonAuthEmailServerErrors, GetNeonAuthEmailServerResponse, GetNeonAuthEmailServerResponses, GetNeonAuthError, GetNeonAuthErrors, GetNeonAuthPhoneNumberPluginData, GetNeonAuthPhoneNumberPluginError, GetNeonAuthPhoneNumberPluginErrors, GetNeonAuthPhoneNumberPluginResponse, GetNeonAuthPhoneNumberPluginResponses, GetNeonAuthPluginConfigsData, GetNeonAuthPluginConfigsError, GetNeonAuthPluginConfigsErrors, GetNeonAuthPluginConfigsResponse, GetNeonAuthPluginConfigsResponses, GetNeonAuthResponse, GetNeonAuthResponses, GetNeonAuthWebhookConfigData, GetNeonAuthWebhookConfigError, GetNeonAuthWebhookConfigErrors, GetNeonAuthWebhookConfigResponse, GetNeonAuthWebhookConfigResponses, GetOrganizationData, GetOrganizationError, GetOrganizationErrors, GetOrganizationInvitationsData, GetOrganizationInvitationsError, GetOrganizationInvitationsErrors, GetOrganizationInvitationsResponse, GetOrganizationInvitationsResponses, GetOrganizationMemberData, GetOrganizationMemberError, GetOrganizationMemberErrors, GetOrganizationMemberResponse, GetOrganizationMemberResponses, GetOrganizationMembersData, GetOrganizationMembersError, GetOrganizationMembersErrors, GetOrganizationMembersResponse, GetOrganizationMembersResponses, GetOrganizationResponse, GetOrganizationResponses, GetOrganizationSpendingLimitData, GetOrganizationSpendingLimitError, GetOrganizationSpendingLimitErrors, GetOrganizationSpendingLimitResponse, GetOrganizationSpendingLimitResponses, GetOrganizationVpcEndpointDetailsData, GetOrganizationVpcEndpointDetailsError, GetOrganizationVpcEndpointDetailsErrors, GetOrganizationVpcEndpointDetailsResponse, GetOrganizationVpcEndpointDetailsResponses, GetProjectAdvisorSecurityIssuesData, GetProjectAdvisorSecurityIssuesError, GetProjectAdvisorSecurityIssuesErrors, GetProjectAdvisorSecurityIssuesResponse, GetProjectAdvisorSecurityIssuesResponses, GetProjectBranchData, GetProjectBranchDataApiData, GetProjectBranchDataApiError, GetProjectBranchDataApiErrors, GetProjectBranchDataApiResponse, GetProjectBranchDataApiResponses, GetProjectBranchDatabaseData, GetProjectBranchDatabaseError, GetProjectBranchDatabaseErrors, GetProjectBranchDatabaseResponse, GetProjectBranchDatabaseResponses, GetProjectBranchError, GetProjectBranchErrors, GetProjectBranchResponse, GetProjectBranchResponses, GetProjectBranchRoleData, GetProjectBranchRoleError, GetProjectBranchRoleErrors, GetProjectBranchRolePasswordData, GetProjectBranchRolePasswordError, GetProjectBranchRolePasswordErrors, GetProjectBranchRolePasswordResponse, GetProjectBranchRolePasswordResponses, GetProjectBranchRoleResponse, GetProjectBranchRoleResponses, GetProjectBranchSchemaComparisonData, GetProjectBranchSchemaComparisonError, GetProjectBranchSchemaComparisonErrors, GetProjectBranchSchemaComparisonResponse, GetProjectBranchSchemaComparisonResponses, GetProjectBranchSchemaData, GetProjectBranchSchemaError, GetProjectBranchSchemaErrors, GetProjectBranchSchemaResponse, GetProjectBranchSchemaResponses, GetProjectData, GetProjectEndpointData, GetProjectEndpointError, GetProjectEndpointErrors, GetProjectEndpointResponse, GetProjectEndpointResponses, GetProjectError, GetProjectErrors, GetProjectJwksData, GetProjectJwksError, GetProjectJwksErrors, GetProjectJwksResponse, GetProjectJwksResponses, GetProjectOperationData, GetProjectOperationError, GetProjectOperationErrors, GetProjectOperationResponse, GetProjectOperationResponses, GetProjectResponse, GetProjectResponses, GetSnapshotScheduleData, GetSnapshotScheduleError, GetSnapshotScheduleErrors, GetSnapshotScheduleResponse, GetSnapshotScheduleResponses, GrantPermissionToProjectData, GrantPermissionToProjectError, GrantPermissionToProjectErrors, GrantPermissionToProjectRequest, GrantPermissionToProjectResponse, GrantPermissionToProjectResponses, HealthCheck, IdentityProviderId, Invitation, InvitationCreateRequest, Jwks, JwksCreationOperation, JwksResponse, LimitParam, Limits, LimitsUnsatisfiedResponse, LinkedAuthAccount, ListApiKeysData, ListApiKeysError, ListApiKeysErrors, ListApiKeysResponse, ListApiKeysResponses, ListBranchNeonAuthOauthProvidersData, ListBranchNeonAuthOauthProvidersError, ListBranchNeonAuthOauthProvidersErrors, ListBranchNeonAuthOauthProvidersResponse, ListBranchNeonAuthOauthProvidersResponses, ListBranchNeonAuthTrustedDomainsData, ListBranchNeonAuthTrustedDomainsError, ListBranchNeonAuthTrustedDomainsErrors, ListBranchNeonAuthTrustedDomainsResponse, ListBranchNeonAuthTrustedDomainsResponses, ListNeonAuthIntegrationsData, ListNeonAuthIntegrationsError, ListNeonAuthIntegrationsErrors, ListNeonAuthIntegrationsResponse, ListNeonAuthIntegrationsResponse2, ListNeonAuthIntegrationsResponses, ListNeonAuthOauthProvidersData, ListNeonAuthOauthProvidersError, ListNeonAuthOauthProvidersErrors, ListNeonAuthOauthProvidersResponse, ListNeonAuthOauthProvidersResponse2, ListNeonAuthOauthProvidersResponses, ListNeonAuthRedirectUriWhitelistDomainsData, ListNeonAuthRedirectUriWhitelistDomainsError, ListNeonAuthRedirectUriWhitelistDomainsErrors, ListNeonAuthRedirectUriWhitelistDomainsResponse, ListNeonAuthRedirectUriWhitelistDomainsResponses, ListOrgApiKeysData, ListOrgApiKeysError, ListOrgApiKeysErrors, ListOrgApiKeysResponse, ListOrgApiKeysResponses, ListOrganizationVpcEndpointsAllRegionsData, ListOrganizationVpcEndpointsAllRegionsError, ListOrganizationVpcEndpointsAllRegionsErrors, ListOrganizationVpcEndpointsAllRegionsResponse, ListOrganizationVpcEndpointsAllRegionsResponses, ListOrganizationVpcEndpointsData, ListOrganizationVpcEndpointsError, ListOrganizationVpcEndpointsErrors, ListOrganizationVpcEndpointsResponse, ListOrganizationVpcEndpointsResponses, ListProjectBranchDatabasesData, ListProjectBranchDatabasesError, ListProjectBranchDatabasesErrors, ListProjectBranchDatabasesResponse, ListProjectBranchDatabasesResponses, ListProjectBranchEndpointsData, ListProjectBranchEndpointsError, ListProjectBranchEndpointsErrors, ListProjectBranchEndpointsResponse, ListProjectBranchEndpointsResponses, ListProjectBranchRolesData, ListProjectBranchRolesError, ListProjectBranchRolesErrors, ListProjectBranchRolesResponse, ListProjectBranchRolesResponses, ListProjectBranchesData, ListProjectBranchesError, ListProjectBranchesErrors, ListProjectBranchesResponse, ListProjectBranchesResponses, ListProjectEndpointsData, ListProjectEndpointsError, ListProjectEndpointsErrors, ListProjectEndpointsResponse, ListProjectEndpointsResponses, ListProjectOperationsData, ListProjectOperationsError, ListProjectOperationsErrors, ListProjectOperationsResponse, ListProjectOperationsResponses, ListProjectPermissionsData, ListProjectPermissionsError, ListProjectPermissionsErrors, ListProjectPermissionsResponse, ListProjectPermissionsResponses, ListProjectVpcEndpointsData, ListProjectVpcEndpointsError, ListProjectVpcEndpointsErrors, ListProjectVpcEndpointsResponse, ListProjectVpcEndpointsResponses, ListProjectsData, ListProjectsError, ListProjectsErrors, ListProjectsResponse, ListProjectsResponses, ListSharedProjectsData, ListSharedProjectsError, ListSharedProjectsErrors, ListSharedProjectsResponse, ListSharedProjectsResponses, ListSnapshotsData, ListSnapshotsError, ListSnapshotsErrors, ListSnapshotsResponse, ListSnapshotsResponses, MaintenanceWindow, MaskingRule, MaskingRulesResponse, MaskingRulesUpdateRequest, Member, MemberRole, MemberUserInfo, MemberWithUser, NeonAuthAddDomainToRedirectUriWhitelistRequest, NeonAuthAddOAuthProviderRequest, NeonAuthAllowLocalhostResponse, NeonAuthConfigResponse, NeonAuthConfigUpdate, NeonAuthCreateAuthProviderSdkKeysRequest, NeonAuthCreateIntegrationRequest, NeonAuthCreateIntegrationResponse, NeonAuthCreateNewUserRequest, NeonAuthCreateNewUserResponse, NeonAuthDeleteDomainFromRedirectUriWhitelistItem, NeonAuthDeleteDomainFromRedirectUriWhitelistRequest, NeonAuthEmailAndPasswordConfig, NeonAuthEmailAndPasswordConfigUpdate, NeonAuthEmailServerConfig, NeonAuthEmailVerificationMethod, NeonAuthIntegration, NeonAuthMagicLinkConfig, NeonAuthMagicLinkConfigUpdate, NeonAuthOauthProvider, NeonAuthOauthProviderId, NeonAuthOauthProviderType, NeonAuthOrganizationConfig, NeonAuthOrganizationConfigUpdate, NeonAuthPhoneNumberConfig, NeonAuthPhoneNumberConfigUpdate, NeonAuthPluginConfigs, NeonAuthProviderProjectOwnedBy, NeonAuthProviderProjectTransferStatus, NeonAuthRedirectUriWhitelistDomain, NeonAuthRedirectUriWhitelistResponse, NeonAuthSupportedAuthProvider, NeonAuthTransferAuthProviderProjectRequest, NeonAuthTransferAuthProviderProjectResponse, NeonAuthUpdateOAuthProviderRequest, NeonAuthWebhookConfig, Operation, OperationAction, OperationResponse, OperationStatus, OperationsResponse, OrgApiKeyCreateRequest, OrgApiKeyCreateResponse, OrgApiKeyRevokeResponse, OrgApiKeysListResponseItem, OrgDeletionConditionName, Organization, OrganizationCreateRequest, OrganizationGuest, OrganizationGuestsResponse, OrganizationInvitationsResponse, OrganizationInviteCreateRequest, OrganizationInviteUpdateRequest, OrganizationInvitesCreateRequest, OrganizationMemberUpdateRequest, OrganizationMembersResponse, OrganizationMembership, OrganizationMembershipsResponse, OrganizationsResponse, OrganizationsUpdateRequest, Pagination, PaginationResponse, PaymentSource, PaymentSourceBankCard, PgSettingsData, PgVersion, PgbouncerSettingsData, PlanDetails, PlanVersion, PreloadLibraries, Project, ProjectAuditLogLevel, ProjectCreateRequest, ProjectJwksResponse, ProjectLimits, ProjectListItem, ProjectOwnerData, ProjectPermission, ProjectPermissionLevel, ProjectPermissions, ProjectQuota, ProjectRecoverResponse, ProjectResponse, ProjectSettingsData, ProjectTransferRequestResponse, ProjectUpdateRequest, ProjectsApplicationsMapResponse, ProjectsBranchesCountResponse, ProjectsCountResponse, ProjectsIntegrationsMapResponse, ProjectsResponse, ProjectsWithIntegrationResponse, Provisioner, RecoverProjectData, RecoverProjectError, RecoverProjectErrors, RecoverProjectResponse, RecoverProjectResponses, RegionResponse, RemoveOrganizationMemberData, RemoveOrganizationMemberError, RemoveOrganizationMemberErrors, RemoveOrganizationMemberResponse, RemoveOrganizationMemberResponses, ResetProjectBranchRolePasswordData, ResetProjectBranchRolePasswordError, ResetProjectBranchRolePasswordErrors, ResetProjectBranchRolePasswordResponse, ResetProjectBranchRolePasswordResponses, RestartProjectEndpointData, RestartProjectEndpointError, RestartProjectEndpointErrors, RestartProjectEndpointResponse, RestartProjectEndpointResponses, RestoreProjectBranchData, RestoreProjectBranchError, RestoreProjectBranchErrors, RestoreProjectBranchResponse, RestoreProjectBranchResponses, RestoreSnapshotData, RestoreSnapshotError, RestoreSnapshotErrors, RestoreSnapshotResponse, RestoreSnapshotResponses, RevokeApiKeyData, RevokeApiKeyError, RevokeApiKeyErrors, RevokeApiKeyResponse, RevokeApiKeyResponses, RevokeOrgApiKeyData, RevokeOrgApiKeyError, RevokeOrgApiKeyErrors, RevokeOrgApiKeyResponse, RevokeOrgApiKeyResponses, RevokePermissionFromProjectData, RevokePermissionFromProjectError, RevokePermissionFromProjectErrors, RevokePermissionFromProjectResponse, RevokePermissionFromProjectResponses, Role, RoleCreateRequest, RoleOperations, RolePasswordResponse, RoleResponse, RolesResponse, SendNeonAuthTestEmailData, SendNeonAuthTestEmailError, SendNeonAuthTestEmailErrors, SendNeonAuthTestEmailRequest, SendNeonAuthTestEmailResponse, SendNeonAuthTestEmailResponse2, SendNeonAuthTestEmailResponses, SetDefaultProjectBranchData, SetDefaultProjectBranchError, SetDefaultProjectBranchErrors, SetDefaultProjectBranchResponse, SetDefaultProjectBranchResponses, SetOrganizationSpendingLimitData, SetOrganizationSpendingLimitError, SetOrganizationSpendingLimitErrors, SetOrganizationSpendingLimitResponse, SetOrganizationSpendingLimitResponses, SetSnapshotScheduleData, SetSnapshotScheduleError, SetSnapshotScheduleErrors, SetSnapshotScheduleResponse, SetSnapshotScheduleResponses, SharedEmailServer, Snapshot, SnapshotUpdateRequest, SortOrderParam, SpendingLimitResponse, SpendingLimitUpdateRequest, StandardEmailServer, StartAnonymizationData, StartAnonymizationError, StartAnonymizationErrors, StartAnonymizationResponse, StartAnonymizationResponses, StartProjectEndpointData, StartProjectEndpointError, StartProjectEndpointErrors, StartProjectEndpointResponse, StartProjectEndpointResponses, StatementData, StatementResult, SupportTicket, SupportTicketSeverity, SuspendProjectEndpointData, SuspendProjectEndpointError, SuspendProjectEndpointErrors, SuspendProjectEndpointResponse, SuspendProjectEndpointResponses, SuspendTimeoutSeconds, SystemStatusSummaryResponse, TelemetryAuthentication, TelemetryConfig, TelemetryConnection, TelemetryResource, TimeoutParam, TransferNeonAuthProviderProjectData, TransferNeonAuthProviderProjectError, TransferNeonAuthProviderProjectErrors, TransferNeonAuthProviderProjectResponse, TransferNeonAuthProviderProjectResponses, TransferProjectsFromOrgToOrgData, TransferProjectsFromOrgToOrgError, TransferProjectsFromOrgToOrgErrors, TransferProjectsFromOrgToOrgResponse, TransferProjectsFromOrgToOrgResponses, TransferProjectsFromUserToOrgData, TransferProjectsFromUserToOrgError, TransferProjectsFromUserToOrgErrors, TransferProjectsFromUserToOrgResponse, TransferProjectsFromUserToOrgResponses, TransferProjectsToOrganizationRequest, UpdateBranchNeonAuthOauthProviderData, UpdateBranchNeonAuthOauthProviderError, UpdateBranchNeonAuthOauthProviderErrors, UpdateBranchNeonAuthOauthProviderResponse, UpdateBranchNeonAuthOauthProviderResponses, UpdateMaskingRulesData, UpdateMaskingRulesError, UpdateMaskingRulesErrors, UpdateMaskingRulesResponse, UpdateMaskingRulesResponses, UpdateNeonAuthAllowLocalhostData, UpdateNeonAuthAllowLocalhostError, UpdateNeonAuthAllowLocalhostErrors, UpdateNeonAuthAllowLocalhostRequest, UpdateNeonAuthAllowLocalhostResponse, UpdateNeonAuthAllowLocalhostResponses, UpdateNeonAuthConfigData, UpdateNeonAuthConfigError, UpdateNeonAuthConfigErrors, UpdateNeonAuthConfigResponse, UpdateNeonAuthConfigResponses, UpdateNeonAuthEmailAndPasswordConfigData, UpdateNeonAuthEmailAndPasswordConfigError, UpdateNeonAuthEmailAndPasswordConfigErrors, UpdateNeonAuthEmailAndPasswordConfigResponse, UpdateNeonAuthEmailAndPasswordConfigResponses, UpdateNeonAuthEmailProviderData, UpdateNeonAuthEmailProviderError, UpdateNeonAuthEmailProviderErrors, UpdateNeonAuthEmailProviderResponse, UpdateNeonAuthEmailProviderResponses, UpdateNeonAuthEmailServerData, UpdateNeonAuthEmailServerError, UpdateNeonAuthEmailServerErrors, UpdateNeonAuthEmailServerResponse, UpdateNeonAuthEmailServerResponses, UpdateNeonAuthMagicLinkPluginData, UpdateNeonAuthMagicLinkPluginError, UpdateNeonAuthMagicLinkPluginErrors, UpdateNeonAuthMagicLinkPluginResponse, UpdateNeonAuthMagicLinkPluginResponses, UpdateNeonAuthOauthProviderData, UpdateNeonAuthOauthProviderError, UpdateNeonAuthOauthProviderErrors, UpdateNeonAuthOauthProviderResponse, UpdateNeonAuthOauthProviderResponses, UpdateNeonAuthOrganizationPluginData, UpdateNeonAuthOrganizationPluginError, UpdateNeonAuthOrganizationPluginErrors, UpdateNeonAuthOrganizationPluginResponse, UpdateNeonAuthOrganizationPluginResponses, UpdateNeonAuthPhoneNumberPluginData, UpdateNeonAuthPhoneNumberPluginError, UpdateNeonAuthPhoneNumberPluginErrors, UpdateNeonAuthPhoneNumberPluginResponse, UpdateNeonAuthPhoneNumberPluginResponses, UpdateNeonAuthUserRoleData, UpdateNeonAuthUserRoleError, UpdateNeonAuthUserRoleErrors, UpdateNeonAuthUserRoleRequest, UpdateNeonAuthUserRoleResponse, UpdateNeonAuthUserRoleResponse2, UpdateNeonAuthUserRoleResponses, UpdateNeonAuthWebhookConfigData, UpdateNeonAuthWebhookConfigError, UpdateNeonAuthWebhookConfigErrors, UpdateNeonAuthWebhookConfigResponse, UpdateNeonAuthWebhookConfigResponses, UpdateOrganizationMemberData, UpdateOrganizationMemberError, UpdateOrganizationMemberErrors, UpdateOrganizationMemberResponse, UpdateOrganizationMemberResponses, UpdateProjectBranchData, UpdateProjectBranchDataApiData, UpdateProjectBranchDataApiError, UpdateProjectBranchDataApiErrors, UpdateProjectBranchDataApiResponse, UpdateProjectBranchDataApiResponses, UpdateProjectBranchDatabaseData, UpdateProjectBranchDatabaseError, UpdateProjectBranchDatabaseErrors, UpdateProjectBranchDatabaseResponse, UpdateProjectBranchDatabaseResponses, UpdateProjectBranchError, UpdateProjectBranchErrors, UpdateProjectBranchResponse, UpdateProjectBranchResponses, UpdateProjectData, UpdateProjectEndpointData, UpdateProjectEndpointError, UpdateProjectEndpointErrors, UpdateProjectEndpointResponse, UpdateProjectEndpointResponses, UpdateProjectError, UpdateProjectErrors, UpdateProjectResponse, UpdateProjectResponses, UpdateSnapshotData, UpdateSnapshotError, UpdateSnapshotErrors, UpdateSnapshotResponse, UpdateSnapshotResponses, UpdateUserInfoRequest, UserDeletionConditionName, VerifyUserPasswordRequest, VpcEndpoint, VpcEndpointAssignment, VpcEndpointDetails, VpcEndpointWithRegion, VpcEndpointsResponse, VpcEndpointsWithRegionResponse };
//# sourceMappingURL=types.gen.d.ts.map