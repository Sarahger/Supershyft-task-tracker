# Production: URL of your deployed FastAPI backend (no trailing slash)
# Example: https://notion-task-tracker-api.onrender.com
VITE_API_URL=

# Leave empty for local dev — Vite proxies /api to localhost:8000
