# Production: leave empty on Vercel (API is same origin at /api)
# Local dev: leave empty — Vite proxies /api to localhost:8000
# Only set if API is on a different domain:
# VITE_API_URL=https://example.com
